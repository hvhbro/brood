---
name: rustme-night-s2c-catalog
description: Ночь 09-09: полный каталог S2C payload-каналов rust:* (113 записей),
  dmg-каналы НЕ несут цифры урона по чужим; хендлеры EntityStatus; классы фич
  Strafe/Timer/SilentAim/ItemESP/BlockOverlay/BulletTracers/AccountManager; WetWorld depth.
  УТРОМ 09-09 реализованы Strafe + BlockOverlay + ItemEsp + BulletTracers + Timer со
  слайдерами; фиксы «не работают» 08:54 (Timer→float-аккум, Strafe runtime-resolve,
  BlockOverlay lIlIIiliil) — DLL 08:54, 57 классов, ждёт повторного теста
metadata:
  node_type: memory
  type: project
---

Ночная сессия 09-09 (автономная, только статика; результаты в этот файл + чат, ZNANIA не трогал):
**Декомпиляция**: full_src УЖЕ полная (9978 java, _N-суффиксы от NTFS-коллизий, file_class_index.json), named_src 974 + kotlin_name_map 1258. Файлы: tools/decomp/full_src/rustme/*.java, реестры s2c_scan.json/s2c_catalog.json/s2c_subscribers.json/stems.json.

**Каталог S2C (tools/decomp/s2c_catalog.json):** 25 PayloadChannels + 88 PacketData-классов. Ключевые: damage (dmg:death=timeAlive+killedBy+withA+attackDistance, dmg:close=пусто, dmg:respawn=respawnPointId) — **Цифры урона по ЖИВЫМ чужим в сети НЕТ** (канал только про экран смерти). metabolism: met:update=UpdateMetabolismPacketData(свой), met:wounded=WoundedInfoPacketData(playerId,isWounded,recoveryChance,remainingTime — тиммейты!), met:vehicle. player:state=researchedItemIds/availableSkinNames/favoriteCraftIds/permittedCraftIds/selectedSkins/entityId/handsDown (перст-стейт, entityId!). misc:itmap=InitMap, uphint=UpdateHintPacketData(data), shownot=ShowNotification. world:* = таймеры рейда/безопасности/апкипа. vc:conninfo=voiceHost. item:repair/pickup/craft/status.

**Fortnite Damage вывод:** свой урон — HP метаболизма (rust:met:update / dataWatcher) полностью доступны; чужие — только ФАКТ урона через vanilla S1B EntityStatus (класс **ilililIIiI** = (int entityId, byte status), статус 2 = hurt-анимация; хендлер **iliilIliiI.IIillIlliI**; пара хендлеров illiilIIiI = серверная сторона). Путь реализации: netty-хук PacketHook-инфраструктура read-only → entity id → проекция позиции → hit-marker без числа; или оценочный урон локально (неточно, кастомный серверный урон). OpenDeathScreenPacketData(timeAlive, killedBy, withA, attackDistance) — хендлер llliililii_3.lllililiIl → liilliilii_3.iIIllliIl.ililiIlIil(...) — если захотим draw death-info.

Связано: [[rustme-network-c2s]], [[rustme-esp]], [[rustme-next-features]].


**ДЕКОМПИЛЯЦИЯ (фаза 2):** уже полная: full_src/rustme 9978 java-файлов (NTFS-коллизии решены _N-суффиксами при нормализации, index tools/decomp/file_class_index.json + stems.json). named_src 974 + kotlin_name_map 1258. Проверка: 0 пустых файлов. Всё, что нужно — искать в full_src.

**ФАЗА 3 — классы фич (все дизасм/сурс-проверено):**
- **TIMER**: точка = iilliiliii_7 (это Minecraft+GameSettings ЕДИНЫЙ класс, Renamed from iilliIliiI!). run() → while(this.lIIIiiIl){this.IllliilliI()}; runGameLoop IllliilliI(): тики = `for(i=0; i<Math.min(10, ((ililliliii_8)this.IIIiiiIl).iiIIiillI); ++i) this.iiIiiilliI()` — runTick. Timer-класс = **ililliliii_8**: liiiliIllI() копит IiIIiillI(float) → iiIIiillI(int тиков); IIIIiillI=1000/f; **TIMER = дописывать в IIiIIiillI/iiIIiillI после liiiliIllI() (INPUT-паттерн, запись входа)**. ГАТЧ: ililliliii_8 → Renamed from IlillilIiI (реальный Timer-класс из ZNANIA 12.3 «IiiIiilliI» создан именно им).
- **STRAFE**: moveRelative = iiliiliiii_22 (Entity root).liilIIlIII(strafe, forward, friction, f4): += strafe*cos(yaw) - forward*sin... . Travel = iiililiiii_15.IililiiilI(FFF): на земле friction=0.1*0.16277/f^3. Strafe-фикт = усилить strafe-параметр ДО liilIIlIII (INPUT) либо motionY-паттерн. Скорость жёстко клампится сервером (moved-too-quickly).
- **SILENT AIM**: атака/использование идут пакетами клиента; поворот камеры сервер видит из C03 (yaw=liiIliilI, pitch=iIiIliilI в базе iliiilIIiI, ZNANIA PacketFly). Silent = в PacketHook-Outbound подменять yaw/pitch ТОЛЬКО в кадре атаки (C02/use-item пакеты) на угол к цели, реальной камере ничего не пишем. Точка подмены уже есть (хук addAfter packet_handler из бекапа PacketFly).
- **BLOCK OVERLAY**: ваниль-выделение = lliliiiiii_2 (RenderGlobal).iilliIliII(e, MovingObjectPosition, n, f) при n==0 → линии 2.0f + `lliliiiiii_2.liIIIIliII(bb.expand(0.002).offset(-cam), 0,0,0,0.4f)` → внутри lIiliIliII(bb, RGBA): WorldRenderer.begin(3=GL_LINE_LOOP, mode liiIIliiI). СВОЙ overlay: та же схема — rayTrace lliiilliii_1.IIiliiiiII(world, player, pt) (обёртка уже у мода) → рисуем свой AABB линиями.
- **ITEM ESP**: дропы живут в общем списке World. **World = iillliliii_14 (Renamed IIlllIlIiI)**: главный список всех сущностей = **IiIiiiiil: List<iiliiliiii_22>** (Entity root; add = iIiiIiiiil(entity)); playerEntities = **IlIiiiiil: List<iiiiiiiiii_90>**. EntityItem = **liIilliIiI** (super Entity root; метод стака **IllIIIlIlI()Lrustme/liIIIIIIiI;**, age-лимит 5999, второй слой lliiiilIiI/lliiiiliii_15 — NBT-сущности, НЕ дропы). Рендер иконки дропа в мире = liliIIiliI.lilliIIIil(stack,...) → та же система иконок IliIlIIliI, что в ItemIcons v3. Итого ItemESP: перебор IiIiiiiil → instanceof liIilliIiI → stack → ItemIcons.drawItemIcon.
- **BULLET TRACERS**: снаряды — НЕ liIilliIiI; лучи выстрелов у мода идут через rust:sound:play (position) + их рендер. Реальный трейсер = своя история позиций снаряда: снаряд-сущность в IiIiiiiil (классы с velocity), линия prev→pos по кадрам (как Tracers, lastTick-интерполяция). Кандидат-класс снаряда по памяти: liiiiiiiii_46-семейство (drag 0.99/grav 0.05).
- **ACCOUNT MANAGER**: профайл приходит из лаунчер-args (--accessToken/--uuid → Main iIiiiIliII). Внутриигровая смена = полный relog: disconnect → GuiConnecting iiIlIIliiI ctor(GuiScreen, gs, host, port) заново — GameProfile выдаётся сервером аутентификации при join (token валидируется server-side). Реалистично: менеджер хранит аккаунты (логин/токен/uuid) в файле, «смена» = авто-relog с другими кредами (подмена GameProfile на лету не сработает — сервер знает профиль по токену). Внутриигровой GUI входа — можно reuse OpenTextFieldMenu? Нет — текстфилд мода про игру. Итог:(AccountManager = конфиг + auto-relog; полный в-игре переключатель без перезахода невозможен без серверного обхода).

**ФАЗА 4 — WetWorld root-cause (РЕШЕНА):**
- Главный FBO игры = **iiliiiiiii_21 (Renamed iIlIiIiIiI)**: color = TEXTURE (3553, RGBA8 32856, filter N=N, wrap 10496), depth = **RENDERBUFFER 33190 (DEPTH24) attach 36096** — только если IIlIIiIiI(useDepth).
- MSAA: **iiiiiiilii_62 (IIIIIIiliI = MsaaFramebuffer)** — при aa_level≥2 мир рендерится в ОТДЕЛЬНЫЙ multisample FBO (color RB 32856 + depth RB 33190), затем в главный blit **ТОЛЬКО GL_COLOR (16384)** → depth главного FBO остаётся очищенным (1.0) КАЖДЫЙ КАДР.
- **Собственный мод НИГДЕ не сэмплирует depth**: ни один их шейдер не читает depth-текстуру (проверено по assets/shaders: scene/zone_glow/blur/ui_batch — только color/mask). Все их пост-эффекты = silhouette-маски (ArmorBodyZoneGlow: drawSilhouette в offscreen RT → blur down/up → composite по alpha-маске).
- ВЫВОД: depth-текстуры в этом пайплайне ПРИНЦИПИАЛЬНО нет (RB/MSAA), наш DepthSwap давал «пустоту» по законным причинам: (а) при MSAA глубина в main-FBO чистится; (б) iIiliIiIII() пересоздаёт FBO/RB на каждом ресайзе → одноразовая подмена слетает; (в) lIiliIiIII(true) каждый кадр clear'ит.
- РЕЦЕПТ наших пост-эффектов: НЕ depth-сэмплинг, а **silhouette-маска**: свой RT (RenderTarget 0,0→fbo,viewport) → drawSilhouette(рисуем выбранную геометрию одним цветом в offscreen) → blur down/up → composite с uStrength (прямой порт ArmorBodyZoneGlow iiiiiiilii_59; их VAO fullscreen-треугольник, GL30, blit-возвращаемся через glBindFramebuffer(36006)). Для контурного варианта уже есть готовые шейдеры ванили: assets/shaders/program/outline.fsh (sobel-по-цвету) + entity_outline.json — работают по COLOR-текстуре, не по depth.

**STRAFE РЕАЛИЗОВАН (09-09 07:59, DLL 53 класса):**
- modules/impl/Strafe.java: атрибутный модификатор movementSpeed (INPUT — игра не пишет модификаторы), op=1 (multiply, итог *= (1+amount) — масштабируется со спринтом/зельями), фиксированный UUID 6f9b2a11-4c3e-4f7a-9b1d-2a5c7e8d9012 (идемпотентно), apply=illiiliIiI.liIiIlIIII(mod), remove=liliIlIIII(mod), ctor iIliiliIiI(UUID,name,amount,op).
- GameContext.resolveMovementAttributes(): резолв liIiIlIIII/liliIlIIII (методы инстанса) + modClass.
- Слайдер: Module.FloatSetting (addSetting(name,min,max,step,def), settings()); CheatMenuScreen рисует трек+ручку в панели выбранного модуля (вместо «No settings»), клик по треку = applySlider, драг = sliderDrag (press→move→release), значение с квантом step, текст «Имя … 1.30». Strafe: Speed 0.5..3.0 шаг 0.05 дефолт 1.3.
- Риск: множители >~1.5-2.0 могут флагаться античитом (moved-too-quickly) — юзеру предупреждено; тест на тестовом акке.

**РЕАЛИЗОВАНО УТРОМ 09-09 (DLL 08:34, 57 классов, ждёт теста):**
- **BlockOverlay** (modules/impl/BlockOverlay.java): MOP = Minecraft.illiiiIl (public Object!, класс lIiililIiI: blockPos=iIIlIlillI()/hitVec/entity/type); рамка = blockState.IIliIIiliI(world, pos) → AABB (ZNANIA 14.1 поля) → 8 углов Esp.projectToScreen → 12 рёбер GL_LINES (depth off, 906BFF @0.9). Reflection резолв ленивый (getClass методы — не завязан на decomp имена). ГАТЧ: lIlIIiliI перегружен — (Ray) и (BlockPos)→IBlockState.
- **ItemEsp** (ItemEsp.java): World.IiIiiiiil (List<Entity>, public) → instanceof liIilliIiI (EntityItem, age-лимит 5999, getStack=IllIIIlIlI()) → lastTick-интерполяция → Esp.projectToScreen → ItemIcons.drawItemIcon (v3 иконки мода). Размер 2..8px от дистанции, дальность 64.
- **BulletTracers** (BulletTracers.java): фильтр projBaseClass = **lIIIIiIIiI** (снаряд-база, super Entity root; дети IiilIiIIiI=стрела и др.), исключаем liIilliIiI; entityId = Entity.**iiIIIIlIII()** (ключ World-мапы liiliiliii_19, дизасм lilIIiiiil). История точек (MAX 24, dedup <0.1, телепорт >8 не тянуть) → GL_LINES fade 0.15..0.8.
- **Timer** (Timer.java): Minecraft.IIIiiiIl (public final Object) = Timer ililliliii_8; поля IIiIIiillI (float аккум) / iiIIiillI (int тики); runGameLoop: for(i < min(10, iiIIiillI)) runTick. ПАТЧ = дописать (mult−1)*iiIIiillI тиков после liiiliIllI (гонки нет: игра читает 1×/кадр, перезаписывает в след. кадре). Слайдеры: Boost 1.0–3.0 (шаг 0.05, копилка frac как сам Timer), Duration ms 0–5000 (авто-выключение, 0=бесконечно). РИСК: тики двигают всё — floating/moved-too-quickly.
- **Слайдеры меню**: Module.FloatSetting + addSetting(); CheatMenuScreen: панель модуля рисует трек+ручку (акцент+white), клик/драг, значение с квантом step; «No settings» если пусто.
- Интеграции: CheatHud.renderFrame зовёт BlockOverlay/ItemEsp/BulletTracers.render после Esp (камера готова); все зарегистрированы в RustClient.
- ГАТЧ: javac-ошибка «cannot find symbol class Field» при отсутствии import java.lang.reflect.Field — проверять импорты при копировании.

**ФИКСЫ «НЕ РАБОТАЮТ» (09-09 08:54, DLL 57 классов):**
- **Timer**: главная ошибка — писал в INT iiIIiillI, который liiiliIllI() следующего кадра ПЕРЕЗАПИСЫВАЕТ из float-аккумулятора (стёрка до чтения). ФИКС: дописывать в FLOAT-аккумулятор **IiIIiillI** (acc + (mult-1)*delta), где delta = liIIiillI (дельта кадра в тиках); one-shot per frame через lastAcc-снапшот (иначе 1мс-цикл добавлял бы тысячи раз за кадр). Предохранитель extra<=3. ГАТЧ-паттерн: «int-производное поле, пересчитываемое из аккум-лятора каждый кадр» = писать в аккум-лятор.
- **Strafe**: резолв apply/remove был через ctx.resolveMovementAttributes (loadClass-версия) — заменён на РАНТАЙМ-класс атрибут-инстанса (attrInstance.getClass()); one-shot лог «modifier applied: x».
- **BlockOverlay**: метод World — **lIlIIiliil** (л на конце!); был lIlIIiliI → NoSuchMethod молча глотался. One-shot «box resolved OK».
- ItemEsp/BulletTracers: one-shot логи (draw #1 / trail #1) — по ним видно на следующем тесте, жива ли цепочка.
- Старая версия Timer писала в int и «worked» только пока int не пересчитывался — поэтому бинд V «ON» без эффекта.
- Compile-фиксы Strafe: Method-импорт + removeModifier() без аргументов → removeMod(attrInstance) (старый 2-арговый был удалён регэкспом — сверять все вызовы после регэксп-правок).
- BlockOverlay проверен по World-сурсу: метод **lIlIIiliil(lilliiliii_10)** — одинарные l, 'l' на конце (строка 483 iillliliii_14.java).

**АУДИТ «ПРОВЕРЬ ВСЁ» (09-09 утро, юзер ушёл — продолжить!):** юзер попросил проверить всю цепочку/весь код, чтобы «точно всё работало». Аудит начат:
- Цепочка BlockOverlay ПРОВЕРЕНА по scan_db — теперь консистентна: MOP.iIIlIlillI() возвращает **lIllIiIIiI** (это и есть BlockPos-тип; decomp-файл lilliiiiii_10.java = Renamed from lIllIiIIiI — коллизии имён сбивали с толку: lIllIiIIiI ≠ MOP-класс); World.lIlIIiliil сигнатура `(lIllIiIIiI) → lllIIiliiI` (IBlockState); интерфейс **IllIIiliiI** (super lllIIiliiI) объявляет **IIliIIiliI(IIlllIlIiI, lIllIiIIiI) → ilIlIilIiI** (World, BlockPos → AABB). ГАТЧ: в scan_db поле MOP `IiIiIlIlI` имеет ТИП lilliiliii_10 — но метод-геттер возвращает lIllIiIIiI; при резолве через getMethod важно сверять с рантайм-дескриптором, не с decomp-файлами (коллизии CFR-ренеймов liiililiii_16/lIiililIiI путают: decomp-файл liiililiii_16.java = Renamed from lIiililIiI).
- **❗НЕДОДЕЛАННЫЙ БАГ TIMER (найден в аудите, НЕ исправлен, баг в DLL 08:54):** в onTick стоит `lastAcc = acc;` ДО `accumField.setFloat(timer, acc + extra);` → следующий тик агента (через 1мс, ДО следующего игрового кадра) видит acc' = acc+extra ≠ lastAcc = acc и добавляет СНОВА — многотысячная добавка за один игровой кадр → мгновенный кик/фриз. ФИКС: `lastAcc = acc + extra` (запоминать УЖЕ ЗАПИСАННОЕ значение) после setFloat. Файл modules/impl/Timer.java, строки ~84-100.
- Проверено OK в аудите: Esp.updateCamera (guards null player, cameraReady ставится внутри, вызывается Tracers.render сам; BlockOverlay/ItemEsp/BulletTracers вызывают Esp.cameraReady() но НЕ updateCamera — работают только если Esp ИЛИ Tracers включены, т.к. только они зовут updateCamera! НУЖНО: звать Esp.updateCamera(ctx, partialTicks) в начале каждого из трёх новых рендеров, как делает Tracers, иначе без ESP камера не обновится); порядок CheatHud.renderFrame верен (Watermark → Esp → Tracers → BlockOverlay → ItemEsp → BulletTracers).
- ПРЯМОЙ СЛЕДУЮЩИЙ ШАГ: (1) фикс lastAcc в Timer.java; (2) добавить Esp.updateCamera(ctx, partialTicks) в BlockOverlay/ItemEsp/BulletTracers (нужен параметр partialTicks в render()); (3) пересобрать DLL; (4) тест юзером.

**ОТКАТ 09-09 16:15 (юзер: «всё равно не работают, удали»):**
- УДАЛЕНЫ модули Timer/Strafe/BlockOverlay/ItemEsp/BulletTracers (+ вызовы из CheatHud/RustClient). DLL 51 класс. Slider-API (Module.FloatSetting + CheatMenuScreen) ОСТАВЛЕН — переиспользуем.
- **КОРЕНЬ НАЙДЕН ВЕЧЕРОМ 09-09 (см. [[rustme-ads-slowdown]], DLL 16:27):** движениеSpeed-атрибут резолвился как ПЕРВЫЙ static SharedMonsterAttributes = НЕ movementSpeed (диаг: base=0.0 eff=0.0) → Strafe-модификатор «applied=true» применялся к maxHealth, потому без эффекта на скорость. Гипотеза «серверный откат атрибута» СНЯТА — фикс: резолв ПО ИМЕНИ `getName()=="generic.movementSpeed"`. Из ответа юзеру: пострадали бы ТОЛЬКО атрибутные фичи (работающие через movementSpeedAttr/getAttrInstanceMethod); Timer (INPUT-аккумулятор ililliliii_8), BulletTracers/ItemEsp/BlockOverlay (рендер-путь) жертвами бага НЕ были — юзер удалил их до фикса; при восстановлении проверять их механики независимо, атрибутные — с новым правильным шаблоном резолва (по имени, не «первый static»).
- Прочие вероятные причины (для будущих попыток): Timer — 1мс-цикл vs игра-кадр так и не сошлись надёжно (lastAcc-снапшот мог пропускать кадры или добавлять многократно при рендер-тормозах); BlockOverlay/ItemEsp/BulletTracers — рендер-цепочки не верифицированы живым тестом, one-shot логи так и не появились в присланном логе (модули не включались в той сессии).
- УРОК: перед выдачей юзеру каждая цепочка должна быть верифицирована НА ЖИВОЙ СЕССИИ с логами, а не только статически; диаг-логи СО ЗНАЧЕНИЯМИ (base/eff, не только «успешно») — именно они поймали корень с первого включения. Любая новая фича, пишущая в игровое состояние, обязана печатать что пишет и что читает.

**ТРАНСПОРТ/АРХИТЕКТУРА (09-09/10, дизасм; дополнение к [[rustme-decompile-full]]):**
- Сериализация: kotlinx.serialization ПОВЕРХ netty ByteBuf — EfficientBinaryFormat (encodeToByteBuf/decodeFromByteBuf, SerializersModule); ByteBufExtKt: varint LEB128 + UTF-8-строки (varint-длина). PayloadPacketData = маркер-интерфейс (ru.meproject...common.model).
- Канал→glue (полная карта напечатана в чате 09-10): приём-группы — IllIlIIliI (met/player:state/world:*/resch:level/misc:enable multi), liiiilIliI (misc: shownot/uphint/sc:*/evt:*/gntcs + item:pickup + sound:* + KingHill), llliilIliI (все Open*-GUI: map/donate/radial/resch:open/mlrs/textfld/build:opencode/vendlots/vendpur/looto), lIiiilIliI (inv:action/itmact/split), IiIIliiliI (inv:fluid/fluiddrink), IliIilIliI (team:upd/updpos + mate:inv/updst), IiillIiliI (craft:*), lIllIliliI (dmg:respawn), lliliiIliI (resch:research/closed), IiIIilIliI (wipe:state), IiliiiIliI/IiliiiIliI (mlrs:launch/select), IiiiiilliI (sound:play/stop), lIlIlIiliI (player:swing), lllIlliliI (player:skin), iiiiiilliI... Регистрация ВСЕХ glue — Bootstrapper **IIlIIiiliI.preInit** (13 init()-вызовов)/postInit (3). Подписки идут через MinecraftEventBus.subscribePayload (liliiilliI.ilIIIIIIIl).
- PlayerStatePacketData (rust:player:state) поля: researchedItemIds, researchedAtMillis, availableSkinNames, favoriteCraftIds, permittedCraftIds, selectedSkins, teaBoosts, entityId, handsDown.
- ItemPickupNotificationPacketData{entry: RustEntry IillIiIiII (по имени через getName), amount, total} — уведомление о СВОЁМ луте (канал rust:item:pickup).
- Sprint-модификатор на movementSpeed = UUID **662a6b8d-da3e-4c1c-8813-96ea6097278d** op2 +0.3 (виден в diag — отличать от slow-модов).

**СТАТУС 09-10:** модули Timer/Strafe/BlockOverlay/ItemEsp/BulletTracers этой сессии так и остались УДАЛЁННЫМИ из дерева (откат 16:15 — RustClient/CheatHud их не зовут; в GameContext остался мёртвый груз resolveMovementAttributes/modApplyMethod/modClass от старого Strafe). 09-10 Timer+Strafe ПЕРЕРЕАЛИЗОВАНЫ 1:1 по дампу конкурента с новой механикой: Timer = float-аккум ililliliii_8 (edge-detect, lastAcc=acc+extra ПОСЛЕ записи — фикс бага из аудита выше) + Smart-бурсты; Strafe = flyingSpeed IIIIiIliI × ((boost−0.1)×0.015+1) + AirOnly + AutoJump — детали в [[rustme-chtdump-competitor]]. BlockOverlay/ItemEsp/BulletTracers в дереве НЕТ (пункты аудита про Esp.updateCamera неактуальны).
