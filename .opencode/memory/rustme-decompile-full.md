---
name: rustme-decompile-full
description: Полная декомпиляция rustme (CFR, 9977+974) — карта имён, сеть,
  чужой инвентарь S04, баллистика, эксплойт-поверхность
metadata:
  node_type: memory
  type: project
  originSessionId: sess_65480615-27bb-44e9-bebd-b1e29debe510
---

Сессия 2026-09-08: декомпилирован ВЕСЬ клиент CFR 0.152 (tools/decomp/cfr.jar):
- `full_src/` = 9977 обфусцированных классов (game_cp.jar), `named_src/` = 974 named (dump ru/ + me/).
- `tools/decomp/kotlin_name_map.json` — 1259 CFR-имя → оригинальное Kotlin-имя (из @Metadata d2).
- `tools/decomp/file_class_index.json` — класс → java-файл (NTFS case-коллизии CFR: файл `X_N.java` ≠ CP-имя, смотреть содержимое/индекс).
- ГАТЧ: имена в ZNANIA (dump: IIlIIliIiI…) ≠ имена в full_src (CFR: iiliiliiii_22…) — мост через this_class в scan_db.json / «Renamed from rustme.X» в шапке файла.

СЕТЬ: свой Netty, 1.8-style пакеты (S04-экипировка = `iiililiiii_24`: int entityId + EquipmentSlot + ItemStack → `entity.iIiIliiilI(slot,stack)`; hands-слоты 0/1, armor-слоты 2..8). Custom payload шлюз = `iliililiii_7.IlliillliI(SPacketCustomPayload)`: каналы "astraea"/"astraea-init" → NativeAPI.handlePacket (нить C++), "gun:recoil" → GunRecoilController (float pitch,float yaw,byte), "gun:recoil:animation", default → шина `lilliillii_2.ilIIIIlIIl(new PayloadPacketReceiveEvent(channel,ByteBuf))` = ПОДПИСЫВАЕМАЯ точка перехвата. Отправка: `liliiilliI.IlIIIIIIIl(player, channel, PayloadPacketData)` (EfficientBinaryFormat+kotlinx). PayloadPacketReceiveEvent = `ililliilii_0`.

ЧУЖОЙ ИНВЕНТАРЬ: сервер шлёт MainHand/OffHand + 7 Armor-слотов каждого игрока (EquipmentSlot: 0=MainHand,1=OffHand (hands, index 0..1 в списке 2), Armor1..7 (index 1..7 в списке 7)). У REMOTE `iiililiiii_19` списки: hands `lIiIiIliI`(2), armor `liliiIliI`(7); getter `IiliIiiilI(EquipmentSlot)`. ХОТБАР чужих (30 слотов `iiiiiiiiii_91.lllliIiII`) НЕ синхронизируется — только локальный. Лут-история чужих: рендер рук/брони = клиент точно знает броню+руку+ предмет в руке (для predикта снаряда).

БАЛЛИСТИКА: снаряды = `liiiiiiiii_46` (vanilla-Throwable-физика): tick: motion *= 0.99, motionY -= 0.05, spread = nextGaussian*0.0075*inaccuracy, урон = ⌊|motion| * damageMultiplier⌋ (NBT 'damage', дефолт 2.0), попадание по AABB-expansion(0.3)+rayTrace, alive 1200 тиков. Спавн-коды S2C 0x3X: 90/91 = стрела/ракета (iiiliiiiii_40/41), 60/61 = гранаты (iiiliiiiii_39/41 family), 68 = iiliiiiiii_46. Референс rockstar PredictionModule: симуляция 150 тиков v=v*0.99-g, полёт по raycast, для жемчуга формула наклона + превентивный aim-ассист.

ЭКСПЛОЙТ-ПОВЕРХНОСТЬ (не использовать без явного «делай»): admin-каналы "rust:admin:give"(entry,count)/"rust:admin:spawn" клиент отправляет — сервер должен проверять права; NBT-десериализация ItemStack в S04; astraea-канал с NativeAPI (native-хендлер = валидатор/античит); своя шина подписывается на ВСЕ unknown-каналы.

RAW-ИМЕНА (вечерняя сессия 09-08, дизасм-проверено на сыром дампе — для КОДА АГЕНТА; CFR-имена выше ≠ raw, мост через this_class):
- Сеть: NetworkManager=`IllIlIIIiI` (pipeline splitter→decrypt→decompress→decoder→handler→encoder→compress→prepender→encrypt; канал=поле lilIIllII); vanilla NHPC=`lliIlIIIiI` (iface IlliilIIiI, 28 методов); MOD NHPC=`iliilIliiI` (iface illiilIIiI, 77 методов, карта игроков IiIllIIl). CPacketCustomPayload=`IlIIilIIiI`, SPacketCustomPayload=`IIlllIIIiI` (channel ≤64 симв + buf, лимит 32767). Тик-проверка в голове КАЖДОГО хендлера = `liIllIIIiI.iIiIiiiIlI` (кидает вне главного потока).
- Шина: MinecraftEventBus=`lIllIilliI` (post=ilIIIIlIIl(Object), subscribe=IIlIIIlIIl(Class,Function1)); payload-объект=`IlilliiliI(channel,ByteBuf)`; PacketExt=`liliiilliI` (sendPayloadPacket=IlIIIIIIIl, subscribePayload=ilIIIIIIIl). Dispatch `iliilIliiI.IlliillliI(CPacketCustomPayload)`: switch по hashCode канала — gun:recoil/gun:recoil:animation/MC|TrList/astraea-init/astraea вручную, ВСЁ ОСТАЛЬНОЕ → шина.
- 3 пути перехвата из агента (без нативных хуков): (1) подписка на их шину `lIllIilliI.IIlIIIlIIl(IlilliiliI.class, анонимный-класс)` — чистейший, все мод-пакеты без правки полей; (2) подмена netHandler = поле игрока `lIlilIIl` (Object) на наследника iliilIliiI → MITM 77 методов; (3) свой ChannelInboundHandlerAdapter в netty-pipeline до/после "decoder" (netty в classpath, 59 классов). НЕ ТРОГАТЬ astraea/astraea-init (NativeAPI.call("initCipher")/handlePacket — шифрованный мост к серверному ядру).
- S04 raw = `IiililIIiI` (entityId varint + slot enum + ItemStack): хендлер `iliilIliiI.IllIlIlliI` → `entity.iIiIliiilI(slot,stack)` на ЛЮБУЮ сущность. Enum `iilliilliI` = MainHand/OffHand/Armor1..Armor7. Чтение `IIiIIiIIiI.IiliIiiilI(slot)` (javap): MainHand→`inv.lIiIIIiilI()`, Armor→`inv.IliiIIiII.get(index)`, OffHand→ВСЕГДА EMPTY (уточнение к записи выше). Запись `iIiIliiilI`: MainHand→`inv.lllliIiII[currentItem]`+render-флаг, Armor→armorList.set(index). Инвентарь `iiIIIiIIiI`: main 30 / armor 7 / offhand 0, выбранный слот `iliiIIiII:I`; рендер иконок — RenderItem lllIliil ([[rustme-nametags-icons]]). GearESP = легальные иконки брони/руки чужих.
- БАЛЛИСТИКА: у GunProperty `ilIiiIIiII` (17 полей: damage/spread/zoomSpread, recoil-флоаты, Full/Semi, Normal/Smooth, список патронов) и AmmoData `lIIiiIIiII` (item+damage+spread+zoomSpread) констант скорости/драга пули НЕТ; пули = серверные сущности (large/small_ammo_entity, трассеры ammo_fire_trasser/ammo_air_trasser), физика в нативе (astraea/NativeAPI). Ваниль-снаряды клиент симулирует сам: EntityThrowable `liIilliIiI.IliiIIiIil()` = drag 0.98/0.98 + grav 0.04 (декомпил). ВЕРДИКТ предикта: движение бегущего игрока — ДА статически (rockstar rockstar-client-src/src/main/java/rockstar/client/AimBotModule.java:430-580: взвешенное среднее последних 6 позиций, 6 итераций fixed-point время полёта↔позиция, 8 точек прицеливания, скорости bow 3.0/crossbow 3.15/trident 2.5 — портится на наши геттеры ZNANIA 14.1); полёт пули — константы из снаряд-класса выше + эмпирика трассеров (клиент видит их каждый тик → 5-10 выстрелов дают speed/drag/gravity). soyuz ChinaHat = только визуал.
- ТИМ-ДАННЫЕ: TeamInfo{teammates:List<TeammateInfo>, leaderMarkers}; TeammateInfo{profile, joinTime, position, leader, state}; Team$UpdateTeammatePositions{teammatesInfo}; инвентаря в тим-каналах НЕТ. mate:inv = запрос просмотра инвентаря тиммейта (имя+UUID `iIlilIIliI`) → сервер открывает контейнер; ItemPickupNotificationPacketData{entry RustEntry, amount, total} — только про себя.
- ГАТЧИ декомпиляции (полный CFR-прогон умер): NTFS case-коллизии затирают output → каждый класс в ОТДЕЛЬНЫЙ каталог; WinError 206 (длина cmdline) при батче ≥400 файлов; CFR StackOverflowError → -Xss16m + retry по одному; javap-копию называть по this_class из CP (имя файла ≠ класс). Инструменты ночи 09-08: tools/decomp/m1..m26 (точечные декомпы), marker_scan2.py (kotlin-карта: 3810 kotlin/678 serializers/54 coroutines), decompile_all.py (частично), jv-таблицы в tools/decomp/jt*.
- Kotlin-маркер-скан 09-09 (byte-scan `kotlin/Metadata` в CP, marker_scan2.py → tools/decomp/marker_scan.json): 3810 классов с kotlin/Metadata (38% дампа), 678 kotlinx.serialization, 54 корутины; named-часть почти вся сеть (team 49, inventory 35, voicechat 33, donate 46+15, map 28...). Байт-скан надёжнее CP-строк (Metadata — атрибут, не строка).
- ДЕКОМПИЛЯЦИЯ 09-10 (пополнение): драйвер decompile_all.py (группы case-коллизий → отдельные outdir g0000..g0343, RETRY по одному при FAIL, -Xss16m) падал на WinError 206 при батчах 400 (лимит cmdline) и зависал на битых классах; для точечных работ — javap/CFR поштучно в изолированные каталоги (tools/decomp/m1..m36, r1..r9, jt*); fbo_constants.py — извлечение GL-констант из clinit через javap.

**РЕВЕРС РЕНДЕРА ИКОНКОК (09-08 поздно, причина пустых GearESP-иконок):** `RenderItem.iIIilIliII(stack,x,y)` из overlay НЕ работает: рендер идёт через их `RenderItemController IilllIiliI` (renderModel: ветка в UI-батчер при флаге `llIIiIiIiI.IiIliiIiI`, активен в их GUI-фазе; resolveModel с НЕГАТИВНЫМ кэшем в статик LinkedHashMap — null кэшируется навсегда). Тот же корень у IconRender друга (glTexCoord2f-путь). РЕШЕНИЕ — ItemIcons: сэмплировать ИХ блок-атлас (уже дешифрован и сшит игрой; PNG в rp.zip зашифрованы префиксом `3Yz8H8v7`) своим шейдером uniform-UV: mesher=`RenderItem.lllIIIiiI`, model=`mesher.iiIIliliII(stack)`, sprite=`model.iIilIIIIil()` (particle texture), UV-геттеры sprite: minU=lIIiilliII/maxU=liiIilliII/minV=lIliilliII/maxV=IiliilliII (поля lIIIlIiiI/lIiIlIiiI/iliIlIiiI/lllilIiiI), texId=`mesher.iliIliliII().iiilIlliil()`. Имя предмета = `stack.IIllIililI()` → Item → entry base `IillIiIiII.illlIIiIll()`. Спрайт-геттеры мода работают в его GUI-фазе (хотбар-контейнер lIliiIliiI: lighting disable→enable вокруг иконки, их zLevel=100).

Порядок для юзера: анализ/отчёт → ждать «делай». Правила: без лямбд (invokedynamic), инжект 1 раз, ZNANIA не трогать. [[rustme-esp]] [[rustme-jni-dll]] [[rustme-remote-player-info]] [[rustme-network-c2s]]
