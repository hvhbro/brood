---
name: rustme-next-features
description: Статус фич rustme (09-10 ночь-2, DLL 00:11 88 кл — ждёт теста): +Hotbar/AntiOverlay/NoSlowJagger поверх ItemEsp/KillEffect/AutoShoot/FOV-конус 360°/VisibleCheck/t-сдвиг/меню-элементы rock; git push 1f6db945; JumpCircle+JumpDistort подтверждены юзером; ПРАВИЛО биндов — без хардкод-клавиш, только tickBind
metadata:
  type: project
---

**09-11: Конфиг-система РЕАЛИЗОВАНА (DLL 12:01, 89 классов invoke-чист — ждёт теста):** вкладка CONFIG за нижним чипом-шестерёнкой рельсы меню; кнопки Save/Load/Delete/Reload/Open dir; файлы %LOCALAPPDATA%\RustMe\*.rm шифрованы (XOR-xorshift32 + magic RMCF + CRC32), load в 2 прохода; round-trip тест оффлайн PASS; формат/гатчи в [[rustme-config-system]].

**АКТУАЛЬНО 09-10 вечер-4 (DLL 23:41, 81 классов invoke-чист — ждёт теста юзером):**
- **KillEffect РЕАЛИЗОВАН** (Visuals): шейдерный эффект на месте убитого, 2 режима (Смерч/Портал) — порт kill_effect_tornado/portal юзера GLSL 330→120 (utils/render/KillShaders.java: texCoord из gl_FragCoord+rect с y СНИЗУ-ВВЕРХ, vertexColor→uniform, #version 120 ОБЯЗАТЕЛЕН в head(), fbm с литеральными границами циклов). ДЕТЕКТ: kill-feed сервера НЕТ (rust:dmg:death = ТОЛЬКО локальный death-screen; PlayerStatePacketData тоже локальный) → vanilla-звук entity.player.death на позиции трупа (пайплайн SoundEsp, имя содержит "death"). Атрибуция «Только мои» (def ON) = AimBot.recentTargetNear(x,y,z,8м,12с) — жертва была целью аима и её буфер рядом с точкой смерти. Рендер: билборд-квад по проекции базы и вершины (y+Размер), uniform rect в ФИЗ.пикселях, uTime/uProgress=age/Длительность/uColor/uIntensity; настройки Режим/Цвет/Дистанция(64)/Размер(3бл)/Длительность(3000)/Интенсивность(85%). РИСК теста: имя звука смерти в форке может отличаться — если не спавнится, смотреть имена звуков в SoundEsp-логе ([[rustme-esp]]).
- **AimBot FOV-конус до 360° + AutoShoot**: гейт переделан с евклидовой дельты yaw/pitch на ЧЕСТНЫЙ 3D-угол (acos dot(viewVec,toTarget), 0..180) — FOV-слайдер 10..360, 360=все цели вокруг; FOV-круг рисуется от КАМЕРЫ: r=tan(fov/2)/tan(camFovY/2)×screenH/2, camFovY из PROJ m11 (fallback 90°), при half≥90° или r за экраном не рисуется. Выбор цели вынесен в AimBot.selectTarget + ПУБЛИЧНЫЙ findTarget(ctx,maxDist,out) — работает при выключенном AimBot. **AutoShoot** (Combat): при угле цель↔прицел ≤ «Точность» (0.2..5°, деф 1) зажимает KeyBinding getKeyPrimaryAttack (KeyBindingsCategory, паттерн AutoSprint: pressed=true, отпуск один раз, игра бьёт по своему кулдауну) + Дистанция-слайдер; видимость гарантирует findTarget. ГАТЧ: приватный onTick(TickEvent) в модуле = cannot reduce visibility против public Module.onTick → приватные хендлеры называть иначе (shootTick) ([[rustme-aimbot-predict]]).

**АКТУАЛЬНО 09-10 вечер-3 (DLL 23:09, 75 классов invoke-чист):**
- **GIT: ЗАПУШЕНО** — 1f6db945 на github hvhbro/rml-broodcheat (история: initial → a7ec5a57 друга → наш; dump/ в репо, kimiko/rockstar-client-src/chtdump в gitignore; детали и гатчи rebase в [[rustme-no-git-push]]).
- **ItemEsp РЕАЛИЗОВАН**: текст на EntityItem на полу — World.IiIiiiiil (общий List<Entity>) → instanceof liIilliIiI → item.IllIIIlIlI() → stack.iIllIililI() (custom-name aware; фолбэк IlIlIililI) / count lIiiIililI(); плашка 0xA80C0C12 r2 + 7px текст «Имя xN Nм», prev-интерполяция (порог 4 бл), Esp.updateCamera+projectToScreen, MAX_LABELS 128, слайдер Дистанция 8..128 (деф 48); регистрация в RustClient + рендер в CheatHud после SoundEsp ([[rustme-esp]]).
- **AimBot Visible Check починен + тоггл** (addBool «Visible Check», def ON): прежний ray-trace НИКОГДА не работал — резолвили world.ilIlillllI(cls), но lIllIilIiI = **BlockPos, НЕ Vec3** (ilIlillllI = спуск «с неба до твёрдого блока») → NoSuchMethod → молчаливый fail-open. Рабочее: Vec3 = rustme/lliililIiI ctor(DDD); луч = World.IlIilllllI(Vec3,Vec3) = ванильный rayTraceBlocks (есть 3-арг liiilllllI / 5-арг ilIIIllllI со stopOnLiquid/ignoreBB/returnLast), null = видно; кэш ctor+Method; fail-open с разовым логом ([[rustme-aimbot-predict]]).
- **NPE-урок AimBot**: в фикс первой-мс забыли `buf[s] = new Snapshot()` → NPE «Cannot assign field x» в collect КАЖДЫЙ тик, буферы пусты, аим молча не работает (лог юзера 22:09).
- **ГАТЧ JDK-пути после rebase**: чужие НЕ-конфликтующие хунки принесли в pack_agent.py/jv.py битый путь `AppData\...\jdk-8.0.504.1` → «javac not found» ВОЗВРАТНО; фикс — списки кандидатов javac/javap с os.path.isfile-выбором; после merge/rebase с чужими tools проверять JDK-пути.

**АКТУАЛЬНО 09-10 вечер-2 (DLL 22:23, 74 классов invoke-чист — ждёт теста юзером; меню-элементы подтверждены логом юзера: тогглы булов/чипов режимов, назначение бинда MOUSE4 из меню):**
- **Меню-элементы rock 1:1**: Setting-иерархия в Module.java (Bool/Mode/Multi/Color/Section + generic visibleWhen-цепочки), чекбоксы/чипы/мультибокс/свотчи/секции в CheatMenuScreen; плавный скролл — колесо форка НЕ доходит до экрана (handleMouseInput игнорирует), читается rustme.IiiliilliI.lillilIIIl()=getEventDWheel в override lIIillIlil (IOException → try/catch), ease 1−exp(−dt/90), scissor с точным GuiScale ([[rustme-menu-elements]]).
- **JumpCircle настройки = kimiko 1:1**: сепараторы Основное/Искажение/Свечение/Подкраска/Цвет, булы Турбулентность/Свечение/Подкрашивать цветом/Второй цвет, режимы Анимация [Обычная,Эластичная,Назад] и Режим цвета [Радуга,Клиент,Свой] («Клиент»=фейд #906BFF↔#5A4BFF, «Свой»=2 ColorSetting-свотча), visibleWhen-цепочки.
- **AimBot**: фикс «первая мс от цели» (ring-init ВСЕ 8 слотов = текущая поз → vel=0; иначе vel=newest−(0,0,0)×100 бл/с уводил прицел) + диагностика промахов: SHOT-строка по edge ЛКМ (pred/real/vel/k/drop/ping/snapAge), aim-строка 100мс, target ACQ/SWITCH/LOST, no-target total/inFov/visible, snap reset/vel clamp, Locale.US, ник через reflection (GameProfile НЕ в cp) ([[rustme-aimbot-predict]]).
- **pack_agent: payload ТОПОСОРТИРОВАН** (class_header: однопроходный CP-разбор super+интерфейсов; topo_sort_classes: Kahn) — DefineClass резолвит заголовок eagerly, алфавитный порядок давал `define -> 0` на Module$BoolSetting и NoClassDefFoundError ([[rustme-jni-dll]]).
- **ItemIcons (Gear-иконки в nametags): фикс fbHeight** — uniform не заливался → GLSL 0.0 → uv.y<0 → прозрачные пиксели ([[rustme-esp]]).
- **AimBot/Strafe: 0/1-слайдеры → addBool** (Draw FOV, Predict Y, Auto Jump).

**АКТУАЛЬНО 09-10 вечер (DLL 18:29, 65 классов invoke-чист — ждёт теста юзером):**
- **JumpCircle+JumpDistort — ПОЛНЫЙ kimiko-визуал, юзер: «УРА ВСЕ РАБОТАЕТ»**: кольцо = аддитивный lightning-блендинг (SRC_ALPHA,ONE) 64 сегм, цвета анимированы (hue=angle+ms/8), easing 3 режима; волна jumpdistort.fsh 1:1 (без depth-окклюзии — удалена 18:29 по юзеру, глушить кольца целиком не должна); круги до 3-4 живых, спавн каждый прыжок ([[rustme-jumpcircle-input]]).
- **Strafe v3.1** — max(ref×«Ускорение», curMag): замедление A/D снято, спринт-прыжковый буст/кнокбэк/разгон СОХРАНЯЮТСЯ (пред. версия ела буст = «слишком медленный»); слайдер Ускорение 0.5..1.6 def 1.0 ([[rustme-field-map-round2]]).
- **AimBot — предикт 1:1 из дампа** (k=возраст СВЕЖЕГО снапшота+пинг — НЕ trackingSince, старое прилипало к 6.0 = прицел мимо целей; 3 итерации, Y-drop таблицы; [[rustme-aimbot-predict]]). Выбор цели = ближайшая к ЦЕНТРУ ПРИЦЕЛА (угловая, была с начала — «непонятно в кого» давал перелёт упреждения).
- **InstantUse — shuse-версия** (duration-форс отклонён сервером, откат по юзеру; [[rustme-instantuse]]).
История ночи 09-10 ниже.

**АКТУАЛЬНО 09-10 ночь (DLL 64 класса invoke-чист, сборка 02:18 — ждёт теста юзером):** Timer УДАЛЁН юзером (их Timer-код в VMP недостижим — Timer.java удалён, deregistered). **Strafe.java был УДАЛЁН из дерева (файла и регистрации не было), пересоздан 1:1 по РЕАЛЬНОМУ runtime-коду конкурента** (найден после фикса off-by-one сканера слотов; функция 0x5c3xx-0x5c6xx): гейт AirOnly&&onGround→exit; инпут strafe/forward, mag=sqrt(s²+f²), mag<0.04→exit; на земле speed=min(0.2806,mag) — ваниль, в воздухе mag×((boost−0.1)×0.015+1) (boost 0.77→×1.010, 100→×2.485); угол=yaw±90(strafe-знак)+180(forward<0) ×0.0174532925; motionX=−sin(rad)·speed, motionZ=cos(rad)·speed; AutoJump: onGround+horiz-collision → motionY=0.42; слайдеры Boost 0.1-100 (деф 0.77)/AirOnly (деф вкл)/AutoJump (деф выкл). Старая flyingSpeed-версия — НЕих, удалена. **SoundEsp добавлен** (payload-шина rust:sound:play + словарь конкурента 1:1 + real entryId из CP-скана jar; ТРИ бага найдены: неверный класс события rustme.IlilliiliI — юзер тестировал старую DLL; подписка = ИНСТАНС-метод illIIIlIIl у СИНГЛТОНА lIllIilliI.iiIiliIiiI; парсер int32 vs VARINT — переписан + диаг `payload channel:`/`sound: id=...`); **JumpCircle починен дважды** (1: glVertex без glBegin/glEnd — GL игнорирует вершины вне примитива; 2: diag-прорыв w1=0.12→sx=5985/sy=8321 — кламп W_CLAMP растягивает вершины у плоскости камеры → near-clip NEAR_W=0.2 + счётчики drawn/skipped; screenSpaceStart/End УБРАН — матрицы в overlay уже экранные ×2; дефолты Размер 4.5/Эластичность 1; Esp.lastW() добавлен; из 1-го лица кольцо видно при взгляде вниз — kimiko та же геометрия). InstantUse v2 МОЛЧИТ — снят полный lguse-контракт, РАБОЧИЙ план = lguse по uphint при longUse!=null — ждёт «делай» ([[rustme-instantuse]]). Разделы ниже про атрибутный Strafe (UUID 6f9b2a11), Timer-слайдеры Boost/Duration, BlockOverlay/ItemEsp/BulletTracers — ИСТОРИЯ старой версии (в GameContext остались мёртвые resolveMovementAttributes/modApplyMethod/modClass). Детали/гатчи 1:1-версий в [[rustme-chtdump-competitor]].

2026-09-07 ночь. Шрифт MSDF работает (корень всех багов — GL_BLEND=0xBE в стабе, исправлен на 0x0BE2). **Пинг: цепочка player.iliiililiI() → iiIilIliiI → illIillliI() → int привязалась (net chain bound в логе), но пинг=0 ms — билд 02:12 с illIillliI ещё НЕ инжектился (последний лог юзера был от 02:12-сборки). ЕСЛИ снова 0 после инжекта 02:29/02:49: (а) проверить с открытым Tab; (б) искать живого держателя lIllilIIiI; (в) NetHandlerPlayClient=iliilIliiI хранится в GameSettings.iIIiiilliI() → player.lIlilIIl.** FullBright: гамма 100 переполняла lightmap (голубой персонаж) → FULLBRIGHT_GAMMA=10.

**v88 (DLL 02:49): перекрытие стыков плашек ArrayList.** Юзер: «отступ всё равно остался». Механика понята точно: у SDF-квада пиксель на границе стыка оказывается СНАРУЖИ ОБОИХ квадов (RectSmoothness 0.25×scale=0.5 физ.пикс — граничный фрагмент с dist>0 даёт alpha≈0) → сквозь шов светит мир. ФИКС: фон и полоска рисуются высотой rectH+0.5 scaled (перекрытие стыка на 1 физ.пиксель, шаг currentY += rectH не тронут). ВИДИМАЯ высота плашки прежняя.

**v87 (DLL 02:47): пиксельный шов между плашками ArrayList.** После RectSmoothness-фикса юзер: «расстояние между функциями всё равно осталось». Причина: высота плашки дробная (8+1.2×2=10.4 scaled = 20.8 физ.пикс) — стык внутри пикселя → полупрозрачный шов. ФИКС: PADDING_V 1.2→1.0 → высота ровно 10.0 scaled, стык на границе пикселей. УРОК: высоты/ширины стыкующихся SDF-плашек держать ЦЕЛЫМИ в scaled-пикселях.

**v86 (DLL 02:40): «тень» на стыках + отступ слева.** (1) Тень = мягкий SDF-фейд 2px: в ROUND-шейдер добавлен uniform RectSmoothness (1:1 с их adaptive_ui/ui_batch), drawRoundedRectShader — перегрузка со smoothness (ArrayList 0.25×scale = жёсткий край; watermark 1.0), +1px запас-костыль УБРАН (геометрия 1:1 оригиналу); (2) LEFT_X=6 («отступ от левого края слишком маленький»).

**v84-85 (DLL 02:29-02:40): ArrayList слева + vanquish-фиксы.** (1) «Watermark пропал» → при переписывании CheatHud потерян вызов Watermark.render() — ВОССТАНОВЛЕН (урок: переписывая файл, проверять вызовы из него); (2) ArrayList СЛЕВА: LEFT_X, полоска на левом крае, текст после полоски (их ветка isRightSide=false); (3) FONT_SIZE 8; (4) вода/цвета: PRIMARY=0xFF906BFF/SECONDARY=0xFF5A4BFF константы CheatHud.

**v80: ArrayList 1:1 под VANQUISH (юзер дал исходник ArrayListWidget):** отдельная плашка rgba(12,12,18,240) на модуль (radius 0), сортировка по ширине убывание, полоска 1.5px вертикальный градиент, ГРАДИЕНТНЫЙ ТЕКСТ (CustomFont.drawGradientString — цвет глифа lerp по позиции, без тени), medium, БЕЗ анимаций. Ватермарка: medium 7px, бары белые (незажжён @20% белым — DarkColor убран, чёрный бар был при ping=-1).

**Ватермарка стабильно:** ряд (часы HH:mm белым + капсула r=7 с градиентом 69% fade вправо + бары пинга) центрирован целиком. Путь пинга: `player.iliiililiI()→iiIilIliiI→illIillliI()I`. Резервная цепочка (v72-74): iliilIliiI (NetHandlerPlayClient форка, Map<UUID,iiIilIliiI>, поле player'а lIlilIIl, геттер профиля lllIillliI) — работает, но снапшот заморожен без Tab; знания сохранить. resolvePing = только прямые имена (сканы убраны по требованию юзера). Техника реверса: javap на копии дампа-файла под чистым именем (parse_cp врёт на крупных классах); молчаливый catch = логировать всё; NoSuchMethod в логе = точное имя виновника. Детали в [[rustme-custom-render]].

**ДВА ШРИФТА (v64):** MsdfFont переписан из синглтона в ЭКЗЕМПЛЯРЫ: `MsdfFont.get(fontBase)` + CACHE; CustomFont: `HUD_FONT=sf_semibold` и `WM_FONT=medium` (ватермарка; теперь и ArrayList на medium после v80). У medium режим properAdvance: перо = advance*size + 0.025em трекинг + left bearing (инк 0.78em > advance 0.5em → expensive-трюк width*(size-1) раздвигал буквы; leftEm добавлен в глиф-массив, индекс 8). sf_* остался на expensive-трюке. Детали в [[rustme-custom-render]].

**Загрузка шрифтов: ВШИТЫ В DLL (07.09 17:25, отложенный план РЕАЛИЗОВАН иначе).** pack_agent.py generate_font_data() регенерирует utils/render/FontData.java (4 атласа Base64-чанками по 60000) перед компиляцией — класс едет в payload; MsdfFont.loadAtlas: ДИСК = dev-оверрайд, иначе FontData.get(); лог «atlas source: disk/embedded payload»; byte-MATCH проверен чистым JDK; DLL 2.1МБ (29 классов), java/lang/invoke нет. sf_medium не зашит. Обновление шрифта: файлы в fonts/msdf → build_dll.bat. Детали в [[rustme-esp]]. Лого-иконка отменена юзером; их лого = SVG `rockstar-client-src/.../icons/logo.svg` (если вернут — конверт в PNG, грузить как атлас, fixed-pipeline квад).

**2. ESP — РЕАЛИЗОВАН И РАБОТАЕТ (07.09, v3 DLL 17:04):** toggle G, боксы+подписи (имя+дистанция) по игрокам из world.playerEntities. Проекция: view свой (глаза игрока+eye-height) + PROJ из lliIilliiI с перспктивной сигнатурой; псевдо-клип вместо скипа при зуме; perf-уроки (reflection-кэш, один begin/end). Детали/грабли в [[rustme-esp]]; ночная карта имён — ZNANIA 14 + [[rustme-night-map]]. Ночной анализ 9977 классов ВЫПОЛНЕН (ZNANIA раздел 14).

**Ночной таск ВЫБРАН юзером (2026-09-07 ~02:50): автономный АНАЛИЗ классов, БЕЗ изменения кода, в НОВОМ чате** (этот чат забит контекстом). Готовый промпт вшит в ZNANIA.md раздел 8 («НОЧНАЯ ЗАДАЧА» после ЗАДАЧА): прогнать все 9977 классов дампа (карта disk_name_map.json, дизасм через javap на копии — parse_cp врёт), классифицировать: (1) классы для ESP: pos X/Y/Z (double-поля в IIlIIliIiI/liIlIliIiI, верифицировать javap), yaw/pitch, FOV из iilliIliiI, EntityRenderer-аналог с матрицами, bounding box; (2) держатель ЖИВЫХ lIllilIIiI (пинг — снапшот даёт 0); (3) HUD/рендер: их watermark/бары/иконки/шейдеры, сверка с assets/minecraft/shaders/; (4) сеть/настройки/события/модули — полная карта. Итог: обновить ZNANIA.md новым разделом, каждое имя верифицировать javap/дизасмом, помечать «проверено»/«гипотеза». КОД НЕ МЕНЯТЬ.

**3. Меню (ClickGUI) — самое объёмное, ~400–600 строк, делать при 3–4 модулях:** подмена/создание GuiScreen (база `rustme.IlIlliliiI` — abstract, drawScreen=`IiIIIlIlil(IIF)V`; НАЙТИ: метод открытия экрана в урезанном Minecraft `lIliIiiIiI` и mouse-хендлеры), мышь/клава через org.lwjgl.glfw.GLFW (glfwGetMouseButton), панели с драгом, привязка к реестру Modules. Сейчас модулей 2 (AutoSprint X, FullBright J) + ватермарка + ArrayList.

**Расшифрованные ассеты мода: `C:\Users\Admin\Desktop\rustme\assets`** (текстуры вырезал юзером): шейдеры ui_batch/item_icon/sprite_renderer/scene/terrain/zone_glow/armor/blur_*, MSDF-шрифты мода `assets/minecraft/font/msdf/` (medium — СКОПИРОВАН к нам, default, map_ru, mlrs, regular, map_en).

**ГАТЧ СБОРКИ:** DLL не пересобирается пока игра запущена (LNK1168 держит dll) — проверять/убивать Get-Process rustme/RustMe перед сборкой. Папка jni/agent/build может блокироваться shell-сессией с cwd внутри — переименовать в build_locked и собрать заново. rustme.exe ЖЁСТКО убивается ТОЛЬКО юзером (Stop-Process/taskkill дают «Отказано в доступе») — просить юзера закрыть.

**ПРЕФ: ЧИСТЫЕ ЛОГИ (08.09, юзер попросил):** удалены все Log.info ESP (diag/PROJ captured/toggled/net chain), FullBright (gamma/toggled/resolved), Watermark (ping 5s/net chain/drawn), HUD (FIRST CALL/scaledW/swap/vanquish); heartbeat 10с→60с. Держать в логе только одноразовые привязки (DepthSwap/PostRig/шейдеры) и ВСЕ ошибки.

**ДОРОЖНАЯ КАРТА 09-08 (юзер спросил «что можно добавить» — только анализ, кода нет, [[rustme-phase-gating]]):**
- **NoSlow прицела — РЕВЕРС ВЫПОЛНЕН 09-08, чистого обхода НЕТ ([[rustme-ads-slowdown]]):** механика = onLivingUpdate ×0.2 на forward/strafe холдера IIIililIiI при using-item (liIliiil:Z) и не-верхом; ГЛАВНАЯ ГРАБЛЬ: холдер-подкласс lIIililIiI перечитывает клавиатуру КАЖДЫЙ ТИК (IiiIllillI) прямо перед умножением — записи агента умирают; travel замедления прицела не содержит. Единственный вариант — компенсация атрибутом movementSpeed ×5 при прицеле (attrSetBase готов) с риском античита; нужен живой тест серверной валидации. Юзер уточнил (09-08): NoSlow = прицельное замедление ОРУЖИЯ (инвентарный вариант снят); флаг прицела = dataWatcher бит 7 (`iIIlliilII()Z` liIlIliIiI) — сервер-синхронизируемый, читается и у чужих.
- **NoRecoil — РЕАЛИЗОВАН И РАБОТАЕТ 09-08 (клавиша N), [[rustme-norecoil]]:** отдача = S2C gun:recoil (float yaw, float pitch, byte mode) → модуль жжёт накопители ilillIiliI.IliiiiIIl/iliiiiIIl каждый 1мс + офсеты фасада lIillIiliI ТОЛЬКО при выключенном free-look (иначе там живая камера). Тест юзера: лог «burned smooth recoil (hits=...)» + «burned facade offsets» — сервер шлёт ОБА mode (mode==1 и free-look-ветку mode==0); mode==0-прямой не подтверждён.
- **Скины SVO — РЕАЛИЗОВАНЫ И РАБОТАЮТ 09-08 (клавиша H), [[rustme-svo-skins]]:** все скины → putin.png (вшит байтами, pack_agent generate_asset_data → AssetData.java); SvoTexture implements iiIiliiIiI (TextureObject), регистрация в TextureManager gs.IIiIiilliI().lliIilliII (сам зовёт loadTexture — GL на главном потоке), перезапись SKIN-ключа в картах снапшотов (identity-map для открата), подмена/откат через main-thread очередь GameContext.runOnMainThread (выгребается в CheatHud.renderFrame). ГАТЧ: newInstance() на интерфейсе iiIiliiIiI = InstantiationException → только new rustme.SvoTexture().
- **Particles из rockstar — анализ 09-08, переносимы, не начато:** KillEffectsModule = СОБСТВЕННАЯ система частиц rockstar (НЕ ванильный ParticleManager): физика в клиенте (gravity/scatter, отскок от блоков через getBlockState, жизнь 1.5–3с), billboard-квады + bloom.png additive (SRC_ALPHA, ONE); их ParticleManagerMixin — лишь УДАЛЕНИЕ ванильных частиц (RemovalsModule). Рендер у нас готов (проекция ESP), физика копируется 1:1, картинки байтами = паттерн FontData (Base64-чанки → ImageIO.read → glTexImage2D RGBA, +33% размера, DLL самодостаточна). НЕ РЕШЁН триггер: EntityDeathEvent их шины, HP чужих не синхронится — варианты: смерть/урон своего, факт выстрела (gun:* каналы), ambient-декор.
- **NameTags кандидаты:** sneak/sprint-индикаторы (dataWatcher-флаги, геттеры готовы), «смотрит на тебя» (yaw/pitch цели → вектор взгляда → конус на свою голову ⚠), скорость цели + «в воздухе», тиммейт-метки (TeamInfo Online/Dead/Wounded → клиентский кэш llIllIiliI), динамический цвет пинга (пороги как в ватермарке), видимость «за стеной» (ГОТОВАЯ обёртка lliIilliiI.IIiliiiiII(World,Entity,F) = rayTrace — звать джавами), SDF-плашка-фон под текстом. Мелкий реверс: gamemode из снапшота, лут-история по дроп-пакету iIlllIIIiI (entityId+ItemStack). НЕВОЗМОЖНО: HP/броня/хотбар чужих (сервер не шлёт, проверено).
- **Из rockstar-client-src портируемы:** Saturation/color-grading (пост-конвейер WET готов — ещё один проход почти даром), ~~замена неба~~ **РЕАЛИЗОВАНО = SkyNebula (toggle M, 09-08, см. [[rustme-wet-world-shader]])**, entity glow через СОБСТВЕННЫЙ zone_glow мода (iiIIIIiliI, uMask/uColor/drawSilhouette) — усиление ESP, backdrop blur под плашки (их blur_down/up/composite-цепочка в форке), dynamic light (рискованно: lightmap = int[256], пересчитывается каждый тик; частично закрыто FullBright). WetWorld (SSR, toggle N) тоже реализован — 3 итерации фиксов, главная: камера-относительный ViewProj; ждёт теста. Предложено отдельной фазой прогнать rockstar-src по списку модулей (там свой менеджер модулей — я видел только AmbienceModule).
- **Новые фичи у нас:** Zoom (FOV-node через Settings.getData→…→setValue — тот же путь, что гамма FullBright; FOV-хелдер liIIiillI:F), Keystrokes HUD (input holder liIIIiIIiI: 2 float+5 bool уже резолвится в GameContext), Radar (точки из world.playerEntities), Tracers (линии к боксам ESP), Coords/FPS HUD, AutoRespawn (конвейер ГОТОВ: rust:dmg:respawn RespawnPacketData, вся отправка payload из агента — [[rustme-network-c2s]]), AutoEat (rust:inv:itmact, ключ Eat), Chest/Storage ESP (195 tileentity-классов в дампе, нужен реверс списка тайлов World — средний объём). РЕКОМЕНДОВАННЫЙ ПОРЯДОК: NoSlow-реверс → NameTags-апгрейд → Zoom+Keystrokes → Saturation/небо; ПРЕДЛОЖЕНО юзеру 09-08 после C2S-анализа (ждёт «делай»): AutoRespawn → AutoEat → NoSlow-инвентарь (гипотеза: slowdown при открытом инвентаре локальный — компенсировать локально, rust:inv:toggle не трогать). Tracers РЕАЛИЗОВАН (09-09 merge, RCTRL/меню).
- **Fortnite Damage (вопрос юзера 09-08, «не пиши код» — ждёт «делай»):** эффект цифр урона над головой жертвы из gamesense-Lua (юзер дал скрипт): по player_hurt копить {сумма, время, позиция головы, headshot} → каждый кадр world_to_screen → цифры-спрайты с анимацией (quart_in подъём + elastic_out отскок каждой цифры, fade по alpha, таймаут 1–10с, масштаб 25–200%), фильтры свой/тиммейты/враги. ЧАСТИЧНО ОГРАНИЧЕНО: свой урон = полный эффект (метаболизм rust:met/update, разница тиков = цифра); ЧУЖОЙ урон — сумма НЕ синхронизируется ванилью (HP чужих нет, hurt = EntityStatus-пакет без числа) → нужен реверс rust:dmg:* каналов (llliilIliI: dmg:death/dmg:close/dmg:respawn — есть ли величина) или fallback «хит-маркер без цифры»; факт урона чужого ловится через netty PacketHook (backup_packetfly, read-only). СПРАЙТЫ-ЦИФРЫ юзер хочет ИМЕННО из Lua (10 PNG 59×105 base64): декодировать в jni/agent/src/assets/ → pack_agent вшьёт в AssetData (паттерн putin.png), загрузка как MsdfFont.ensureLoaded/SvoTexture.upload, рендер — GL20-квад с uniform uvRect+цветом (схема ICON/MSDF; fixed-pipeline glTexCoord2f в HUD ненадёжен — грабли IconRender). Проекция готова (Esp.projectToScreen), анимации = чистая математика.

**СТАТУС 09-08 поздно (после merge друга):** меню RSHIFT работает (CheatMenuScreen+GlassGrab — [[rustme-menu-tracers-merge]]). **ДЕФОЛТНЫЕ БИНДЫ УДАЛЕНЫ** (юзер: «теперь появилось меню»): X/G/J/RCTRL/N/H выпилены у всех модулей, тогглы ТОЛЬКО из меню через Module.tickBind(bindKey); в Module добавлен ctor (name, category); RSHIFT 344 = только само меню. **❗ДВОЙНОЙ БИНД — КОРЕНЬ жалобы юзера «бинд работает только при открытом меню» (лог 09-09 вечер):** юзерский AimBot был по СТАРОМУ шаблону (`super("AimBot","Combat",82)` + свой handleKey с edge-детектом R) → ДВА независимых edge-детектора на одну клавишу: в меню tickBind спит (гейт menuOpen) и работает хардкод — «нормально»; вне меню оба срабатывают на одно нажатие → бесконечный ON/OFF (в логе пары `toggled by R -> ON` / `bind -> OFF`). ФИКС (DLL 21:18): хардкод вырезан из AimBot. **ПРАВИЛО ДЛЯ НОВЫХ МОДУЛЕЙ: `super("Имя","Категория")` БЕЗ клавиши, БЕЗ handleKey/isKeyDown-тоггла — переключение только через Module.tickBind; hold-семантика (зажать R для аима) = своя isKeyDown внутри onTick, НЕ через toggle().** Сопутствующие фиксы: (1) `MenuModule.onScreenClosed()` — вызывается из CheatMenuScreen.onGuiClosed (iilillIlil) при ЛЮБОМ закрытии экрана (ESC/RSHIFT/замена GUI): menuOpen=false + suppressUntilRelease + Modules.set("Menu", false) — раньше Menu state залипал ON, тогглы рассинхронизировались; (2) Module.tickBind имеет diag раз в 3с (`bind diag: key= menuOpen= down=`) — убрать после стабилизации. **ФИКС ПОДТВЕРЖДЁН логом юзера 09-10 (~15:00): одиночные `bind -> ON/OFF` без пар с `toggled by R`; ammo-классификация живая (`item.sulphur MP5A4 -> 1`, `Полуавтоматическая винтовка -> 2`); баллистика считает (k/dist/flightSec/lead строки).** **GearESP:** v5 (RenderItem.iIIilIliII) провален — их RenderItemController из overlay не рисует (UI-батчер + негативный кэш моделей; юзер подтвердил: «и в меню иконки не рисует»); v5.1 ItemIcons = сэмплинг ИХ блок-атласа своим uniform-UV шейдером (идея юзера «вызывать иконки из загруженного ресурспака»; PNG в rp.zip зашифрованы префиксом `3Yz8H8v7` — растеризовать с диска нельзя) — DLL 23:46 ждёт теста; полная цепочка mesher→model→sprite→UV и гатчи в [[rustme-esp]]. **KeybindsWidget — ПЕРЕПИСАН 1:1 по sweetie KeybindsWidget (DLL 15:53):** двухплашечный хедер — иконка-плашка (x-5, y-5, 23×23, глиф "L" NE1Z → IconRender "setting" tint primary, 14.6f) + "KeyBinds"-плашка (x+w1+10+3−4, y−5, w2+8, h1+10; текст centered на x+4+w1+10+3+20); headerWidth=(w1+10)+3+(w2+8); список: maxWidth=headerWidth−9.3, bind слева x+2 / имя справа x+maxWidth−width, обрезка maxWidth−25+"...", анимации 0.12/0.15/6×(1−alpha); драг hudDrag=3 готов. ГАТЧ: per-corner радиусы (Vector4f) наш SDF не поддерживает — единый 5f. **AimBot — ПОЛНОСТЬЮ ПЕРЕПИСАН 09-09 вечер ( Combat, toggle R, полный конвейер конкурента из chtdump):** ring-buffer 8 снимков/игрока (шаг ≥0.2м), ammo-классификация по displayName (НЕ unlocalizedName — он generic "item.sulphur" для всего!) с русскими+английскими названиями моделей (MP5A4→1, Винтовка→2, LR300→2), баллистика flightTimeCoeff(dist, ammoType) × elapsedSec × pingComp (clamp 0..6), FOV-гейт, Visible rayTrace fail-open, поворот в root-поля IlIlIiliI/IilIIiliI (INPUT), FOV-круг GL_LINE_LOOP из CheatHud. Слайдеры меню: FOV/PingComp/Prediction/Draw FOV. Резолвы ОК (inventory через WRAPPER, rotation через ROOT — walk-up). Молчаливый отказ = ammo-классификация (diag "ammo: ws=..." добавлен). Детали/гатчи в [[rustme-chtdump-competitor]].

**InstantUse — РЕАЛИЗОВАН 09-09 (Combat, modules/impl/InstantUse.java):** детект vanilla usingItem (noSlowAimMethod), отправка rust:misc:lguse (PerformLongUseActionPacketData — пустой пакет). Путь: их helper liliiilliI.IlIIIIIIIl(player, channel, data) — static util, но первый параметр liIililiii_1 (НЕ наш SP!) → резолвится их player-getter lllillilii_2.iIiliiIIIl() (их Minecraft wrapper, static) с fallback на наш ctx.player. Edge-detect + resend 500мс. Зарегистрирован в RustClient.

**Dormant ESP — РЕАЛИЗОВАН 09-09 (встроен в Esp.java):** dormPos HashMap<entityId,{x,y,z,time}> обновляется в основном цикле, dormRender() рисует жёлтый бокс (alpha 20-40% гаснет за 4.48с) + "DORMANT X.Xs" подпись для игроков, пропавших из world.playerEntities <4.48с. Рендер: 12 рёбер GL_LINES через projectToScreen.

**JumpCircle — РЕАЛИЗОВАН 09-09 (Visuals, modules/impl/JumpCircle.java, порт kimiko):** расширяющийся круг под ногами при прыжке (юзеру понравился визуал kimiko). Детект: onGround→air + motionY>0.02/curY>prevY+0.02. Рендер: 3D-кольцо 48 сегментов через projectToScreen, двусторонний quad. Цвета: радуга (HSV) или градиент cyan 0x7FF2FF→pink 0xFF3296 (kimiko). Настройки: Время/Размер/Эластичность/Режим цвета. Зарегистрирован в RustClient после InstantUse; рендер из CheatHud после AimBot.render. Гатчи: onGround/motionY — поля root через reflection/projectToScreen double-возврат; tools/srcs.txt перегенерировать после создания нового файла (иначе javac молча не видит класс). **БАГ №1 (лог юзера «прыгаю но ничего не происходит»): модуль стартовал OFF — в конструкторе НЕ было setState(true), а меню-бинда у JumpCircle нет → tickBind его не включал → onTick мёртв (в логе только registered). ПРАВИЛО: модуль БЕЗ меню-бинда обязан стартовать setState(true), иначе мёртв. ФИКС: setState(true) в ctor + диаг (tick-лог 5с «tick: inWorld= circles= onGround=» + «circle spawned #N at (x,y,z)» при прыжке). DLL 58 классов invoke-чист — ждёт теста: если tick-лог появился но circle spawned нет при прыжке — детект onGround/motionY читается неверно.**

**AimBot КАМЕРА исправлена (09-09, юзер: «камеру уводит вниз при наведении»):** причина — абсолютный yaw/pitch в поля игрока каждый 1мс-тик (мышь боролась с аимом). Фикс: ПЛАВНАЯ доводка (MAX_TURN=25°/тик к цели), clamp pitch [-90..90], float-cast wrapDeg. Классификация патрона подтверждена живым логом: MP5A4→1, Винтовка→2 (по displayName); predict k=0 на первом кадре — норм (elapsed=0), delta копится в ring-buffer со второго кадра. DLL 55 классов (с InstantUse+AimBot+Dormant), invoke чист. Ожидается тест наводки по живой цели.

**Кандидаты на НОЧНУЮ автономную смену (предложены 09-09, юзер: «просто вопрос ничего не делай» — ждут выбора):**
1. **Реверс rust:dmg:\* + полный каталог S2C** (главный, разблокирует Fortnite Damage): у нас список ~96 каналов rust:\*, но не сняты классы-данные и поля подписчиков subscribePayload → таблица «S2C-канал → класс → поля → хендлер»; отдельно dmg:death/close/respawn (llliilIliI) — шлёт ли сервер ЦИФРУ урона по чужим.
2. **Update-resilience**: find_validator.py по константам 40039/28200/59236 (погонять на обоих дампах), прогон calibrate.py на старом дампе (воспроизвести 4 известных RVA), расширенные байт-отпечатки ~14 критичных слотов env-таблицы.
3. **Каталог фич референс-клиентов** (soyuz-master + rockstar-client-src): ChinaHat/Aimbot/AspectRatio/RadialMenu… → таблица «фича → реализация у них → портируемо ли (чисто Java/GL без нативных хуков) → риск».
НЕ для ночи: PacketFly (нужен живой тест, сервер флагает), rust:admin:*/voicechat (бан-риск), самозапуск игры/инжекты (1 инжект на запуск, нужен юзер). Формат результата: отчёт в чат утром + memory-файлы; ZNANIA только по явному разрешению.

**Итог ночи 09-09/10:** п.1 (dmg-реверс + S2C-каталог) ВЫПОЛНЕН автономно — 92 S2C канала в s2c_full_catalog.json; dmg-цифр урона по чужим в сети НЕТ (см. [[rustme-night-0910-analysis]]).

**ОТЛОЖЕНО юзером 09-09 утром** (предложил следующие ночные задачи, юзер: «пока что не надо» — вернуться в следующее автономное окно):
1. Починить полную CFR-декомпиляцию 9977 классов (NTFS case-коллизии → per-class каталоги, CFR -Xss16m, батчи ≤200 файлов против WinError 206 cmdline).
2. Статы брони/защиты (экран iililliliI: armorSlots + stat.projectile/melee/explosive/cold/radiation/bite) → честный «детект голых» для Tracers (атрибут generic.armor=0 у всех — броня серверная кастомная) + броня в GearESP-подписи.
3. rust:mate:inv полный протокол (запрос имя+UUID `iIlilIIliI` → сервер открывает контейнер тиммейта) — путь к хотбару чужого.
4. Фолбэк иконок GearESP по имени: TextureMap `iiiIliiIiI` getSprite = `ilIlilliII(String)`/`IlIlilliII(String)` — для предметов без иконки мода.
5. find_validator.py + semantic-матчер имён ([[rustme-update-resilience]]).

**ОТЛОЖЕНО 09-10 утром (юзер: «пока что не надо» — ночной план на следующее автономное окно, код не трогать):**
1. Полная CFR-декомпиляция 9977 классов: NTFS case-коллизии (имена differing только регистром перезаписывают друг друга) → изолированные каталоги/пер-класс прогоны; CFR нужен -Xss16m (StackOverflow на глубоких цепочках); батчи ≤300 файлов против WinError 206 (cmdline too long); bat-драйвер tools/decomp/decompile_all.py + normalize_classes.py готовы.
2. Статы брони/защиты (экран iililliliI: armorSlots + stat.projectile/melee/explosive/cold/radiation/bite) → честный «детект голых» для Tracers + броня в GearESP-подписи.
3. rust:mate:inv полный протокол (запрос имя+UUID `iIlilIIliI` → сервер открывает контейнер тиммейта) — путь к хотбару чужого.
4. Фолбэк иконок GearESP по имени: TextureMap `iiiIliiIiI` getSprite = `ilIlilliII(String)`/`IlIlilliII(String)` — для предметов без иконки мода.
5. find_validator.py по константам 40039/28200/59236 + semantic-матчер имён ([[rustme-update-resilience]]).

**NoSlow прицела — v3 (DLL 09:28, ждёт диаг-теста юзера):** v1-правка холдера ПРОВАЛЕНА тестом 07:41 (супер-вызов на офсете 1404 стоит ПОСЛЕ ×0.2 — тик атомарен, правка холдера между тиками умирает; старая запись «потребление до умножения — работает» была ошибкой); v2-атрибут ×5 ПРОВАЛЕН тестом 07:59 (vanilla-флаг liilIiilII не взводится ганами — в логе нет compensating; + attrGetValue/attrSetBase не были инициализированы); v3 = флаг бит 7 `iIIlliilII()` + vanilla usingItem + диаг 1с — дневник тестов, цепочка и трактовка диаг в [[rustme-ads-slowdown]].

**Strafe — тест 09-09 «ничего не делает» + слайдер мёртв (фиксы в DLL 09:28):** (1) ГАТЧ GameContext: attrGetValue/attrSetBase были ОБЪЯВЛЕНЫ, но НИКОГДА не присваивались (в init() только комментарий без кода) — резолв добавлен сразу после getAttrInstanceMethod; это чинит и NoSlow-restore, и Strafe-диаг. (2) Слайдер: координатный баг CheatMenuScreen (клик-хендлер локальные vs трек экранные — [[rustme-menu-tracers-merge]]). (3) В Strafe добавлен диаг 1с (applied/mult/base/eff) — по следующему тесту видно, применяется ли модификатор (механика атрибута проверена по байткоду: op1 → base×(1+amount), apply/remove ставят dirty). Сам модуль-механика (UUID 6f9b2a11, remove→apply) не менялась.

**Strafe — РЕАЛИЗОВАН 1:1 09-10 02:18 (DLL 64 класса; файл был удалён из дерева — пересоздан + зарегистрирован в RustClient между JumpCircle и SoundEsp):** перезапись motionX/Z каждый 1мс-тик по yaw+инпуту (детали алгоритма и константы в [[rustme-chtdump-competitor]]). Инпут из ctx.moveStrafeField/moveForwardField (Input iIIiIIiII/IiIiIIiII, резолвятся в GameContext.init), movementInput живёт в ctx.movementInput (обновляется в update()). Поля motion через walk-up от SP. ВАЖНО: ctx.motionXField/motionYField/motionZField уже в GameContext (iIIlIiliI/IiiIIiliI/IIiIIiliI) — Strafe резолвит свои дубликаты walk-up'ом.

**УТРОМ 09-09 (после Strafe, юзер «создай функции») — РЕАЛИЗОВАНЫ 4 модуля (DLL 08:34, 57 классов, ждут теста; полные цепочки в [[rustme-night-s2c-catalog]]):**
- **BlockOverlay** (Visuals): MOP = Minecraft.illiiiIl (public Object; класс lIiililIiI: blockPos=iIIlIlillI(), hitVec, entity, type) → world.lIlIIiliI(pos) → blockState.IIliIIiliI(world,pos) → AABB (поля ZNANIA 14.1) → 8 углов Esp.projectToScreen → 12 рёбер GL_LINES (depth off, 906BFF @0.9). ГАТЧ: World.lIlIIiliI перегружен — (Ray) и (BlockPos)→IBlockState.
- **ItemEsp** (Visuals): World.iillliliii_14.IiIiiiiil (главный List<Entity>, public) → instanceof liIilliIiI (EntityItem; стак IllIIIlIlI(); age-лимит 5999) → lastTick-интерполяция → Esp.projectToScreen → ItemIcons.drawItemIcon (v3 иконки мода). Даль 64, размер 2..8px. НЕ ПУТАТЬ: IIIiiiiil = List<lliiiiliii_15> — 1.8-слой NBT-сущностей, НЕ дропы.
- **BulletTracers** (Visuals): фильтр projBaseClass = **lIIIIiIIiI** (снаряд-база, super Entity root; дети IiilIiIIiI=стрела, iIilIiIIiI), исключаем liIilliIiI; entityId = Entity.**iiIIIIlIII()** (ключ World-мапы liiliiliii_19 — найдено дизасмом spawn-метода lilIIiiiil; первые догадки iIiIiilII/iIililIiIl были неверны); история ≤24 точек (dedup <0.1, телепорт >8 не тянуть), GL_LINES fade 0.15..0.8 к голове.
- **Timer** (Movement): Minecraft.IIIiiiIl (public final Object) = Timer ililliliii_8, создаётся `new ililliliii_8(20.0f)` (20 TPS); поля IIiIIiillI (float аккум) / iiIIiillI (int тики); runGameLoop: for(i < min(10, iiIIiillI)) runTick. ПАТЧ = после liiiliIllI дописать (mult−1)×iiIIiillI (гонки нет: игра читает 1×/кадр, перезаписывает в след. кадре), frac через копилку как сам Timer, лимит 10/кадр соблюдён. Слайдеры: Boost 1.0–3.0 шаг 0.05 + Duration ms 0–5000 (авто-выключение через onTick, 0=∞; enabledAt сбрасывается в onEnable). РИСК: тики двигают всё — floating/moved-too-quickly.
- Интеграции: CheatHud.renderFrame зовёт BlockOverlay/ItemEsp/BulletTracers.render после Esp.render (камера готова); все зарегистрированы в RustClient.
- ГАТЧ javac: «cannot find symbol class Field» = забыли import java.lang.reflect.Field при копировании класса.
- ГАТЧ full_src: имена-коллизии (liiililiiI.java ≠ SP-класс; llliiiliii_2 = IBlockState-интерфейс, не RayTraceResult) — класс верифицировать по комментарию «Renamed from rustme.X» в шапке файла.

**ФИКСЫ «НЕ РАБОТАЮТ» 09-09 08:54 (DLL 57 классов, ждёт повторного теста; детали в [[rustme-night-s2c-catalog]]):**
- **Timer**: главная ошибка — дописка в INT iiIIiillI, который liiiliIllI() следующего кадра ПЕРЕЗАПИСЫВАЕТ из float-аккумулятора IiIIiillI (стёрка до чтения → бинд V «ON» без эффекта). ФИКС: писать в FLOAT-аккумулятор IiIIiillI: acc += (mult−1)×delta, где delta = liIIiillI (дельта кадра в тиках); one-shot per frame через lastAcc-снапшот (1мс-цикл иначе добавлял бы тысячи раз), предохранитель extra≤3. ГАТЧ-ПАТТЕРН: «int-поле, пересчитываемое из аккум-лятора каждый кадр» = всегда писать в аккум-лятор.
- **Strafe**: резолв apply/remove через ctx.resolveMovementAttributes (loadClass) заменён на РАНТАЙМ-класс атрибут-инстанса (attrInstance.getClass()); one-shot лог «modifier applied: x».
- **BlockOverlay**: метод World = **lIlIIiliil** (л на конце; был lIlIIiliI → молча глотаемый NoSuchMethod, рамка не рисовалась). One-shot «box resolved OK».
- ItemEsp/BulletTracers: one-shot логи (draw #1 / trail #1) — по следующему тесту видно, жива ли цепочка.
- Compile-фиксы: Method-импорт; после регэксп-удаления методов сверять оставшиеся вызовы (removeModifier/→removeMod).

**Fortnite Damage — НОЧНОЙ ВЕРДИКТ 09-09 (реверс dmg-каналов выполнен, [[rustme-night-s2c-catalog]] + [[rustme-night-0910-analysis]]):** цифры урона по живым чужим в сети НЕТ (dmg:death = только death-screen своего: timeAlive/killedBy/withA/attackDistance; dmg:close пустой). ПУТИ: (а) свой урон = полный эффект (метаболизм, разница тиков); (б) чужие = ФАКТ урона через EntityStatus ilililIIiI (int entityId + byte status, 2=hurt; хендлер iliilIliiI.IIillIlliI) via netty PacketHook read-only → hit-marker без цифры; (в) ЛОКАЛЬНЫЙ расчёт: пересечение трассера снаряда с AABB чужого + ⌊|motion|×NBT damage⌋, справочник множителей DamageMap liiilillii_6 (Map<броня,Map<оружие,Double>>) — клиентский, не сетевой. Спрайты-цифры из Lua-PNG (59×105 → assets → AssetData) остаются в силе для рендера.

Связано: [[rustme-custom-render]], [[rustme-jni-dll]].
