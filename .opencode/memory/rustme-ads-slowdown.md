---
name: rustme-ads-slowdown
description: NoSlow — ФИНАЛ v5.1 (DLL 21:18, ждёт чистого теста): замедление прицела = СЕРВЕРНЫЙ op2-модификатор −0.45 (×0.55) + серверный Slowness-эффект (оба подтверждены логом); v4 компенсация ПО МОДИФИКАТОРАМ (свой op2 +0.8182 по UUID) + v5 removePotionEffect(Slowness) каждый тик; флаги (бит7/usingItem) ганами НЕ ставятся; корень-1 = резолв атрибута «первый static»=maxHealth (фикс по имени); серия NPE после удаления Strafe закрыта (самодостаточный резолв в модуле); API IAttributeInstance/Potion/Slowness внутри
metadata:
  type: project
---

**СТАТУС 09-10: v5.1 РАБОТАЕТ — живой лог DLL ~00:52 показывает `[NoSlow] compensating: +0.8182` при прицеле (после фикса резолва атрибута по имени); финальная проверка скорости ходьбы в прицеле юзером не прислана.** Валидированная серверная картина замедления прицела (оба подтверждены живыми диаг): (1) **СЕРВЕРНЫЙ op2-модификатор −0.45** на movementSpeed (×0.55; при спринт+прицел mods=[-0.45/2 0.3/2] → eff=0.0715); (2) **серверный Slowness-эффект** (`[NoSlow] slowness effect removed` в логе 17:49). Флаги aimBit7/usingItem при прицеле ганами НЕ взводятся — все flag-based версии (v1-v3) молчали по определению. **Юзер УДАЛИЛ Strafe («создадим позже»)** — слайдер-фикс остался, механика не менялась. Дневник: v1 (07:41, правка полей холдера) — «все равно медленный» (тик атомарен, super на офсете 1404 ПОСЛЕ ×0.2); v2 (07:59, атрибут ×5) — «не работает», нет compensating (гейт-флаг не взводился + attr-методы не инициализировались); v3 (09:28, бит 7 + диаг) — диаг №1 выдал корень-1: `base=0.0 eff=0.0 mods=[]` → резолв атрибута «первый static» = maxHealth (фикс по имени, DLL 16:27); diag №2 (DLL 16:27) выдал корень-2/ФИНАЛ: `mods=[-0.45000001788139343/2]` eff=0.055.

**v4 (DLL 16:49): компенсация ПО МОДИФИКАТОРАМ без флагов.** Каждый 1мс-тик: скан `attrGetModifiers=lIIiIlIIII()` → произведение отрицательных op2 `Π(1+a_neg)`; если <1 — свой op2-мод (UUID 7a1c3d92-55e4-4b8a-9c60-1f2d3e4b5a6b, name rv_noslow_comp) amount = 1/Π(1+a_neg) − 1 (для −0.45 → +0.8182; eff = base×0.55×1.818 = base). Спринт (+0.3/2) не трогается (положительный, не входит в произведение; прицел+спринт: 0.1×0.55×1.3×1.818≈0.13). Ушёл замедляющий мод → свой remove (getModifierByUID=`IliiIlIIII(UUID)`, removeByUUID=`IIIiIlIIII(UUID)V`; повторный apply того же UUID = IllegalArgumentException → remove+apply при смене amount). Серверные S20 не трогают наш мод (пере-apply только своих). Диаг 1с: `neg= need= applied= base= eff= mods=[UUID:amount ...]` (dumpMods принимает ctx — ГАТЧ компиляции: static-метод не видит instance-поля, передавать ctx параметром). Критерий успеха: при прицеле `eff` возвращается к 0.1; спринт-бонус 0.13 сохраняется честно.

**v5 (DLL 17:39→17:49) = v4 + снятие vanilla-эффекта Slowness каждый агент-тик (юзер: «в noslow добавь такаже removePotionEffect(Slowness)» — паттерн конкурента NoSlowJaggerTick, [[rustme-chtdump-competitor]]):**
- Potion-API (всё public, javap): Potion-класс = `iillIlIIiI`; статический поиск по имени `IIlIIIIIlI(String)` → "slowness" (надёжнее ID — не зависеть от порядка регистрации); на EntityPlayer `liIlIliIiI`: removePotionEffect = `lIIiliilII(Potion)V`, isPotionActive = `IiIiiIilII(Potion)Z`.
- GameContext.resolveNoSlow дополнен: potionByName/removePotionEffect/isPotionActive (+attrNameMethod).
- **ТЕСТ 17:39: `[NoSlow] slowness effect removed` ПОДТВЕРДИЛ — сервер вешает Slowness-эффект при прицеле** (снимается каждый тик пока активен, один лог; если сервер ретранслирует — увидим частоту). Диаг дополнен `slow=true/false`.

**❗СЕРИЯ NPE v5 (17:39/17:44/17:49) — корень один: после удаления Strafe висячие зависимости GameContext:**
1. `resolveExtra` брал `ctx.modClass` (резолвился только в resolveMovementAttributes() удалённого Strafe) → null → getMethod NPE, resolveExtra не ретраился. ФИКС 17:44: NoSlow резолвит `rustme.iIliiliIiI` сам, методы — по ИНТЕРФЕЙСУ `rustme.illiiliIiI` (безопасно при прокси), ранний выход с ретраем в след. тик.
2. `ctx.modApplyMethod` — та же история. ФИКС 17:49: `modApplyMethod = attrInstC.getMethod("liIiIlIIII", modC)` в resolveExtra, вызов через своё поле.
**УРОК: после удаления модуля проверять, что GameContext-поля, которые резолвил только он, всё ещё кем-то резолвятся — или резолвить лениво в самом модуле (паттерн самодостаточного модуля).**

**v4 (DLL 16:49): компенсация ПО МОДИФИКАТОРАМ без флагов.** Каждый 1мс-тик: скан `attrGetModifiers=lIIiIlIIII()` → произведение отрицательных op2 `Π(1+a_neg)`; если <1 — свой op2-мод (UUID 7a1c3d92-55e4-4b8a-9c60-1f2d3e4b5a6b, name rv_noslow_comp) amount = 1/Π(1+a_neg) − 1 (для −0.45 → +0.8182; eff = base×0.55×1.818 = base). Спринт (+0.3/2) не трогается (положительный, не входит в произведение; прицел+спринт: 0.1×0.55×1.3×1.818≈0.13). Ушёл замедляющий мод → свой remove (getModifierByUID=`IliiIlIIII(UUID)`, removeByUUID=`IIIiIlIIII(UUID)V`; повторный apply того же UUID = IllegalArgumentException → remove+apply при смене amount). Серверные S20 не трогают наш мод (пере-apply только своих). Диаг 1с: `neg= need= applied= base= eff= mods=[UUID:amount ...]` (dumpMods принимает ctx — ГАТЧ компиляции: static-метод не видит instance-поля, передавать ctx параметром). Критерий успеха: при прицеле `eff` возвращается к 0.1; спринт-бонус 0.13 сохраняется честно.

**v5 (DLL 17:39→17:44) = v4 + снятие vanilla-эффекта Slowness каждый агент-тик (юзер: «в noslow добавь такаже removePotionEffect(Slowness)» — паттерн конкурента NoSlowJaggerTick, [[rustme-chtdump-competitor]]):**
- Potion-API (всё public, javap): Potion-класс = `iillIlIIiI`; статический поиск по имени `IIlIIIIIlI(String)` → "slowness" (надёжнее ID — не зависеть от порядка регистрации); на EntityPlayer `liIlIliIiI`: removePotionEffect = `lIIiliilII(Potion)V`, isPotionActive = `IiIiiIilII(Potion)Z`.
- GameContext.resolveNoSlow дополнен: potionByName/removePotionEffect/isPotionActive (+attrNameMethod).
- Снятие только если `isPotionActive` был true (один лог `[NoSlow] slowness effect removed`, без спама — сервер вешает эффект заново пакетом); диаг дополнен `slow=true/false`.

**❗NPE-БАГ v5 (лог 17:39) — первый из серии:** `resolveExtra` брал `ctx.modClass`, но `resolveMovementAttributes()` (единственный его резолвер) вызывал только УДАЛЁННЫЙ Strafe → modClass=null → `modGetUID = modC.getMethod` NPE, resolveExtra больше не ретраился, каждый тик падал на `getModifierByUID.invoke` null. **ФИКС 17:44:** NoSlow резолвит `rustme.iIliiliIiI` САМ (`gameLoader.loadClass`, обновляет ctx.modClass), методы атрибут-инстанса резолвятся по ИНТЕРФЕЙСУ `rustme.illiiliIiI` (безопасно при прокси), и при провале resolveExtra тик тихо возвращается с ретраем в следующем тике (без каскада NPE). **УРОК: после удаления модуля проверять, что GameContext-методы, которые резолвил только он, всё ещё кем-то резолвятся — или резолвить лениво в самом модуле.** (Вторая глава серии — modApplyMethod — выше в шапке v5.1.)

**❗КОРНЕВАЯ ПРОБЛЕМА (диаг v3, DLL 16:27):** `movementSpeedAttr` в GameContext.init() резолвился как **ПЕРВОЕ static-поле типа lIliiliIiI в SharedMonsterAttributes (IlilIiIIiI)** — а это НЕ movementSpeed! В классе 10 статик-констант (порядок clinit: maxHealth 20.0/0..1024, followRange 32.0/0..2048, knockbackResistance 0/0..1, movementSpeed, flyingSpeed, attackDamage, attackSpeed, armor, armorToughness, luck; имена-строки в XOR-классе iiiiIIiIII), и порядок полей class-файла ≠ порядок clinit. ВСЕ атрибутные фичи (Strafe-модификатор ×N, NoSlow setBaseValue ×5) молча применялись к чужому атрибуту — потомуStrafe «applied=true» без эффекта на скорость.
**ФИКС:** резолв **ПО ИМЕНИ** — перебор static-полей, `attr.getName()` (метод интерфейса lIliiliIiI) == `"generic.movementSpeed"`; не найдено → лог `movementSpeed attr NOT FOUND by name!`. В NoSlow одноразовый лог `attr instance of: <имя>`. Подтверждено логом юзера: `movementSpeed attr resolved by name OK` + `attr instance of: generic.movementSpeed` + `base=0.1 eff=0.13`.
**Диаг расширен:** `mods=[amount/op]` (attrGetModifiers=`lIIiIlIIII()`, modGetAmount=`lIllilIIII()D`, modGetOp=`IIllilIIII()I`). Юзер УДАЛИЛ Strafe («создадим позже» — модуль-механика не менялась, слайдер-фикс в CheatMenuScreen остался: клик-хендлер слайдера сравнивал локальные wx/wy против экранных trX — исправлены клик и драг в локальные координаты).

**ЦЕПЬ (финальная, всё дизасм-проверено):**
- `SP.liIIIiiilI` (onLivingUpdate, один тик): офсет **347** холдер `IIIililIiI.IiiIllillI()` перечитывает клавиатуру → 350-398 `if (liilIiilII() && !IlIIillIII()) holder.iilIIlIlI/lilIIlIlI ×= 0.2` → **1404** invokespecial super (`iiIililiiI.liIIIiiilI`, резолвится ВВЕРХ до wrapper'а — AbstractClientPlayer не переопределяет) → wrapper 125 super → EntityPlayer.liIIIiiilI: 353 виртуальный `IiIlliiilI()` копирует холдер в move-поля → 532 `travel(IiliIIliI, lIiliIliI, llIliIliI)`. **Всё атомарно в одном тике, потребление ПОСЛЕ умножения** → правка полей холдера между тиками мертва (v1 провален; старая запись «потребление до умножения» была ошибкой — super оказался на 1404, а не 125).
- Скорость клиента: `wrapper.liIIIiiilI` (IIiIIiIIiI) — офсет 143 `if(isRemote) skip` → 146-155 setBaseValue(movementInput.ililIIiilI()) НЕ работает на клиенте; 195-203 `liiiliilII(attr.getValue())` = setSpeed — читает атрибут КАЖДЫЙ тик. ⇒ `setBaseValue(base×5)` живёт весь тик; travel = вход холдера(×0.2) × speed(base×5) = ×1. Схема v2 была верной по механике — провалилась по флагу.

**ПРОВАЛ v2 — два корня:**
1. Гейт-флаг `liilIiilII()` = vanilla isUsingItem (поле SP `liIliiil:Z`). Писатели (дизасм): `iilIliilII(llIIIilIiI)` [start: isEmpty-чек + !уже-using → true], `IIilliilII()` [stop: false]. Ганы мода — НЕ vanilla-использование, флаг не взводится → компенсация не включалась. СТАРАЯ РЕКОМЕНДАЦИЯ «опираться на liilIiilII, не на бит 7» ОТМЕНЕНА.
2. КРИТИЧНЫЙ БАГ GameContext: `attrGetValue`/`attrSetBase` ОБЪЯВЛЕНЫ, но НИКОГДА не присваивались (в init() был только комментарий «здесь только attrGetValue/attrSetBase» без кода) — все атрибутные фичи молча теряли методы. ФИКС 09:28: резолвятся в init() сразу после getAttrInstanceMethod (`attrInstC = getAttrInstanceMethod.getReturnType()`).

**v3 (текущая):** главный флаг = dataWatcher **бит 7** `iIIlliilII()Z` (EntityPlayer; сервер-синхронный aim-флаг из анализа юзера 09-08: liiIillIII()=sneak бит 1, прицел бит 7) ИЛИ vanilla usingItem; компенсация при `(aimBit||usingItem) && !riding`; normalBase фиксируется из getBaseValue на первом тике прицела (в атрибуте замедления НЕТ — оно на входе холдера), вне прицела легитимная смена base трекается. DIAG раз в 1с: `[NoSlow] diag: aimBit7= usingItem= riding= base= eff= normal= comp=`.
**Трактовка диаг:** aimBit7=false при прицеле → искать настоящий aim-флаг (gun-классы/mouse-путь); aimBit7=true + comp=true, а скорость низкая → замедление в третьем месте (сравнить eff с фактической скоростью).

**IAttributeInstance (интерфейс illiiliIiI, impl rustme/IlliiliIiI):**
- getBaseValue = `ilIiIlIIII()D` (поле iiillIIiI:D); setBaseValue = `iIliIlIIII(D)V`; getValue = `iIIiIlIIII()D` — КЭШ (IllIlIIiI:D + dirty-флаг lIlIlIIiI:Z), пересчёт `iiIiIlIIII()D`: base + Σ(op0-моды); op1: value += base×amount (одиночный мод = base×(1+amount)); op2: value ×= (1+amount).
- **Все мутаторы (apply/remove/setBase) зовут `llIiIlIIII()` = setDirty** — кэш корректно инвалидируется.
- **ГАТЧ: apply `liIiIlIIII(mod)` КИДАЕТ IllegalArgumentException, если мод с тем же UUID уже есть** (IliiIlIIII(UUID)!=null → throw) — перед apply удалять (по UUID `IIIiIlIIII(UUID)` безопасно). Strafe делает remove→apply.
- getModifier(UUID) = `IliiIlIIII`; removeModifier(obj) = `liliIlIIII`; hasModifier = `IiIiIlIIII(mod)Z`; getModifiers = `lIIiIlIIII()`; by-op = `IiliIlIIII(int)`.
- AttributeModifier = `rustme/iIliiliIiI`: ctor (UUID, String, double amount, int op), getAmount = lIllilIIII()D, getOp = IIllilIIII()I. **movementSpeed НЕ фиксированное поле IlilIiIIiI (старая запись «static IIlilIiII»/«IIlilIiII» неверна) — резолвить ТОЛЬКО по имени** (getName() интерфейса lIliiliIiI), порядка полей/строк не доверять.
- Холдер `rustme.IIIililIiI`: iilIIlIlI=forward, lilIIlIlI=strafe (public float; SP-поле `illIiiil:Object`). НЕ ПУТАТЬ с movementInput `liIIIiIIiI` (wrapper.iiliiIiII) — другой класс.
- **lIillIiliI = free-look СИНГЛТОН, НЕ прицел**: iIlllliIl:Z = free-look флаг (канал FreeLookTogglePacketData); как aim-флаг не годится.

**Ревизия флагов 09-09 (полная, всё дизасм):** бит 7 = ВАНИЛЬНЫЙ eat/bow-флаг: писатель true ЕДИНСТВЕННЫЙ — `iIiiIIiIil(NBT)` (чтение из сейва!), сброс в travel `if(onGround && !remote) setFlag(7,false)`; держится `lliiliilII()` (vanilla-апдейтер: если bit7 && !onGround?? && !riding && в руке BowItem && usingItem — оставить + звук еды раз в 20 тиков, иначе false). `setFlag = IllllIlIII(IZ)V` на root; остальные флаги-обёртки: бит1=sneak `ilIiIllIII(Z)`, бит3=sprint `llllIiiilI(Z)`, бит5=лук `iiIliIilII(Z)`, бит6=сидит `lIliIllIII(Z)` (putfield IilililiI:Z + bipush 6), `liiiiiiilI()`=флаг 20, `IliiIIiIil()`=флаг 6. **Ганы мода бит 7 и vanilla usingItem НЕ ставят** — ПОДТВЕРЖДЕНО diag 16:27 (aimBit7=false usingItem=false при прицеле). Замедление при прицеле = серверный op2-модификатор (diag mods=[-0.45/2] — ПОДТВЕРЖДЕНО).

**Поза-таблица юзера (его реверс):** wrapper.lillllIlil() через setSize `lliliillII(FF)` (width llIililiI:F, height lilililiI:F): ползти lillIiiilI() 0.2×0.2/eye 0.2; прицел iIIlliilII() 0.6×0.6/eye 0.4; sneak liiIillIII() 0.6×1.65/eye 1.54; стоя 0.6×1.8/eye 1.62; через модифицируемое событие lIIlliiliI(player,w,h) (высоту править iIllIIiIil(F)). Бит 7 юзер считал прицелом — ревизия показала vanilla eat/bow; v3 использует бит 7 как флаг только вместе с vanilla usingItem и диаг.

Риски: сервер видит полную скорость при активном прицеле (сервер вешал модификатор — знает) → moved-too-quickly/античит; если флагается — вместо полной нейтрализации компенсировать частично (цель = eff×0.6-0.7). Живой тест с тестового аккаунта.

Юзер-вопрос «BulletESP/Timer не работали из-за этого?»: атрибутный баг бил ТОЛЬКО фичи через movementSpeedAttr/getAttrInstanceMethod (Strafe, NoSlow-attr); Timer (INPUT-аккумулятор), BulletTracers/ItemEsp/BlockOverlay (рендер-путь) жертвами не были — юзер удалил их до фикса, при восстановлении проверять механики независимо.

Связано: [[rustme-norecoil]], [[rustme-next-features]], [[rustme-night-s2c-catalog]].
