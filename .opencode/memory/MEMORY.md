# Memory index

- [User speaks Russian](user-language-russian.md) — respond in Russian
- [rustme JNI DLL project](rustme-jni-dll.md) — jni_rva_check: dllmain.cpp чистый (attach → лоадер с валидацией → DR0+VEH → DefineClass пакета → install); конвейер build_dll.bat = pack_agent.py (javac JDK8 → protected-кодирование с round-trip → agent_payload.h) → cl.exe; payload ТОПОСОРТИРОВАН (DefineClass резолвит super eagerly — потомок раньше родителя = define→0; pack_agent class_header+topo_sort); юзер сократил ZNANIA раздел 11 — вырезанные детали/планы только в memory
- [rustme phase gating](rustme-phase-gating.md) — юзер делит работу на фазы: анализ/реверс → отчёт в чат → ждать явного «делай»; код и инжект не начинать самовольно
- [rustme JNI offset recovery](rustme-jni-offset-recovery.md) — noslow архив (jni/agent/backup_noslow/): slowdown не локализован; класс-иерархия игрока: liIililiiI→iiIililiiI(4)=AbstractClientPlayer→IIiIIiIIiI(91)→liIlIliIiI(10)→IIlIIliIiI(23); ГАТЧ: lIliIiiIiI=Minecraft vs liIlIliIiI=EntityPlayer отличаются на 1 символ — проверять класс при NoSuchMethod
- [Cristalix feder API (jni/1)](rustme-cristalix-feder-api.md) — старая серверная библиотека dev.xdark.feder: для чит-клиента напрямую бесполезна; идея на полку — Threads.getAllThreads() через JavaLangAccess как запасной путь, если заблокируют getAllStackTraces
- [ZNANIA update policy](rustme-znania-policy.md) — ZNANIA.md обновлять только по явному разрешению; сначала результаты в чат
- [No file versioning](rustme-no-file-versioning.md) — в rustme не плодить версии файлов (v2/v3), менять существующие на месте; хук-путь и чат-тест отклонены
- [Git rustme: пуш по явной просьбе](rustme-no-git-push.md) — remote github hvhbro/rml-broodcheat, запушено 1f6db945; dump/ в репо, kimiko/rockstar-client-src/chtdump в gitignore, jar>100МБ не едет; перед пушем fetch + rebase -X theirs; не пушить самовольно
- [Правки вручную, не скриптами](rustme-manual-edits.md) — код править только Edit/Write; хередоки-скрипты ломали переводы строк в Java-строках (3 поломки, 09-10)
- [rustme MSDF-шрифты + watermark + ArrayList (сводная сессии 09-06/07)](rustme-msdf-font.md) — СВОДКА: MsdfFont instance-based (sf_semibold + medium rockstar, грузятся с диска jni/agent/fonts/msdf), Watermark rockstar (y=26, r=7, #906BFF, HH:mm, пинг-бары 450/300/150/75), ArrayList vanquish слева x=2; КРИТИЧНЫЕ ГРАБЛИ: GL_BLEND=0xBE→0x0BE2 в стабе, glTexCoord2f и glGetFloat мертвы в lwjglx, GL_ALPHA_TEST режет MSDF-градиент, glColor-сброс в конце ВСЕГО кадра HUD (иначе персонаж синеет), SDF-плашки рисовать +1px запаса (швы), FullBright гамма 10 (100=синий lightmap), пинг-ловушка Tab-снапшота (живой геттер illIillliI); С ДИСКА агент читает ТОЛЬКО 2 атласа (sf_medium мёртвый — можно удалить), пишет только логи C:\Logs, классы/шейдеры зашиты в DLL
- [rustme фичи/пинг/ночной план](rustme-next-features.md) — статус фич: ESP/Tracers/меню/watermark/GearESP-иконки/AimBot/Dormant/JumpCircle/ItemEsp/AutoShoot/KillEffect готовы; 09-10: Timer УДАЛЁН (их Timer-код в VMP), Strafe пересоздан 1:1 (перезапись motionX/Z), SoundEsp v2 = поллинг SoundManager.playingSounds (payload rust:sound:play не ходит; [[rustme-soundesp]]); DLL 23:41 81 кл (+ItemEsp, VisibleCheck, t-сдвиг предикта, FOV-конус 360°, AutoShoot, KillEffect-звук смерти) ждёт теста; ДЕФОЛТНЫЕ БИНДЫ УДАЛЕНЫ (тогглы только tickBind из меню, RSHIFT=меню; хардкод-бинды в модулях ЗАПРЕЩЕНЫ — двойной edge-детектор AimBot); ОТЛОЖЕН ночной план (декомпиляция/броня/mate:inv/спрайты/find_validator); NBT ammo_type=0 — классификация по displayName; гатчи сборки: LNK1168 пока игра запущена (процесс RustMe=лаунчер — не мешает линку), rustme.exe убивается только юзером
- [rustme сессия 09-10 ночь (сводка)](rustme-session-0910-night.md) — Timer удалён (их код в VMP), Strafe 1:1 (motion-перезапись: mag×((boost−0.1)×0.015+1) в воздухе, min(0.2806,mag) на земле, jump 0.42), SoundEsp (varint-парсер + диаг каналов, подписка инстанс-методом на синглтоне шины), JumpCircle (near-clip 0.2 + drawn/skipped счётчики + без screenSpace), KeybindsWidget (sweetie-порт, анимации 1:1), блюр-меню фикс (WINDOW uv по экранным координатам + fbWidth); ГАТЧИ: сканер rip-rel off-by-one, кейс-ловушки имён (zip-namelist проверка), юзер тестировал старую DLL (сверять mtime); InstantUse контракт снят, ждёт «делай»; DLL 02:33 65 классов ждёт теста
- [rustme InstantUse: shuse-версия вернута (09-10)](rustme-instantuse.md) — duration-форс (обнуление duration как у чита) ОТКЛОНЁН тестом: zeroing работал (лог forced 0 was 4000000000) но сервер не принял ранний lguse; по явной просьбе юзера откат на shuse (edge-detect getKeyAction → сразу shuse через helper liliiilliI.IlIIIIIIIl, anti-spam 300мс) — юзер считает её рабочей; «lguse» в дампе чита 0 вхождений; shuse ВСЁ-ТАКИ шлётся игрой (старое «отправителей нет» — ошибка CP-скана); ждёт теста
- [rustme кастомный рендер (история+тех-детали)](rustme-custom-render.md) — подробная история MSDF: геометрия MsdfGlyph.apply (gy=baseline-top*size-1, перо width*(size-1)), шейдер MSDF (pxRange=rangе*qw/(uvW*atlasW) на CPU, GLSL120 без textureSize/dFdx), два режима пера (properAdvance+left bearing у medium vs expensive-трюк у sf_*), загрузка с диска (payload ресурсов не держит), MSDF_DEBUG (R=сэмпл/G=alpha/B=uv-полосы), vh через abs() и min/abs порядок углов квада, parse_cp врёт на крупных классах → javap на копии под чистым именем
- [rustme ночная карта 9977 классов](rustme-night-map.md) — ESP-поля Entity root (X/Y/Z=IlilIiliI/lIllIiliI/IiiililiI, yaw=IlIlIiliI, pitch=IilIIiliI, AABB=ilIlIilIiI), камера lliIilliiI, RenderManager iiiliiiIiI, пинг=снапшоты в сетевом хендлере iliilIliiI.IiIllIIl (живой геттер illIillliI), FOV=gs.iIiiIilliI(); всё дизасм-проверено, детали в ZNANIA 14
- [rustme КАРТА MOTION-ПОЛЕЙ (Исправление ZNANIA)](rustme-motion-field-map.md) — motionY=iIIlIiliI, motionX=IIiIIiliI, motionZ=IiiIIiliI (ZNANIA 5/14.1 ошибочна; доказано jump 0.42→iIIlIiliI + спринт-буст −sin→IIiIIiliI/+cos→IiiIIiliI + addVelocity); motion RAW без XOR (reflection валиден); из-за перевёрнутой карты Strafe «взлетал» (писал в motionY), JumpCircle читал Z вместо Y; фикс РЕАЛИЗОВАН 09-10 14:17 (GameContext+Strafe+JumpCircle через ctx), DLL 65 классов ждёт теста
- [rustme МЕНЮ-ЭЛЕМЕНТЫ РЕАЛИЗОВАНЫ (09-10, DLL 21:15)](rustme-menu-elements.md) — чекбоксы/чипы/мультибокс/свотчи/секции rock 1:1 в CheatMenuScreen (Setting-иерархия в Module.java), плавный скролл: lIIillIlil + IiiliilliI.lillilIIIl=getEventDWheel; JumpCircle=kimiko 1:1; 09-11: нижний чип-шестерёнка = CONFIG-вкладка (конфиг-система); ждёт теста
- [rustme Конфиг-система (09-11, DLL 12:01, 89 кл)](rustme-config-system.md) — вкладка CONFIG за нижним чипом-шестерёнкой рельсы (повторный клик=выход), кнопки Save/Load/Delete/Reload/Open dir; файлы %LOCALAPPDATA%\RustMe\*.rm автосоздаются, шифрование XOR-xorshift32 (самообратим) + magic RMCF + CRC32 (битый файл = exception, не тишина); формат M;модуль;state;bind / F;настройка;float (покрывает Bool/Mode) / U;multi;0101 / C;цвет;argb, load в 2 прохода (значения→setState, onEnable видит загруженное), Menu исключён; ГАТЧИ: enterConfigMode обнуляет selectedAtLayout (иначе конфиг-скролл перетирает scrollStore модуля), клик-блок только панель ниже topH (иначе умирает драг окна), new String(bytes,"UTF-8") требует throws Exception; round-trip тест оффлайн PASS; ждёт инжекта/теста
- [rustme AntiOverlay (маски шлема/дайвинга)](rustme-antioverlay.md) — v4-концепция (реестр) НЕ РАБОТАЕТ: виджеты кэшируют спеки в полях (старый decomp iiiliillii_6); v5 = удалять виджеты из OverlayListener.overlays; **OverlayListener = rustme.liiiilIliI, singleton iiIIIilll, static ArrayList overlays, 22 фабрики (idx 2=HeavyHelmet liIiililiI, 3=DivingMask lliiililiI, 6=HotbarOverlay lilIililiI)**; utils/etc/Overlays.java hide/restore через runOnMainThread (CME!); Kotlin-метаданные текущего билда зачищены; маска = WornItemEffectOverlay (getIntensity=isWorn?1:0, поле item:RustEntry)
- [rustme Ambience + ColorPicker (09-11, DLL 12:53, 91 кл)](rustme-ambience.md) — тинт мира = multiply-квад glBlendFunc(GL_DST_COLOR,GL_ZERO) из CheatIngame ДО super (HUD чистый), белый цвет = нейтраль, Intensity лерпит к белому; пикер rock 143×136: SV 114×70 (4-угла ROUND: TL white/TR hue/BL+BR black), hue-бар 12×70 (6 сегментов, маркер 22+64*hue), пресеты 11×11 шаг 20 (LMB/RMB/+, дефолты 007AFF…), драг окна, клик мимо = закрыть; строка kind 6 = label+чип 10×10; пресеты в presets.dat (XOR); HSV ручной (не java.awt)
- [rustme Custom Time (09-11, DLL 13:04, 93 кл)](rustme-custom-time.md) — клиентское время суток: World.illlillllI() → WorldInfo.liIllIlIiI → setWorldTime iIlilIlllI(J)V (пара к liillIlllI()J с поля iIiIllllI:J — его жрут getCelestialAngle+skylight); apply каждый TickEvent (~1мс, сервер TimeUpdate перетирает), worldInfo заново каждый тик; пресеты-чипы пишут слайдер Time 0..24000; ГАТЧ: Module.onTick мёртвый хук — тики только через EventBus.subscribe(TickEvent, аноним)
- [rustme Custom Sky + Ambience (09-11, DLL 17:01, 106 кл)](rustme-custom-sky.md) — V3 = JumpCircle-style БЕЗ depth (probe DEAD 1.0/1.0 даже в world-pass — depth в форке недоступен, world-pass триггер снят): overlay-фаза, GlassGrab full-res+blur копии (геттеры добавлены), маска неба = |scene-blur|<tol (гладкое=небо, текстурное=рельеф); CustomSky = проц. звёздное небо в маску (blend по skyMask), Ambience = тинт рельефа (небо чистое), порядок CheatIngame: grab→Ambience→CustomSky→super; ГАТЧ: протекание в гладкий туман у горизонта (слайдер Порог неба); Overlays.resolve-спам исправлен (resolved-флаг в success-ветке)
- [rustme Custom Fov (09-11, DLL 13:46, 96 кл)](rustme-custom-fov.md) — FOV 0..200 через FloatNode Settings.getData().getGeneralSettings().getFov() (градусы, дефолт 70; пишется setValue без клампа как FullBright-гамма, читается getFOVModifier из gluPerspective); apply по изменению слайдера + реплай 1с, onDisable возвращает запомненное; кламп ≥1° (0 = вырожденная проекция); ГАТЧ: ZNANIA 14.2/14.12 ошибочны — gs.iIiiIilliI() это getPartialTicks (0..1), не FOV; full_src декомпиляция в tools/decomp/full_src/
- [rustme Hitmarker (09-11, DLL 15:26, 106 кл)](rustme-hitmarker.md) — ✕ на месте попадания (gamesense-порт), 2 слоя: (1) предикшн ЛКМ-edge + ray-AABB по игрокам, (2) сервер-подтверждение через hurt/hit-звуки SoundEsp (поллинг ВСЕГДА, окно атаки 1.2с, victim-snap ≤3м, заменяет предикшн-маркер ≤500мс); AutoShoot совместим — Robot-клик = ОС-клик, GLFW видит; форма 1:1 референсу (статичные ±3..±6, НЕ добавлять moved в координаты), lineWidth=guiScale; мировая привязка Esp.projectToScreen; hurt_time property-система = анимации моделей, не сетевой стейт
- [rustme View model (09-11, DLL 14:12, 101 кл)](rustme-viewmodel.md) — позиция/поворот/масштаб руки: подмена ItemRenderer Minecraft.llIlllll (public, rustme.llilIiiIiI, ctor принимает GameSettings) → rustme.ViewModelRenderer, оверрайд llIIliliII (player,pitch,swing,hand,swing,stack,equip) обёрнут в translate/rotate/scale; офсеты поверх штатного (±0.56/-0.52/-0.72); полный дедуп: старая декомпиляция full_src = ДРУГОЙ билд (имена не совпадают, сопоставлять по константам)
- [rustme JumpDistort 1:1 kimiko (09-10 16:49, DLL 65 кл)](rustme-jumpcircle-input.md) — их jumpdistort.fsh портирован (Shaders.JUMP_FSH GLSL-120 + JumpDistort.java захват-и-проход как GlassGrab + JumpCircle.preOverlay из CheatIngame ДО super: мир без HUD); волна: луч из камеры→плоскость круга (базисы F/R/U, tanF=1/m11), 4 цвета/угол, smoke raymarch 24ш, настройки kimiko, дефолты glow/warp/tint OFF; DEPTH-SWAP в проде (ensureDepth на живом FBO из GL_FRAMEBUFFER_BINDING, окклюзия stored>0.2&&stored<planeDepth−1e-4, graceful при aa≥2; GEQUAL-загадка — возможен флип сравнения); ФИКС ЗАВИСАНИЯ: GLSL-120 без битовых '&' (C7548 → int(mod)), ShaderUtil fail-fast (фрагмент не собрался → program=0, иначе пустой фрагментный квад заливает экран мусором); цвета кольца анимированы, easing 3 режима
- [rustme AimBot ПРЕДИКТ 1:1 + фиксы (09-10 23:28, DLL 77 кл)](rustme-aimbot-predict.md) — pred=pos+vel*(k*0.05+flightSec*k2)+Ydrop по таблицам конкурента; ГЛАВНЫЙ фикс «не предиктит»: t-сдвиг в ring-буфере (dt был секунды → vel ÷10-50); Visible Check рабочий (Vec3=lliililIiI ctor DDD + World.IlIilllllI — старый путь резолвил BlockPos и молча fail-open); FOV-гейт = 3D-конус 0..360° (круг от камеры через PROJ m11); selectTarget/findTarget публично; AutoShoot = зажим getKeyPrimaryAttack при наведённом прицеле; SHOT/aim-диаг; детали и гатчи в файле
- [rustme ВТОРАЯ волна карт: onGround=lliililiI (НЕ iiIIIiliI), Strafe адаптивный, InstantUse откат на shuse, AimBot стоячие (09-10 14:52)](rustme-field-map-round2.md) — гейт прыжка lliililiI&&jumpTicks==0; JumpCircle спавнил 1 раз из-за неверного onGround; Strafe: strafe==0 не трогаем+EMA эталона, стрейф=скорость вперёд; duration-форс сработал но сервер не принял ранний lguse — вернули shuse по юзеру; AimBot: снимок 0.2м ИЛИ 150мс
- [rustme JumpCircle + movementInput=capabilities (09-10)](rustme-jumpcircle-input.md) — белый квадрат = наш debug-код (JumpCircle 233-240, удалить); кольцо невидимо: порт потерял kimiko-ветку spacePressed&&onGround (круг под ногами); БОМБА: liIIIiIIiI = PlayerCapabilities (не input!) → ctx.moveForward=flySpeed, moveStrafe=walkSpeed (константы) — Strafe читал мусор; настоящий input-класс не найден; бинды = named KeyBindingsCategory.getKeyJump/Forward/Left/Back/Right; РАБОТАЕТ (подтверждено юзером 09-10: спавн каждый прыжок, до 3 кругов, волна+кольцо); кольцо = аддитивный lightning-блендинг (SRC_ALPHA,ONE) 64 сегм — иначе «просто радужный круг»; Strafe v3.1: max(ref×Ускорение, curMag) — бусты/кнокбэк сохраняются (18:05)
- [rustme ESP + ItemIcons + ItemEsp + KillEffect](rustme-esp.md) — Esp: view строим сами + PROJ lliIilliiI ТОЛЬКО при перспективной сигнатуре (ortho в буфере = мусор), lastTick-интерп чужих, NameTags с пингом (iliiililiI→illIillliI, без Tab); ItemIcons: lIlillIiIl()=texId ОБЩЕГО атласа (не «своя текстура» — опровергнуто 09-10), uv-поля iliIlIIliI (iIiIiilll,lIiIiilll,iiiIiilll,liiIiilll)=(u0,v0,u1,v1) в кэше float[5] по СТЕКУ, uniform fbHeight ОБЯЗАТЕЛЕН (0.0 = невидимы); ItemEsp РЕАЛИЗОВАН (09-10 23:02, DLL 75 кл): World.IiIiiiiil→liIilliIiI→stack.iIllIililI/lIiiIililI, плашка+7px, MAX 128, слайдер Дистанция; KillEffect РЕАЛИЗОВАН (09-10 23:41, DLL 81 кл): детект по звуку entity.player.death (kill-feed сервера НЕТ), атрибуция=AimBot.recentTargetNear, порт tornado/portal шейдеров юзера в GLSL-120 (KillShaders.java, #version обязателен), билборд по projectToScreen; история v1-v5 (RenderItem-путь провален: негативный кэш RenderItemController), PERF-уроки и раскладка лейблов — в файле
- [rustme NameTags-иконки хотбара + Hotbar HUD](rustme-nametags-icons.md) — RenderItem мода liiiliiIiI доступен из CheatIngame как поле lllIliil (iIIilIliII(stack,x,y)); хотбар форка = 6 слотов main-списка (InventoryPlayer iiIIIiIIiI: main=lllliIiII, selected=iliiIIiII — НЕ iIiiIIiII, тот мусор/счётчик; пустой стек=count 32767); у ЧУЖИХ игроков инвентарь НЕ синхронится сервером (пустой) — иконки только локального хотбара или «лут-история» чужих по entityId из дроп-пакета iIlllIIIiI; Hotbar v5 (09-11 12:31, DLL 90 кл): форковый хотбар рисует ОТДЕЛЬНЫЙ объект iillliil:IIiiiIliiI (не GuiIngame!) → подмена поля прокси-заглушкой HotbarStub (рендер пуст, методы форвардятся оригиналу), выбор слота форка = lIiliIliiI.lIllllIliI(); XP/сердечки/еда — НЕ перенесены
- [rustme что знаем о ЧУЖИХ игроках (карта 09-07+08)](rustme-remote-player-info.md) — ИМЕЕМ: GameProfile (iIIiiIiII/IlIiIiiilI), пинг, gamemode, скин, геометрия+yaw/pitch/sneak/sprint-флаги, absorption (dataWatcher float), тиммейт-позиции+Online/Offline/Dead/Wounded (TeamInfo→llIllIiliI), скорборд, assignfriend-профили, дроп-пакет лут-история; 09-08: рука+броня чужих ЕСТЬ (S04 IiililIIiI); НЕТ: хотбар/HP чужого, голос — без чтения чужих каналов (риск бана)
- [rustme wet_world/sky — УДАЛЕНО (09-08), ROOT-CAUSE найден 09-09](rustme-wet-world-shader.md) — depth-текстуры в пайплайне форка ПРИНЦИПИАЛЬНО НЕТ: главный FBO iiliiiiiii_21 (iIlIiIiIiI) держит depth как RENDERBUFFER, при MSAA aa≥2 мир рендерится в отдельный MS-FBO (iiiiiiilii_62) и в главный blitится ТОЛЬКО ЦВЕТ (mask 16384) → depth очищен каждый кадр; FBO пересоздаётся на ресайзе; сам мод depth НИГДЕ не сэмплирует — все постэффекты = silhouette-маски (ArmorBodyZoneGlow iiiiiiilii_59: drawSilhouette → blur down/up → composite); рецепт наших 3D-эффектов: silhouette-схема или ваниль outline.fsh (sobel по ЦВЕТУ, уже расшифрован в assets); старое: depth-подмена/copyScene работали, glActiveTexture-no-op опровергнут (lwjglx=тонкий прокси в нативный LWJGL3), GL-гатчи (0x8B8D, feedback, far=243м), стабы GL30/GL13 оставлены; soyuz-master (C++ нативные хуки) — источник идей
- [rustme soyuz-master (референс 09-08)](rustme-wet-world-shader.md) — второй клиент-исходник C:\Users\Admin\Desktop\rustme\soyuz-master (C++, нативные GL-хуки): их Sky = хуки glClearColor/glFogfv (запрещённый нам путь), но подтверждает fixed-function GL рендера; модули Aimbot/ESP/Chams/Tracers/WallCheck/ChinaHat/AspectRatio — источник идей как rockstar-client-src
- [rustme depth-swap ПРОЙДЕН (09-08)](rustme-depth-swap.md) — подмена depth renderbuffer→текстура РАБОТАЕТ (COMPLETE, depth живой, игра цела); поля FBO iIlIiIiIiI: fb=lllIIiIiI color=iIlIIiIiI rb=illIIiIiI W=lIlIIiIiI H=iiilIiIiI; DepthTest удалён при реализации WetWorld; константы ревалидированы 09-10 (DEPTH_COMPONENT24=33190 из create-метода, дампы r1-r9); 09-10 18:29: подмена в оверлее УДАЛЕНА (там чужой FBO без depth-rb — ломала игру), read-only probe FBO 1..12 все c0/d0, occlusion из JumpDistort удалена по юзеру
- [rustme устойчивость к апдейту игры (анализ 09-08)](rustme-update-resilience.md) — 4 слоя поломки: 4 RVA (методика calibrate готова), слоты (верифицировать только ~14 критичных, пачкой за инжект), валидатор 0x1C2D0 (ЕДИНСТВЕННОЕ без автопоиска — план find_validator.py по константам 40039/28200/59236), имена агента (semantic-матчер по scan_db.json); СНЯТЬ ОТПЕЧАТКИ КЛАССОВ ДО АПДЕЙТА; пробный класс loader-цепочки → кандидат named ru.rustme.settings.Settings
- [rustme сеть C2S + эксплойт-поверхности (анализ 09-08)](rustme-network-c2s.md) — конвейер отправки payload: liliiilliI.IlIIIIIIIl(player,channel,data) → EfficientBinaryFormat → IIlllIIIiI (≤32767Б) → netHandler.llIlIllilI; карта 47 C2S-каналов в tools/c2s_map.json (itmact: Eat/Drink/RefillTank/WeaponAmmoChange…); клиент payload НЕ валидирует; НЕ ТРОГАТЬ: astraea/astraea-init (античит через NativeAPI.handlePacket — класса в дампе нет), rust:admin:*, voicechat (AES/GCM ключ в lIiIiilliI); серверная валидация неизвестна — только живой тест с тестового аккаунта
- [rustme NoRecoil реализован](rustme-norecoil.md) — клавиша N: отдача = S2C gun:recoil (float yaw, float pitch, byte mode) → жжение накопителей ilillIiliI.IliiiiIIl/iliiiiIIl 1мс-циклом + офсеты фасада lIillIiliI ТОЛЬКО при выключенном free-look (иначе там живая камера); mode==0-прямой не покрыт → дельта-компенсация; РАБОТАЕТ (тест юзера 09-08: горели и smooth-ветка, и facade-ветка); burn-логи удалены при чистке диагностики 22:38 (по логу путь больше не виден)
- [rustme SVO скины реализованы](rustme-svo-skins.md) — клавиша H: все скины → putin.png (вшит байтами через AssetData/generate_asset_data); SvoTexture implements iiIiliiIiI, регистрация в gs.IIiIiilliI().lliIilliII, перезапись SKIN в картах снапшотов, main-thread очередь GameContext.runOnMainThread; ГАТЧ: newInstance() на интерфейсе = InstantiationException — только new rustme.SvoTexture(); РАБОТАЕТ (тест 09-08)
- [rustme NoSlow ФИНАЛ v5.1 (DLL 21:18+ — ждёт чистого теста)](rustme-ads-slowdown.md) — замедление прицела = СЕРВЕРНЫЙ op2-модификатор −0.45 (×0.55) + серверный Slowness-эффект (оба подтверждены диаг); флаги (бит7=vanilla eat/bow только из NBT, usingItem=еда) ганами НЕ ставятся; v4 = компенсация ПО МОДИФИКАТОРАМ (свой op2 +0.8182 UUID 7a1c3d92, скан отрицательных каждый тик) + v5 removePotionEffect(Slowness) каждый тик; КОРНИ провалов: movementSpeedAttr резолвился «первым static»=maxHealth (фикс по имени generic.movementSpeed), attrGetValue/attrSetBase никогда не присваивались в init (фикс), серия NPE после удаления Strafe (modClass/modApplyMethod — резолвить в самом модуле); v1-правка холдера мертва (тик атомарен, super на офсете 1404); IAttributeInstance/Potion/Slowness API и поза-таблица в файле; 09-10: компенсация подтверждена живым логом (+0.8182 при прицеле)
- [rustme SoundEsp v2.3 (09-10, DLL 17:59)](rustme-soundesp.md) — звуки НЕ через payload (rust:sound:play не ходит, diag доказал); поллинг gs.iiliIilliI()→SoundEngine.iIliiiil→SoundManager.IiliIliIl playingSounds (дифф по ключу); ГАТЧ-тройной: имена loadClass копировать из scan_db (IIlIiliiI≠IIIlIiliiI); геттеры по РАНТАЙМ-классу инстанса; ResourceLocation = lllililIiI (path-геттер lIilllillI, НЕ wrapper llIilIiliI.getName=UUID-ключ); 09-11: classify нормализует _→. + step→Footsteps (шаги/ak47 чинены)
- [rustme ревизия 3D-рендера (09-10)](rustme-render3d.md) — FBO iIlIiIiIiI поля/методы (depth=RENDERBUFFER 33190), GL-константы liIlIiiIiI (36160/36161/36064/36096, D24=36055), runGameLoop→renderFrame порядок (камера→sky→terrain→entities→маски до/после→пост), камера-статики lliIilliiI.iIiliiiiII (2982/2983/2978+gluUnProject), пост-группа IllIiIiIiI (FXAA json, пассы), рецепты 3D-фич
- [rustme PacketFly-анализ (09-08)](rustme-packetfly-analysis.md) — onUpdateWalkingPlayer=iiIlililiI (полная ванильная логика флагов/порогов снята), C03-семейство=IIiiilIIiI(C06)/iIiiilIIiI(C04)/lIiiilIIiI(C05)/iliiilIIiI(база, public-поля x/y/z/yaw/pitch/onGround), отправка llIlIllilI→IllIlIIIiI.iIIIlllilI→netty writeAndFlush; S08-телепорта НЕТ (iIIlilIIiI=ping-контроль long→эхо, не трогать); ТЕСТ ПРОВАЛЕН (юзера флагает) → ОТЛОЖЕНО: код в jni/agent/backup_packetfly/ (не компилится), RustClient-регистрация удалена, чистая DLL 40 классов; стабы netty в game_stubs.jar остались; ПРАВИЛЬНАЯ позиция хука=addAfter("packet_handler") (outbound от хвоста!), гипотезы флага в memory-файле; jump-поле инпута=iiIiIIiII:Z; ГАТЧ javap на NTFS case-коллизии — только отдельные каталоги; ГАТЧ netty-jar лаунчера под MD5-хешами — не распаковать, только стабы. СЕРВЕРНАЯ ВАЛИДАЦИЯ декодирована — см. [[rustme-movement-exploits]]: floating-check 80 тиков (dY≥−0.03125 — наш спуск −0.08 ПОПАДАЕТ под кик!), moved-too-quickly 100.0, elytra-бит 7 dataWatcher
- [rustme watermark: имя сервера (РАБОТАЕТ, подтверждено 09-08)](rustme-watermark-servername.md) — mapId = id карты/вайпа НЕ сервера (обход 24 серверов: один mapId на кучу серверов, addr один, serverData null); НАСТОЯЩИЙ источник: таб-заголовок S47-аналог rustme/iIllilIIiI → netHandler.lIiiillliI → GuiIngame.liIiiiIliI().iIilliil (Tab GUI liIlliliiI) поле iIIiIIil, plain-text lllIlliIIl(); читается каждый кадр без открытия таба через наш GuiIngame (подсказка юзера: имя видно в табе, читать не открывая); РЕАЛЬНЫЙ ФОРМАТ хедера многострочный с §-кодами: строка 1 '§cRust§fMe §7» §aПесочница-1' (режим), строка 3 'Онлайн » N' — serverName() = stripColorCodes → первая непустая строка → после » (U+00BB); парсер по '-' был неверен (цеплял Онлайн); 22:38 вся диагностика удалена (KEY-I/TAB-ROWS/probe, NoRecoil burn, Tracers diag) — клавиша I свободна; 22:48 фикс центрирования: остров+бары coreGroupW строго по центру, лейбл пристаёт слева LABEL_GAP=4 (как часы), drag posX=левый край лейбла, getX/getRect согласованы
- [rustme movement-эксплойты: Spider/Fly план (09-08)](rustme-movement-exploits.md) — СЕРВЕРНАЯ валидация в jar (moved-too-quickly 100.0, floating 80 тиков: dY≥−0.03125 && !mayfly && !вода && !elytra(бит7) && нет блока в AABB 0.55 ниже); SPIDER РАБОЧИЙ и ПЕРВЫЙ в очереди «делай» (юзер за): прижатие к стене держит floating=false → motionY +0.2/−0.15/0 по лестничной семантике, флаг лестницы не трогать; FLY без предмета = кик 4с, но 3 ваниль-режима (вдоль поверхностей / пульсирующий подъём с нырком / вода) — отложен до теста Spider; capabilities-карта исправлена (flying=liiiliilI, mayfly=llllIiilI); NoFall = рассинхрон (урон считает сервер), NoClip = rubberband
- [rustme ночь 09-09: S2C-каталог + фичи + WetWorld root-cause](rustme-night-s2c-catalog.md) — dmg-каналы НЕ несут цифру урона по чужим (только death-screen); полный S2C-каталог 113 записей (s2c_catalog.json); WETWORLD=depth-текстуры НЕТ в пайплайне (MSAA-blit color-only + RB + ресайз-пересоздание) → рисовать silhouette-масками как ArmorBodyZoneGlow; декомпиляция full_src уже полная (9978 java); **РЕАЛИЗОВАНО 09-09: Strafe (атрибутный модификатор movementSpeed op=multiply UUID 6f9b2a11, резолв по РАНТАЙМ-классу инстанса) + BlockOverlay (MOP=mc.illiiiIl → iIIlIlillI→lIllIiIIiI(BlockPos) → world.lIlIIiliil(pos) → blockState.IIliIIiliI(world,pos) → 12 рёбер через Esp.projectToScreen) + ItemEsp (World.iillliliii_14.IiIiiiiil → liIilliIiI → ItemIcons v3) + BulletTracers (lIIIIiIIiI, entityId iiIIIIlIII, история 24 точки) + Timer (float-аккум IiIIiillI += (mult−1)×delta liIIiillI — НЕ int iiIIiillI, тот стирается liiiliIllI каждый кадр!), слайдеры Boost/Duration-ms через Module.FloatSetting; DLL 08:54 57 классов; ❗ЮЗЕР ПОПРОСИЛ «ПРОВЕРЬ ВСЁ» — АУДИТ В ПРОЦЕССЕ: найден НЕИСПРАВЛЕННЫЙ баг Timer (lastAcc=acc ставится ДО setFloat(acc+extra) → повторная добавка каждый 1мс-тик внутри кадра → кик; фикс: lastAcc = acc+extra ПОСЛЕ записи), BlockOverlay-цепочка верифицирована (lIllIiIIiI=BlockPos, IIliIIiliI в интерфейсе IllIIiliiI); ОСТАЛОСЬ: фикс lastAcc, Esp.updateCamera в начале BlockOverlay/ItemEsp/BulletTracers.render (иначе без ESP камера не готова — cameraReady=false навсегда), +partialTicks параметры, пересборка DLL
- [rustme полная декомпиляция + сеть + чужой инвентарь + баллистика (09-08)](rustme-decompile-full.md) — CFR 0.152: full_src 9977 + named_src 974, kotlin_name_map.json (1259 имен) + file_class_index.json; S04-экипировка (руки+7 брони) у чужих СИНХРОНИТСЯ, хотбар НЕТ; снаряд liiiiiiiii_46 (0.99 drag/0.05 grav, урон=|motion|×NBT damage); payload-шлюз → PayloadPacketReceiveEvent (подписываемая точка); эксплойт-поверхность: admin-каналы/NBT/astraea — НЕ ТРОГАТЬ без «делай»
- [rustme merge agent-обновления друга (09-09)](rustme-menu-tracers-merge.md) — коммит друга a7ec5a57: взяли ТОЛЬКО agent, tools друга НЕ приняты (его JDK-путь битый на нашей машине); конфликты RenderUtil (наш prevProgram-фикс + их screenSpace/border) и Watermark (наш сервернейм + их drag, якорь=левый край) разрешены; стабы пересобраны из compile_stubs целиком (+glMatrixMode/GL_LINES, kotlin Function1 восстановлен); отладка 09-09 — 3 инжекта за вечер: (1) ложная тревога на старой DLL 40 классов (отпечаток сборки = `define done: N/N` в jni-логе + mtime), (2) реальные баги друга: GlassGrab сэмплил ПУСТУЮ текстуру (экран никто не копировал → фикс glCopyTexSubImage2D) и IconRender на fixed-pipeline texcoord (переписан на GL20-шейдер ICON по MSDF-схеме), гипотеза glActiveTexture no-op ОПРОВЕРГНУТА (lwjglx — честная тонкая прокси в нативный LWJGL3, дизасм dump/classes/minecraft/org/lwjglx/), (3) финальный корень невидимости меню+HUD: glGetInteger(GL_VIEWPORT, IntBuffer) писал в буфер, код читал нулевой int[] → glViewport(0,0,0,0) — УРОК: значения GL читать ИЗ IntBuffer (vpBuf.get(i)), массив не обновляется; сборка 23:05 подтверждена логом (Glass/Icons/Menu ок); a7ec5a57 вошёл в историю 09-10 (rebase -X theirs), наш 1f6db945 запушен — [[rustme-no-git-push]]; после rebase чужие НЕ-конфликтующие tools-хунки принесли битый JDK-путь в pack_agent.py/jv.py → списки кандидатов javac/javap с isfile-выбором; 09-09 вечер — ВИЗУАЛЬНАЯ СВЕРКА меню с оригиналом Class215/Class242: палитра exact (ACCENT2=#4D00FF, окно a229, базы чипов/полей=FLAT), чипы gap 9 после logo-блока, vc/vcM по cellHeight (не size*1.2), шрифты ник/LEEK/NoSettings переставлены, поиск x+8.5, аватар белый круг, порядок паддингов rock=(top,left,bottom,right); сборка DLL 01:00 (47 классов — состав плавает от параллельной правки Esp, критичные на месте)
- [rustme ночь 09-10: S2C каталог + dmg + depth + цепочки](rustme-night-0910-analysis.md) — dmg-каналы НЕ несут цифр урона (только source смерти) → Fortnite Damage только локальный расчёт; 92 S2C канала в s2c_full_catalog.json; их RenderTarget без depth, color-текстура главного FBO сэмплируется их blur'ом (референс); SilentAim: wrapper.iiIiiIiilI + C03 public yaw/pitch; профиль = gs.lllIllil → UserProfile (3 String + GameProfile)
- [rustme chtdump: дамп чужого JNI-чита под наш сервер (09-09)](rustme-chtdump-competitor.md) — полная карта алгоритмов конкурента: AimBot (ammo-enum→ring→баллистика 0x29440→0x2c530), NoSlow в JaggerTick, config→data-слоты всех модулей, SoundESP-словарь 160 записей, HitBox/AutoRespawn/Timer/Dormant пошагово; наши реализации, баллистические таблицы (tools/ballistics_tables.json), недоделки 1:1 (row[18] за 200м, epsilon-ветка) и статусы тестов — в файле
- [rustme сессия 09-09 вечер](rustme-session-0909-evening.md) — AimBot (порт конкурента, ammo-classifier, trackingSince-predict, setRotation-метод) + JumpCircle (порт kimiko, 3D-кольцо, радуга/градиент) + InstantUse (lguse на using-end, shuse убран) + Dormant ESP (4.48с призраки) — все собраны 58 классов invoke чист, ждут теста; PacketFly отложен
