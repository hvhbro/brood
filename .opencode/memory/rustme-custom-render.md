---
name: rustme-custom-render
description: Кастомный рендер rustme: CheatIngame, GL20-шейдеры, MSDF-шрифт работает (корень GL_BLEND=0xBE→0x0BE2); два шрифта (medium=ArrayList+ватермарка, sf_semibold запас); ArrayList переписан под vanquish (плашки на модуль, градиентные полоска+текст, medium 6px, без анимаций); ватермарка y=26 r=7 часы HH:mm + пинг-бары; пинг-геттер illIillliI (живой lIlllIIl); голубой персонаж — glColor-сброс в конце ВСЕГО кадра HUD; FullBright гамма 10
metadata:
  type: project
---

Сессия 2026-09-05/06 (проект rustme): кастомный рендер и структура клиента.

**РАБОТАЮЩИЙ рендер (v39-40, эталон):**
- Подмена GuiIngame: `rustme.CheatIngame extends rustme.liIIliliiI` (пакет rustme обязателен), override `iliIiiIliI(F)V` (renderGameOverlay, public). Инстанс — в поле `gs.liliiiIl` (Object), swap через GameContext.ensureHud() (скан gs→mc→holder, идемпотентно).
- FontRenderer берётся наследуемым геттером `llliiiIliI()` (мод держит шрифт в gs Object-поле, MSDF).
- ScaledResolution = `liIIiIliiI`, ГЕТТЕРЫ: IIllIlIliI()=scaledWidth, lIllIlIliI()=scaledHeight, illlIlIliI()=guiScale (дизасм ctor; НЕ по порядку полей!).
- Система координат (КРИТИЧНО): вершины сырого GL — SCALED (ванильная ModelView ×guiScale ЖИВА в renderGameOverlay), gl_FragCoord/uniforms — ФИЗИЧЕСКИЕ пиксели, FontRenderer.drawString и ванильный Gui.drawRect — SCALED (свой масштаб внутри).
- GL20-шейдеры работают (нативный org.lwjgl.opengl, мод сам их использует — 8 классов). SDF roundedRect: uniform rect=(x,y,w,h) в физике + fbHeight (переворот Y) — texcoord через lwjglx glTexCoord2f НЕ доходит до шейдера (gl_FragCoord — единственный путь).
- Шейдерный квад = обычный glBegin/glEnd QUADS в scaled-координатах, форма вырезает фрагментный шейдер по gl_FragCoord.

**СТРУКТУРА (expensive-стиль):** client/RustClient (entry, pack_agent DEFAULT_ENTRY=client/RustClient), events/EventBus, modules/api/{Module,Modules} (реестр), modules/impl/{AutoSprint,FullBright}, utils/etc/{GameContext,Log}, utils/render/{CheatHud,RenderUtil,ShaderUtil,Shaders,GuiScale}, rustme/CheatIngame. Новый модуль = класс extends Module в modules/impl + new в RustClient.

**Компиляция:** pack_agent --cp = game_cp.jar (9977 rustme классов из minecraft_FULL_DEOBF.jar, чистые имена) + game_stubs.jar (стабы org.lwjglx.opengl.GL11 + org.lwjgl.opengl.GL20 + kotlin Function1). build_dll.bat прокидывает --cp. ГАТЧ: в --cp идёт ГОТОВЫЙ jar — правка исходников стабов tools/compile_stubs/**.java требует ПЕРЕСБОРКИ game_stubs.jar (javac JDK8 -source/target 8 -d tools/compile_stubs/build → `jar cf ../game_stubs.jar -C build .`), иначе javac видит старые символы.

**ArrayList шрифт v47 = CustomFont, гладкий TTF (собран, ЖДЁТ ИНЖЕКТА):** юзер: «в arraylist поменять шрифт с обычного майнкрафтовского на красивый гладкий». Новый `utils/render/CustomFont.java`: системный TTF (цепочка Segoe UI Semilight→Light→Segoe UI→Tahoma→Arial, fallback DIALOG) растеризуется java.awt (TEXT_ANTIALIAS_ON + FRACTIONALMETRICS + RENDER_QUALITY) в атлас 1024×512 (ASCII 32..126 + кириллица 0x410-0x44F + Ёё; GLYPH_PAD=2), глифы БЕЛЫЕ с альфой (RGB=255, цвет задаёт glColor4f через GL_MODULATE). Растеризация 4× (FONT_H=10 scaled → атлас 40px) — при guiScale 2 почти 1:1 тексель/пиксель. Рисование — ТЕКСТУРИРОВАННЫЕ квады через lwjglx GL11 fixed pipeline, НЕ шейдер (texcoord через lwjglx в GLSL не доходит, 12.3 п.8; fixed-текстуринг доказан v41-43). Тень = тот же глиф чёрным offset 1px alpha 60%. Биндинг текстуры save/restore через glGetInteger(GL_TEXTURE_BINDING_2D=0x8069) — не рассинхронизировать GlStateManager игры. Атлас строится лениво при первом кадре с главного потока; при ошибке — fallback на игровой FontRenderer (в CheatHud флаг custom/available()). CheatHud: FONT_H=10, LINE_H=13 и layout не тронуты. Сигнатуры GL11 ВЕРИФИЦИРОВАНЫ по дампу `dump/classes/minecraft/org/lwjglx/opengl/GL11.class` (tools/dump_members.py): glTexImage2D(IIIIIIIILjava/nio/ByteBuffer;)V, glGenTextures()I, glBindTexture(II)V, glTexParameteri(III)V, glGetInteger(I)I — все есть в настоящем lwjglx. Стаб GL11 поправлен (ByteBuffer вместо Object + glGetInteger + константы GL_LINEAR/GL_CLAMP/GL_RGBA/GL_UNSIGNED_BYTE/filter enums). Сборка: 23 класса round-trip OK, java/lang/invoke нет. В логе `Font: custom font built: '<имя>' atlasFontPx=...` + `first frame DRAWN (font=custom)`; стиль крутится константами FONT_H (CheatHud) / FONT_CANDIDATES (CustomFont).

**ArrayList шрифт v48 — фиксы после первого инжекта + ИТОГ (2026-09-06):** симптомы v47 «пиксельный, неровный, слишком большой, вылезает за рамки»: (1) квады рисовались сквозь ЧУЖОЙ GL-state (мод рисует свой MSDF-шрифт GLSL-шейдером; texcoord через lwjglx в него не доходят → сэмплится чужая MSDF-атлас — глифы огромные/мыльные) → перед рисованием: glGetInteger(GL_CURRENT_PROGRAM=0x8B8D) + glUseProgram(0), glGetInteger(GL_ACTIVE_TEXTURE=0x84E0) + GL13.glActiveTexture(GL_TEXTURE0=0x84C0), всё save/restore + GL_TEXTURE_BINDING_2D; (2) у Segoe UI max-метрики ≈1.33em (ascent+descent) — ячейка вылезала за LINE_H=13 → ink-границы через GlyphVector.getVisualBounds (упаковка и квады по реальным чернилам, dy от baseline, advance=glyphMetrics.getAdvance дробный); (3) FONT_H 10→9, верт. центрирование по CustomFont.cellHeight(); (4) стаб GL13 (glActiveTexture(I)V верифицирован по дампу org/lwjglx/opengl/GL13) добавлен в compile_stubs и game_stubs.jar. ГАТЧ: FontRenderContext лежит в java.awt.font, НЕ java.awt. ИТОГ ИНЖЕКТА: «стало лучше, за рамки не вылезает, но всё равно неровный, искажается, пиксельный» — наивный путь (рантайм-растр TTF 36-40px → квады без мипмапов) фундаментально плох.

**Референсы шрифтов разобраны (шейдеры прочитаны, 2026-09-06):**
- **Rockstar = slug (векторный, без запечки):** `rockstar-client-src` — Font.createFont(0=TRUETYPE, .otf) → deriveFont(2048.0f мастер-размер), FontRenderContext(null, false, true) (AA off, FM on); контур глифа разворачивается в квадратичные безье-сегменты (IiIIii_Class20, flatten CPU по запросу) → 3 текстуры: контрольные точки float RGBA (4096 wide, GL_RGBA16F), полосы-адреса (uint), таблица глифов → фрагментный шейдер slug.glsl (164 строки, #version 150, usampler2D, flat int GlyphIndex через UV2) аналитически пересекает пиксель с кривыми. Портировать ≈1000+ строк — оверкилл для ArrayList.
- **Expensive = MSDF (предзапечённый):** `expensive for yougame` — атласы сгенерены msdf-atlas-gen заранее (assets/minecraft/expensive/fonts/: sf_semibold/sf_bold/sf_medium/Montserrat/consolas — PNG+JSON; JSON: atlas.distanceRange/width/height, metrics.lineHeight/ascender/descender, glyphs[].unicode/advance/planeBounds/atlasBounds, kerning). Шейдер: dist = median(rgb)-0.5+Thickness; alpha = smoothstep(-Smoothness, Smoothness, dist*Range*inversesqrt(h)), h = dFdx/dFdy(TexCoord)*textureSize — инвариант к масштабу; GL_LINEAR обязателен («to correct msdf font anti-aliasing»). Готовые MSDF-атласы только для SF UI, НЕ для medium.otf.
- **Watermark rockstar = medium.otf:** IiIiiiiIi_Class190 (island-элемент "default") рисует заголовок через IIiIiI_Class11.i_field_857c0621 = I_method_6047f1b7("medium","medium") → assets/rockstar/fonts/medium.otf. (Был скопирован в jni/agent/fonts/, ПОТОМ УДАЛЁН — юзер перешёл на готовые expensive-атласы.)

**msdf-atlas-gen (скачан юзером):** C:\Users\Admin\Desktop\rustme\tools\msdf-atlas-gen\msdf-atlas-gen.exe (v1.4.0). Дефолт ASCII работает (95 глифов → 512x512). ГАТЧ: НЕ-ASCII charset задать НЕ УДАЛОСЬ — «Failed to load/parse character set specification» на всех синтаксисах: -charset файл с 0x20-0x7E / 0x0410-0x044F, файл «a..z A..Z 0..9», файл «abcXYZ», inline -chars "a..z", "a-z", "0x20-0x7E", юникод-экраны \u0410. (Поэтому юзер и решил не запекать medium.otf вовсе.)

**ФИНАЛЬНОЕ РЕШЕНИЕ (2026-09-06, юзер): НЕ генерировать rockstar-шрифт, взять ГОТОВЫЕ атласы из expensive.** medium.otf/charset-черновики удалены; в проекте `jni/agent/fonts/msdf/`: sf_semibold.png+json (основной) и sf_medium.png+json (запасной), скопированы из `expensive for yougame/src/assets/minecraft/expensive/fonts/sf_*.ttf.{png,json}`. Система рендера = expensive (MSDF), slug rockstar отклонён (~1000+ строк рантайма, отладка вслепую).

**Верификация expensive-атласов (2026-09-06):** PNG 776x776 RGBA 8-бит; JSON type=**mtsdf** (MSDF+однобайтовая альфа), distanceRange=20 В ПИКСЕЛЯХ АТЛАСА при size=64 em (= 0.3125 em — семантику Range в шейдере сверять с этим), yOrigin=**bottom**; metrics emSize=1, lineHeight≈1.193, ascender≈0.952, descender≈-0.241; 160 глифов: латиница 58, кириллица 66 (Ё 0x401 + ё 0x451 есть), цифры, пунктуация; глиф 32 (пробел) БЕЗ planeBounds (только advance — как в expensive обрабатывается); kerning-пар в JSON нет (не критично для HUD).

**РЕАЛИЗОВАНО — MSDF-шрифт «как в expensive» (2026-09-06, «делай как в expensive»), DLL собрана 21:36 (851968 байт, 25 классов, round-trip OK, invokedynamic нет) — ЖДЁТ ИНЖЕКТА:**
- **`utils/render/MsdfFont.java` (новый):** static-блок парсит JSON рукописным `JsonParser` (inner-класс, только схема msdf-atlas-gen: atlas/metrics/glyphs; generic skipValue) → GLYPH HashMap<code, float[minU,minV,maxU,maxV,widthEm,heightEm,topEm,advanceEm]>; UV-конвертация формулой expensive MsdfGlyph: minV=1-top/H, maxV=1-bottom/H. PNG грузится С ДИСКА: `MsdfFont.FONT_DIR = C:\Users\Admin\Desktop\rustme\jni\agent\fonts\msdf\`, `FONT_BASE=sf_semibold` (Files.readAllBytes → ImageIO.read → getRGB → ByteBuffer RGBA; RGB MSDF-каналы КАК ЕСТЬ, НЕ tint, A=255 — alpha даёт шейдер). Payload ресурсов НЕ поддерживает (только классы) → диск; замена шрифта = заменить 2 файла без пересборки. Текстура: LINEAR/LINEAR + CLAMP (expensive: «to correct msdf anti-aliasing set MIN/MAG to GL_LINEAR»), биндинг+program save/restore. КРИТИЧНЫЙ БАГ (поймал до сборки): глифы парсились в static-init ДО присвоения ATLAS_WIDTH (поля final, присвоение после всего try) → UV все нули; ФИКС: атлас-поля НЕ final, присваиваются сразу в ветке "atlas" (в JSON atlas идёт раньше glyphs).
- **`Shaders.MSDF`** = порт expensive fragment.fsh на #version 120: `dist = median(texture2D.rgb)-0.5+thickness; alpha = smoothstep(-smoothness, smoothness, dist*pxRange)`. UV шейдер считает из gl_FragCoord по uniform-кваду глифа: `p.y = fbHeight - p.y; uv = uvRect.xy + (p-quad.xy)/quad.zw*(uvRect.zw-uvRect.xy)`. ГАТЧ: GLSL 120 НЕ знает textureSize/dFdx (130+/расширения) → dFdx(TexCoord)*textureSize заменены CPU: `pxRange = DISTANCE_RANGE * qw/(uvW*ATLAS_WIDTH)` = range в ЭКРАННЫХ px (эквивалент оригинала, математика сверена на 'A': adv 0.668, plane -0.15..0.819em).
- **`CustomFont.java` переписан под MSDF:** геометрия 1:1 из MsdfGlyph.apply — `gy = baseline - topPosition*size - 1` (тот «-1» px из expensive сохранён), шаг пера `width*(size-1)`, у пробела `+advance*size` (пробел=глиф 32 без planeBounds, quad скипается по qw<0.5). Контракт drawString: y = ЛЕВЫЙ ВЕРХ строки, baseline = y + ASCENDER*size (ascender 0.952 метрик). На каждый глиф: uniforms quad/uvRect/pxRange + glBegin/glEnd QUADS (scaled вершины). smoothness=0.5, thickness=0 константы. Тень = второй проход чёрным alpha*0.55 offset 1px. GL-state wrapper: save/restore GL_CURRENT_PROGRAM(0x8B8D)→glUseProgram(0), GL_ACTIVE_TEXTURE(0x84E0)→GL_TEXTURE0(0x84C0), GL_TEXTURE_BINDING_2D(0x8069).
- **ShaderUtil:** добавлен `programId()` (для save/restore). **CheatHud:** вызовы совместимы (int FONT_H=10 → float em size — widening; FONT_H теперь = size в expensive, их HUD-текст 7-10); kerning-пар в JSON нет — не используются.
- **Проверка после инжекта (лог C:\Logs\noslow_agent.log):** `MsdfFont: atlas loaded: 776x776 range=20.0 glyphs=160 tex=NN` → `Font: msdf font ready: atlas 776x776 ... lineHeight=1.1933594 ...` → `first frame DRAWN (font=custom)`. Если атласа нет: `atlas png load failed` + fallback на игровой шрифт. Крутилки: FONT_H (CheatHud), smoothness/thickness (CustomFont.drawString), замена шрифта — файлы в fonts/msdf/ (sf_medium как запасной).

**MSDF v48 ИНЖЕКТ (21:36) — РЕЗУЛЬТАТ И КОРНЕВАЯ ПРИЧИНА ИСКАЖЕНИЯ + ФИКС v49 (21:52, DLL 851968 байт, 25 классов round-trip OK, invokedynamic нет) — ЖДЁТ ИНЖЕКТА:** инжект подтвердил MSDF активен (лог: `MsdfFont: atlas loaded: 776x776 range=20.0 glyphs=160 tex=72` → `msdf font ready` → `first frame DRAWN (font=custom)`), но юзер: «шрифт как то искажается, пиксельный и неровный». **ДИАГНОЗ** (сверился с исходником их шейдера `expensive/util/visual/shader/main/shaders/FontGlsl.java`): у expensive UV — varying gl_TexCoord[0], GL интерполирует его по РЕАЛЬНО растеризуемому кваду → матрица не важна. У нас UV из gl_FragCoord по uniform-кваду, а квад считался как scaled×guiScale — ПРЕДПОЛОЖЕНИЕ, что матрица Overlay ровно ×guiScale без сдвигов. Мод оставляет свои матричные трансформации в стеке → uniform-квад ≠ реальный экран-квад → UV-выборка съезжает в разных частях глифа → медиана MSDF даёт мусорные расстояния → рваные/пиксельные края. Фон-скругление от этой ошибки НЕ страдает (форма вычисляется из uniform, текстуре не нужна) — поэтому баг маскировался как «шрифт виноват». Их drawText (expensive/util/visual/main/fonts/Font.java): EdgeStrength=0.5 (=наш smoothness), Thickness=0, attach/detach шейдера — наши uniform-значения совпадают. **ФИКС v49:** перед отрисовкой строки glGetFloat(GL_MODELVIEW_MATRIX=0x0BA6, buf) + GL_PROJECTION_MATRIX(0x0BA7, buf) (верифицированы по дампу lwjglx GL11: glGetFloat(I)F + glGetFloat(I,FloatBuffer)V; стаб пополнен, game_stubs.jar пересобран), углы квада проецируются САМИ: scaled → MV → PR → NDC (column-major 4x4, деление на w с guard) → физ.px (вьюпорт из ортопроекции: W=2/P00, H=2/P11 — P[0]=PR.get(0), P[5]=PR.get(5)); физ.квад → uniform quad + ТЕ ЖЕ scaled вершины в glBegin (растеризуются той же матрицей) → UV-маппинг совпадает при ЛЮБОЙ матрице мода. pxRange от фактического qw. Прочее: scale/GuiScale.get в drawString больше не нужен; walkString(s,x,baseline,size,fbHeight); MV/PR — статические direct FloatBuffer(64) (ByteBuffer.allocateDirect(64).order(nativeOrder).asFloatBuffer()); GameContext.fbHeight остаётся для переворота Y в шейдере. ТОЧКИ ДАЛЬНЕЙШЕЙ ДИАГНОСТИКИ, если юзер снова скажет «искажается»: дамп фактических MV/PR в лог (сравнить с ожидаемой ×2), проверить в мире vs в меню; спросить характер (мыло/рвано/съехало).

**ПЕРЕСМОТР ЭМПИРИКИ «texcoord не доходит» (2026-09-06, юзер спросил «а у нас чтоль нету gl_TexCoord?» — жёсткий вывод п.8/ZNANIA 12.3 п.8 ВЕРОЯТНО ПОСПЕШНЫЙ):** gl_TexCoord[0] — built-in varying GLSL 120, контекст форка compatibility (наш glBegin работает — в core его бы не было), т.е. в шейдере он ДОСТУПЕН; вопрос в том, чем наполняется. ДОКАЗАТЕЛЬСТВО, что texcoord на этом же форке доходят: FontGlsl expensive использует РОВНО gl_TexCoord[0].st в texture2D — их шрифт работает (но они рисуют через Tessellator/vertex arrays/glTexCoordPointer, а не immediate mode). ГИПОТЕЗА старой эмпирики (v29-32): immediate-mode glTexCoord2f пишет координату в ТЕКУЩИЙ активный texture unit (glActiveTexture), а gl_TexCoord[0] шейдера наполняется от юнита 0; мод перед нашим рендером рисует свой MSDF-шрифт → активный юнит почти наверняка ≠ 0 → координаты уезжали в gl_TexCoord[1], шейдер читал пустой [0] → вывод «не доходит». КЛЮЧЕВОЕ: текущая версия уже форсит glActiveTexture(GL_TEXTURE0) в state-wrapper (фикс v48), т.е. условие ломки теперь под контролем. ПЛАН (ждёт «делай» юзера): переключить MSDF-путь на схему expensive 1:1 — шейдер сэмплирует gl_TexCoord[0].st, Java подаёт UV через lwjglx glTexCoord2f(u,v) перед каждой вершиной; это СУЩЕСТВЕННО проще и надёжнее матричного хака v49 (никаких проекций MV×PR, uniform-квадов и зависимости от матриц, оставленных модом — GL сам интерполирует UV по реальному кваду). Матричная версия v49 (DLL 21:52) — fallback. ПЛАН ИСПОЛНЕН как v50 (см. ниже).

**v50 = ПЕРЕХОД НА gl_TexCoord (2026-09-06, юзер: «может мы что то сделали просто не так, давай попробуем еще раз сделать шрифт через gl_TexCoord и посмотрим что будет») + ФИКС ПРОЗРАЧНОСТИ ПЛАШКИ, DLL собрана 22:03 (851968 байт, 25 классов, round-trip OK, invokedynamic нет) — ЖДЁТ ИНЖЕКТА:** (1) сначала найден БАГ v49, объясняющий «текст исчез» (лог показывал MSDF активен, tex=79): в toNdcY взято PR.get(9) (z-строка) вместо PR.get(5) → NDC.y = константа → qh=0 → условие qh>=0.5 отбрасывало ВСЕ глифы. (2) Прозрачность плашки ArrayList пропадала «через время»: наш текстовый рендер в конце делал glDisable(GL_BLEND); у игры GlStateManager КЭШИРУЕТ GL-состояние (после модового HUD blend «включён» в кэше) и повторно glEnable не вызывает → кэш разъехался с реальностью → полупрозрачные элементы рисуются непрозрачно. ФИКС: после отрисовки blend НЕ выключать — оставить включённым + стандартная blendFunc (GL_SRC_ALPHA/GL_ONE_MINUS_SRC_ALPHA), состояние совпадает с кэшем; если прозрачность поплывёт снова — проверить все пути рендера, где в конце стоит glDisable(GL_BLEND). (3) Схема expensive 1:1: в Shaders.VERT добавлена строка `gl_TexCoord[0] = gl_MultiTexCoord0;` — ОБЯЗАТЕЛЬНО: при наличии кастомного вершинного шейдера built-in gl_TexCoord[0] сам НЕ заполняется (автоматика только без VS; без прокидки texcoord «правда не доходит» — вероятно вторая часть старой эмпиры v29-32); Shaders.MSDF упрощён до FontGlsl-expensive: сэмпл gl_TexCoord[0].st → median → smoothstep, НЕТ uniform-квадов/матриц/fbHeight; CustomFont переписан: UV через glTexCoord2f перед КАЖДОЙ вершиной (маппинг как в MsdfGlyph.apply), юнит форсится GL_TEXTURE0, pxRange = DISTANCE_RANGE * (size*guiScale/64f) — атлас size=64 px/em → экран-px на атлас-px = size*guiScale/64; скип глифа по w<0.25 (scaled). (4) ГАТЧ сборки: папка jni/agent/build может быть занята другим процессом (WinError 32 при rmtree в pack_agent) — retry после sleep 2 срабатывает. Проверка инжекта: текст появился+гладкий, плашка держит прозрачность; если текста снова нет — вернуться на матричную v49 с исправленным PR.get(5) (баг уже понятен, одна опечатка).

**v50 ИНЖЕКТ (22:03) + ДИАГНОЗ ПО СКРИНУ + v51 = ФИКС GL_ALPHA_TEST (2026-09-06, DLL 22:11, 25 классов round-trip OK, invokedynamic нет) — ЖДЁТ ИНЖЕКТА:** юзер прислал скрин плашки «AutoSprint»; зум ×8 (PIL NEAREST, сохранён в tools/font_zoom.png) показал: ФОРМЫ БУКВ ПРАВИЛЬНЫЕ, геометрия чистая → **путь gl_TexCoord РАБОТАЕТ через immediate mode lwjglx** (старая эмпира «texcoord не доходит» полностью снята: реальными причинами были активный texture unit ≠ 0 и отсутствие прокидки gl_TexCoord[0]=gl_MultiTexCoord0 в кастомном VS). Но альфа БИНАРНАЯ: на краях букв ни одного серого пикселя, чистые белое/чёрное ступеньки, тень 0.55 выглядит сплошной чёрной. MSDF-шейдер даёт градиент ~1px → его режет **GL_ALPHA_TEST**: мод перед нашим рендером рисует свой текст и оставляет alpha-тест включённым (у рендереров текста порог обычно 0.5) → всё прозрачнее порога отбрасывается, гладкий градиент обрезается в бинарные ступеньки. **ФИКС v51:** в CustomFont.drawString перед try: `boolean prevAlphaTest = GL11.glIsEnabled(GL_ALPHA_TEST=0xBC0); if (prevAlphaTest) glDisable(...)`; в finally `if (prevAlphaTest) glEnable(...)` — restore прежнего состояния, чужое не задевает. glIsEnabled(I)Z добавлен в стаб GL11 (верифицирован по дампу) + game_stubs.jar пересобран. Крутилки качества после подтверждения: smoothness=0.5 (больше = мягче край), thickness=0 (+=жирнее) в CustomFont.drawString. Отладочный артефакт: tools/font_zoom.png (зум скрина) — можно удалить.

**v51 ИНЖЕКТ (22:11) → «КВАДРАТИКИ» → ВЕРДИКТ ПО gl_TexCoord + v52 = ФИНАЛЬНЫЙ БИЛД (2026-09-06, DLL 22:19, 25 классов round-trip OK, invokedynamic нет) — ЖДЁТ ИНЖЕКТА:** юзер прислал скрин: сплошные белые блобы с чёрными блоками тени (зум tools/font_zoom2.png). Диагноз ОДНОЗНАЧНЫЙ: каждый квад глифа сэмплился в ОДНОЙ точке (uv-константа, попавшая внутрь глифа → MSDF alpha=1 → белый блоб). Это окончательно опровергает теорию про активный texture unit: **glTexCoord2f через immediate mode lwjglx НЕ доходит до GLSL-программы даже с форсированным GL_TEXTURE0 и прокидкой gl_TexCoord[0]=gl_MultiTexCoord0 в VS**. Путь gl_TexCoord ЗАКРЫТ, эмпира ZNANIA 12.3 п.8 ПОДТВЕРЖДЕНА (expensive работает потому, что рисует через Tessellator/glTexCoordPointer, не immediate). **v52 = возврат на рабочую схему gl_FragCoord+uniform-квад (v49), но впервые со ВСЕМИ ТРЕМЯ фиксами вместе:** (1) проекция квада через фактические MV×PR с ИСПРАВЛЕННЫМ багом v49: в toNdcY pr(5) (P[1][1]), НЕ pr(9) (z-строка) — тот самый «текст исчез»; (2) GL_ALPHA_TEST выключается на время отрисовки (гладкий градиент, фикс v51); (3) blend после нас НЕ выключается (прозрачность плашки, фикс v50). Shaders.VERT снова чистый ftransform (без прокидки), Shaders.MSDF снова с quad/uvRect/fbHeight/pxRange. Если v52 всё ещё не то — по характеру артефакта читать какой механизм сбоит: ступеньки=alpha-test вернулся, блобы=UV-константа (не должно), смещение=матрицы, мягкость крутить smoothness/thickness в CustomFont.drawString.

**v52 ИНЖЕКТ (22:19) → «всё равно блобы» + ПРОЗРАЧНОСТЬ СНОВА + v53 = откат на ×guiScale (DLL 22:27) + ДАМП МАТРИЦ РАСКРЫЛ ВСЁ + v54 = ФИНАЛЬНАЯ МАТРИЧНАЯ ПРОЕКЦИЯ (DLL 22:34, 25 классов round-trip OK, invokedynamic нет) — ЖДЁТ ИНЖЕКТА:** v52 (проекция MV×PR) дал те же блобы + юзер: «прозрачность плашки пропадает, текста нет». Лог молчал (MSDF активен). (1) **Прозрачность плашки — ВТОРОЙ источник того же десинка кэша GlStateManager: наш RenderUtil.drawRoundedRectShader в конце делал glDisable(GL_BLEND)** → фикс: blend после плашки тоже НЕ выключать (glEnable(TEXTURE_2D) оставить, glDisable(BLEND) убрать). Правило: НИ ОДИН наш рендер-путь не должен заканчиваться glDisable(GL_BLEND). (2) **Текст пропал в v52 без ошибок** — сначала ошибочно списано на «glGetFloat мёртв» (v53 откат на ×guiScale + одноразовый дамп MV/PR в лог). ДАМП ИЗ ЛОГА ЮЗЕРА РАСКРЫЛ ГЛАВНОЕ: `MV=[1.0, 0.90000004, 0.0, 1.05, 1.0] PR=[0.0020833334, -0.003933137, -1.0, 1.0, 1.0]` (mv(0),mv(5),mv(12),mv(13),mv(15) / pr(0),pr(5),pr(12),pr(13),pr(15)):
- **glGetFloat РАБОТАЕТ** (значения настоящие, не нули) — вердикт v52 «мёртв» НЕВЕРЕН, был отозван;
- **overlay-матрица мода НЕ identity**: MV = scale(1, 0.9) + translateY(1.05)! Поэтому v53 (×guiScale) дал блобы (uniform-квад ≠ реальный растеризованный), а v47/v48/v50 с identity совпадали случайно;
- **PR кодирует SCALED-размеры**: pr(0)=2/960 (=2/scaledWidth!) → физ.пиксели = (2/|pr|)×guiScale → vw=1920, vh=1018. Баги v52-проекции: vw/vh БЕЗ guiScale (квад вдвое меньше, UV сжаты 2:1) и vh=2/pr(5) ОТРИЦАТЕЛЬНА (pr(5)<0) → qh<0 → все глифы отброшены = «текст исчез» v49/52;
- длина строки текста при блобах НЕ меняет диагноз: квад сэмплился в одной точке из-за несовпадения квада с растеризацией.
**v54 ФИНАЛ:** walkString проецирует углы квада через фактические MV×PR: `vw=2/|pr(0)|×guiScale, vh=2/|pr(5)|×guiScale`; projX/projY: ex=mv(0)sx+mv(4)sy+mv(12); ey=mv(1)sx+mv(5)sy+mv(13); ew=mv(3)sx+mv(7)sy+mv(15); cx=pr(0)ex+pr(4)ey+pr(12)ew; cw=pr(3)ex+pr(7)ey+pr(15)ew; ndc=cx/cw; X=(ndc*0.5+0.5)*vw; Y=(1-(ndc*0.5+0.5))*vh. ЗНАКОВЫЙ ФИКС: (gy+h) это НИЗ квада (y вниз) — углы в проекции были перепутаны местами → qh отрицательный; итог: xa/ya=proj(gx,gy), xb/yb=proj(gx+w,gy+h), qx=min(xa,xb), qy=min(ya,yb), qw=|xb-xa|, qh=|yb-ya|. ЧИСЛОВАЯ ВЕРИФИКАЦИЯ на дампнутых матрицах: точка (877,4.63)→1754px = ровно ×2 ✓, глиф A → quad(1754,10.4,150?? нет — qw по w глифа, тестовый скрипт подставил кривую ширину, в коде верно). В drawString по-прежнему: alpha-test off (v51), blend остаётся включённым (v50), glUseProgram(0)+glActiveTexture(GL_TEXTURE0)+save/restore (v48). dumpMatricesOnce() оставлен (matLogged флаг, отдельный от logged; пишет proj(100,10) → px для сверки). Причина цепочки блобов в v52: НЕ «glGetFloat мёртв», а двойной баг vw/vh (без ×guiScale + знак) — исправлено в v54. Сборка: 22:34, 851968 байт. Артефакты: tools/font_zoom*.png (зумы скринов, можно удалить).

**v54 ИНЖЕКТ → ВСЁ ЕЩЁ КВАДРАТЫ, НО ПРОЕКЦИЯ В ИГРЕ ВЕРНА + v55 = ДИАГНОСТИЧЕСКИЙ ШЕЙДЕР (DLL 22:48):** после v54 юзер: «досих пор квадраты». ПРОВЕРКИ v54 в рантайме: (1) лог `Font: proj(100,10): ndc=(-0.7916666,0.960472) -> px(200.00003,996.89996) vw=1919.9999 vh=1016.99994` — X=100×2=200 ✓; Y top-down = 1017−996.9 = 20.1 = 10×2 ✓ — ПРОЕКЦИЯ МАТЕМАТИЧЕСКИ ВЕРНА в игре; (2) атлас PIL: pixel(0,0)=(98,0,0) НЕ белый → теория «uv-константа внутрь глифа» не подтвердилась; атлас валиден. **v55 = MSDF_DEBUG:** `gl_FragColor = vec4(median(texture2D.rgb), alpha, fract(uv.x*8.0), 1.0)` — R=сырой сэмпл, G=alpha, B=fract(uv.x*8) полосы; флаг CustomFont.DEBUG_SHADER.

**v55 ИНЖЕКТ → ДИАГНОСТИКА СРАБОТАЛА: КОНВЕЙЕР ПОЛНОСТЬЮ РАБОЧИЙ (2026-09-06, DLL v56 23:00, 25 классов round-trip OK, invokedynamic нет):** юзер прислал скрин debug-шейдера («синий и красный цвет вокруг букв») + юзер САМ получил расшифрованные шейдеры мода в `C:\Users\Admin\Desktop\rustme\assets` (полное assets-дерево мода, текстуры вырезаны юзером). РАСШИФРОВКА ДАМП-СКРИНА v55: красная кайма вокруг букв = R-канал = сырой MSDF-сэмпл с правильным градиентом (сэмплинг и UV-вариация работают), синие полосы = B-канал fract(uv.x*8) (UV плавно меняются — uniform-квад совпадает с растеризацией), G=alpha корректна. **ВЫВОД: шейдерный конвейер (сэмплинг+проекция+smoothstep) ПОЛНОСТЬЮ РАБОЧИЙ** — «квадраты» были только в финальном цвете.
- **РАСШИФРОВАННЫЙ ui_batch.fsh мода прочитан (ЭТАЛОН):** их MSDF: `screenPxRange = max(0.5 * dot(unitRange, screenTexSize), 1.0)` (unitRange=uPxRange/texSize, screenTexSize=1/fwidth(uv)) → `fill = clamp((sd-0.5)*screenPxRange+0.5, 0,1)`. Математика ЭКВИВАЛЕНТНА нашей (у них линейный clamp, у нас smoothstep), ключевое отличие = **min-кламп AA на 1px** (при любом масштабе сглаживание не схлопывается). Их VSH #version 130: projectionMatrix uniform, батчер шлёт material/outline в params. Батчер поддерживает KindTextured/KindMsdf/KindCircle/KindArc/KindTriangle (типы фигур в одном draw call через flat int passKind), uTextures[8] слоты.
- **v56 (DLL 23:00):** DEBUG_SHADER=false (обычный шейдер), AA-кламп перенят на CPU: `pxRange = Math.max(DISTANCE_RANGE * screenPxPerAtlasPx, 1f)`; одноразовый лог `Font: first glyph: quad=(...) uvRect=(...) pxRange=... loc[quad]=... loc[uvRect]=... loc[color]=... loc[pxRange]=... loc[atlas]=... binding=... tex=...` (метод logGlyphOnce, флаг glyphLogged; MsdfFont.texId() геттер добавлен) — если «квадраты» вернутся, лог сразу покажет локацию -1 или бинд не той текстуры. **ЖДЁТ ИНЖЕКТА:** юзер должен прислать скрин + строку `Font: first glyph: ...`.

**Реверс рендера текста МОДА (2026-09-06, юзер: «найдем как они рендерят почему у них все гладко»):** в дампе найдено: мод держит СВОИ MSDF-шрифты в `assets/minecraft/font/msdf/` (medium=их водяной, default, map_en, map_ru, mlrs, regular — png+json, ТОТ ЖЕ формат msdf-atlas-gen), загрузчик `rustme/IillIIIliI` (kotlinx.serialization Json + ImageIO, '/assets/minecraft/font/msdf/'+.json/.png, тег 'ms:'). **ЮЗЕР РАСШИФРОВАЛ шейдеры мода** — расшифрованное дерево лежит в `C:\Users\Admin\Desktop\rustme\assets` (текстуры вырезаны юзером ради веса): shaders/ui_batch.{vsh,fsh} (главный UI/текст-батчер, см. эталон выше), item_icon, sprite_renderer, scene, terrain, blur_*, zone_glow, armor + shaders/program/* (ванильные). Рендер текста мода = VBO-конвейер: класс с GL15+GL20+GL30 пишет вершины в ByteBuffer по 5 float (x,y,u,v,флаг). Классы с прямыми org/lwjgl/* ссылками ищутся проходом по CP всех dump/classes/minecraft/rustme/*.class (GL20-шейдерный= lIlIIIiliI(15), GLFW-классы lllIiilliI(1) и др.).

**РЕСУРСЫ В АГЕНТЕ (анализ дан юзеру, ждёт «делай»):** assets-папка в jar НЕ сработает (классы DefineClass-ные, без CodeSource; getResourceAsStream ищет по classpath, не по надефайненным классам). Рабочий путь — ресурс-как-класс: tools/embed_font.py → MsdfRes.java с Base64-строковыми константами (лимит UTF8 65535 байт → PNG ≈11 строк; Base64 вместо byte[]-литерала из-за 64КБ лимита метода), <clinit> декодирует java.util.Base64; pack_agent менять не надо; MsdfFont переключить Files.readAllBytes → MsdfRes. Отклонено: JNI-передача из DLL (RegisterNatives не восстановлен), URLClassLoader (всё равно диск).

**ПАМЯТКА ОТЛАДКИ ШРИФТА (хронология версий 21:36→23:07, все DLL собраны, 25 классов round-trip OK):** v48 MSDF «как expensive» ×guiScale → «искажения»; v49 матричная проекция, баг pr(9) → «текст исчез»; v50 gl_TexCoord+blend-on → «пиксельные ступеньки»; v51 alpha-test off → «квадратики» (glTexCoord2f не дошёл); v52 проекция исправленная, без ×guiScale и с отрицательным vh → «текста нет, прозрачность пропала»; v53 откат ×guiScale + дамп матриц → «квадраты» (мод оставил scale(1,0.9)); v54 проекция полная min/abs ×guiScale, верифицирована в игре → всё ещё квадраты; v55 MSDF_DEBUG → **конвейер полностью рабочий** (R=сырой MSDF-градиент, G=alpha верна, B=UV-полосы); v56 обычный шейдер+AA-кламп+лог loc[] → снова квадраты, НО лог first glyph идеален: quad=(1768,11.78,19.4,18.6) физ.пиксели, uvRect 'A' верен, pxRange=6.25, ВСЕ локации валидны (quad=4,uvRect=7,color=1,pxRange=3,atlas=0), binding=57=tex.

**КОРЕНЬ ВСЕХ БАГОВ НАЙДЕН И ПОДТВЕРЖДЁН (v57, 2026-09-06, DLL 23:07 — ЮЗЕР: «ураа теперь все работает» ✅): GL_BLEND=0xBE В СТАБЕ ВМЕСТО 0x0BE2!** В tools/compile_stubs/org/lwjglx/opengl/GL11.java стояло `GL_BLEND = 0xBE` (190) — потерян нолик в hex. glEnable(190) — неверный enum, OpenGL молча игнорирует → **блендинг не включался НИ ОДИН РАЗ во всех билдах**. Это единым махом объяснило ВСЕ симптомы саги: «квадраты» = RGB пишется в буфер как есть (главный проход белый (1,1,1) на весь квад → белые блобы, тень чёрная → чёрные со сдвигом); «пиксельность» v50/v51 = alpha-test резал непрошедшие пиксели, прошедшие рисовались непрозрачным белым → правильные формы с жёсткими ступеньками; debug-шейдер v55 «работал» = кодировал форму в сам RGB, блендинг ему не нужен; «прозрачность плашки пропадает» = плашка drawRoundedRectShader использует ТОТ ЖЕ битый GL11.GL_BLEND — она никогда не была полупрозрачной (тёмный цвет маскировал). ОСТАЛЬНЫЕ константы стаба перепроверены — верные (QUADS=7, TEXTURE_2D=0xDE1, SRC_ALPHA, RGBA и т.д.), битая только GL_BLEND. Фикс: `GL_BLEND = 0x0BE2`, game_stubs.jar + DLL пересобраны (23:07). ВАЖНО: правки стабов требуют пересборки game_stubs.jar, а глюк «WinError 32 папка build занята» лечится sleep 5 + retry build_dll.bat. Итоговая архитектура MSDF-шрифта (полностью рабочая): атласы expensive с диска (jni/agent/fonts/msdf/sf_semibold) → MsdfFont (JSON-парсер+PNG→GL, LINEAR) → Shaders.MSDF (gl_FragCoord+uniform quad/uvRect, pxRange с min-клампом 1px AA по образцу ui_batch.fsh мода) → CustomFont (проекция квада через фактические MV×PR: vw=2/|pr(0)|×guiScale, vh=2/|pr(5)|×guiScale, min/abs знаков; геометрия 1:1 MsdfGlyph.apply; alpha-test off на время отрисовки; blend НЕ выключать; program/unit/binding save/restore).

**ОТЛАДОЧНАЯ МЕТОДОЛОГИЯ, КОТОРАЯ СРАБОТАЛА (повторять):** (1) диагностический шейдер MSDF_DEBUG (R=сырой медианный сэмпл, G=вычисленная alpha, B=fract(uv.x*8)) разделяет сэмплинг/smoothstep/uniform-путь по ОДНОМУ скрину; (2) одноразовый logGlyphOnce (quad/uvRect/pxRange/loc[]/binding/tex) исключает uniform-путь целиком; (3) зум скрина PIL NEAREST ×8 показывает природу артефакта (ступеньки=alpha-test, блобы=нет блендинга, кайма=градиент жив); (4) числовой верифай проекции на дампнутых матрицах до сборки; (5) когда всё кроме финального цвета работает — подозревать GL-константы (проверять hex-значения стаба по спецификации GL!).

**ВАТЕРМАРКА rockstar-стиль РЕАЛИЗОВАНА (v59, DLL 23:30 2026-09-06, 26 классов round-trip OK, invokedynamic нет) — ЖДЁТ ИНЖЕКТА/СКРИНА:** юзер сменил план (изначально хотел ESP первым): «давай скопировать ватермарку из rockstar, текст „ваня стирается", визуал 1:1, БЕЗ анимаций айленда и без ивентов (PvP-статусы)». Новый `utils/render/Watermark.render(scaledWidth)`, вызывается из CheatHud.renderFrame ДО проверки count==0 (независима от модулей). Константы вычитаны 1:1 из rockstar-client-src:
- позиция: верх-ЦЕНТР, y=7 (IiIiiIIII_Class177.iI_method_99c2bcf2: 7.0F в игре, x = screenWidth/2 − w/2);
- айленд: h=15, radius=7 (IiIiiIiii_Class184.size = IiIiiIiiI_Class183(48,15,7)); фон `drawSquircle(x,y,w,h, 2+5*anim, palette[1]=(24,21,29), alpha 216.75/255≈0.85)`; без анимаций радиус фикс 7;
- внешняя подсветка: drawSquircle(x−1,y−1,w+2,h+2, тот же r, палитра[3]=(61,54,71,63.75), alpha 25.5/255=10%);
- паддинги потока IIII(0,5,0,4): I_field=1й=top, Ii_field=4й=left → top=0, right=5, bottom=0, left=4;
- градиент-слой (Class190): drawSquircle(x, y, w×0.69, h, r=2, акцент alpha 71.4/255≈28% сверху → 0 снизу), акцент = палитра[0] = **#906BFF (144,107,255)**;
- лого: drawIcon("logo", 8×8, tint акцент) — ЗАМЕНЕНО скруглённым квадратом 8×8 r=2.5 акцентного цвета (их PNG недоступен нашему рендеру; можно добавить загрузкой PNG как атласа);
- текст: font medium 7px белый (палитра[5]) → наш CustomFont.drawString size 7, белый, без тени, верт. центрирование по cellHeight;
- ширина потока = 4 + 8(лого) + 6(gap) + textW + 5;
- тема мода IiiiiIIIi_Class242.I_field_c75745ca: [0]=#906BFF, [1]=(24,21,29,229.5), [2]=(24,21,29,102), [3]=(61,54,71,63.75), [4]=(77,0,255), [5]=white, [6]=(26,23,31), [7]=(5,4,7); базовый акцент iiI_field=(144,107,255); геттеры: i_method_5dfec6e=[0] акцент, iI_method_8e08d3b1=[5] белый; палитра перекрашивается настройкой юзера (I_method_300b2840), у нас захардкожен дефолт.
Все слои рисуются RenderUtil.drawRoundedRectShader (4-цветный градиент по углам уже поддержан). Лог: `Watermark: drawn: island(x,y)+[Wx15] textW=...`. Крутилки: текст (1я строка render), 0.69f (ширина градиента), a=71 (яркость акцента), TOP_Y=7.

**v60 ИНЖЕКТ → ЮЗЕР УТОЧНИЛ (v61 23:38, v62 ФИНАЛ 23:44 2026-09-06, invokedynamic нет) — ШРИФТ ЮЗЕР ПОДТВЕРДИЛ («ураа теперь все работает»), ватермарка с градиентом ждёт скрина:** v60 убрал лого+градиент целиком; юзер поправил: «градиент у них ЕСТЬ (я сам видел в коде), просто у нас он НЕ ЗАТУХАЕТ вправо, а обрезается». КОРЕНЬ: их порядок вершинных цветов в iIiiIIiii_Class360.I_method_43fbeb57 = **arg1→TL, arg2→BL, arg3→BR, arg4→TR** (проверено по BufferBuilder-эмиссии), их вызов (accent28%, accent28%, 0, 0) = ЛЕВЫЙ край яркий → горизонтальный fade вправо. Наш drawRoundedRectShader смешивает по-другому: top=mix(c1,c2,uv.x), bottom=mix(c3,c4,uv.x), затем mix по uv.y → углы (c1=TL,c2=TR,c3=BL,c4=BR); я передавал (accent,accent,0,0) = вертикальный фейд (незаметен на полосе 15px, выглядит сплошной заливкой). **ФИКС v62:** передавать c1=c3=accent@28%, c2=c4=0 → горизонтальное затухание 1:1 с rockstar. Слой: w×0.69, r=2, alpha 71/255. Лого-квад остаётся убранным, капсула/подсветка/текст как в v60. (Баг сборки v61: при удалении лото-квада случайно удалил константу ACCENT → javac «cannot find symbol» — вернуть.) Одиночная компиляция javac файла даёт ложные ошибки «package utils.etc does not exist» — проверять ошибки только через полный pack_agent.

**ШРИФТ ПЕРЕКЛЮЧЁН НА ИХ MEDIUM (v63, DLL 23:55 2026-09-06, invokedynamic нет) — ЖДЁТ ИНЖЕКТА:** юзер отменил лого-иконку («иконку делать не надо»), попросил шрифт «как у них» в watermark. Сделано: `assets/minecraft/font/msdf/medium.{png,json}` скопированы в `jni/agent/fonts/msdf/`, `FONT_BASE="medium"`. Их атлас: 512×512, **type=msdf, distanceRange=12, size=32**, **yOrigin=top** (vs sf_semibold mtsdf/range20/size64/bottom), 262 глифа, кириллица полная, метрики инвертированы (asc=−0.928, desc=+0.244). **MsdfFont теперь поддерживает ОБА yOrigin:** поле `yOriginTop` парсится из atlas.yOrigin; метрики нормализуются к нашей системе (y вниз): `ASCENDER=−asc, DESCENDER=−desc` при top-origin (обе >0); UV при top-origin: `minV=top/H, maxV=bottom/H` (строки PNG напрямую), при bottom — старая формула expensive; planeBounds при top-origin: `topEm=−bounds.top, hEm=bounds.top−bounds.bottom` (всегда >0). ЧИСЛЕННО ВЕРИФИЦИРОВАНО PIL-сэмплами: у 'в' atlasBounds.top=456.5 — действительно ВЕРХ глифа в PNG (не перевёрнут), row 427 = R=255 (снаружи), row 455 = G=255 (внутри чаши) — маппинг подтверждён. Порядок ключей JSON medium (atlas,metrics,glyphs,kerning) совпадает с парсером. pxRange=12 подхватился из JSON автоматически. Приоритет вершин в шейдере: 1-я вершина квада (верх-лево) получает (minU,minV) — при top-origin это верх глифа ✓. Откат на expensive: FONT_BASE="sf_semibold".

**КРУТИЛКИ ГОТОВОГО ШРИФТА (предложены юзеру после успеха):** толще/тоньше текст = `thickness` в CustomFont.drawString (сейчас 0); мягче края = `smoothness` (сейчас 0.5); другой вес шрифта = положить sf_medium/sf_bold png+json в jni/agent/fonts/msdf/ и поменять `FONT_BASE` в MsdfFont; обводка — в ui_batch.fsh мода есть (passOutlinePx/passOutlineColor, outlineW = min(passOutlinePx, max(0.5*screenPxRange-0.5,0)), mix(outlineColor, color, fill)) — добавить по образцу если юзер попросит. ZNANIA.md НЕ обновлялся (ждёт явного «зафиксируй»).

**ПОСЛЕДСТВИЕ ВКЛЮЧЁННОГО БЛЕНДИНГА (v58, DLL 23:14, юзер: «персонаж голубой стал») — ЖДЁТ ИНЖЕКТА:** когда блендинг наконец заработал, вылезла ранее маскировавшаяся утечка: RenderUtil.drawRect рисует акцентную полоску ArrayList цветом ACCENT_COLOR=0xFF00C8FF (голубой) через glColor4ub и ОСТАВЛЯЕТ текущий цвет GL голубым до конца кадра; GlStateManager игры КЭШИРУЕТ цвет на Java-стороне — его glColor(1,1,1,1) пропускается как «уже белый» → сущности рендерятся застрявшим голубым. ФИКС: drawRect и drawGradientRectV после glEnd делают glColor4f(1,1,1,1) (нейтральный сброс); шейдерный фон уже ставил белый glColor4f(1,1,1,1), шрифт glColor не трогает — после нашего HUD цвет всегда нейтральный. ОБЩЕЕ ПРАВИЛО (добавить к «blend НЕ выключать»): после своих fixed-pipeline квадов сбрасывать glColor в белый. Тех-глюк сборки: папка jni/agent/build была заблокирована собственной shell-сессией (cwd внутри build держит WinError 32; sleep/retry НЕ помог) — лечение: os.rename(build, build_locked) → сборка в свежую → rmtree(locked). Также DLL не пересобирается пока игра запущена (LNK1168 не открыть dll для записи).

**ЗАПРЕТЫ (все причины):** лямбды/::/Proxy = invokedynamic → LambdaMetafactory → plain-класс мимо protected-конвейера → «Corrupted classfile»/тихая смерть. Только анонимные классы. Заливка lightmap (int[256] белым) МИГАЕТ — мод пересчитывает каждый тик: крутить ВХОД (гамму), не выход.

**FullBright v44 РАБОТАЕТ (подтверждено юзером, 2026-09-06):** гамма мода ScreenSettingsCategory.getGamma() → FloatNode; OptionNode.setValue(Object) при tempState=false пишет storedValue БЕЗ клампа validateValue. applyGamma(100.0f) onEnable, restore onDisable (default сохранён). Resolve: ctx.settingsObj (заполняется в resolveSprintKey) → getData() → getScreenSettings() → getGamma(). Настоящий updateLightmap = lIliIiiIiI.IiIIllIiII(F) читает гамму каждый тик. ⚠️ ОБНОВЛЕНИЕ v74 (2026-09-07): gamma=100.0 РАБОТАЕТ, НО ПЕРЕПОЛНЯЕТ lightmap (формула уходит в минус → R/G-каналы переполняются → мир/персонаж СИНИЕ). FULLBRIGHT_GAMMA 100.0f → 10.0f (полный свет уже на ~10). Урок: оживший после чужих фиксов модуль даёт новые побочки от своих экстремальных констант.

**ArrayList v46 = ПОРТ 1:1 celestial recode FeatureList.RenderArray (правая ветка, blur=false), фикс v45:** v45 ошибки: (1) alpha плашки умножал на анимацию модуля — в celestial там БОСОЕ поле this.animYto самой FeatureList (мастер-анимация всего списка, после въезда=255, плашка НЕПРОЗРАЧНАЯ и НАКРЫВАЕТ glow — это и видел юзер как «плашка без прозрачности»); (2) glow-бокс рисовал ровно по бару — в celestial тень = drawBlurredShadow(R-w-6, y-1.0, w+9, 10, blur10, color@150), т.е. бокс ШИРЕ бара и СДВИНУТ вверх, halo торчит сверху всегда. Точная геометрия (offset=0.8=OffsetY 0.7+0.1, R=правый край): тень (R-w-6, y-1, w+9, 10) @alpha150 const; плашка (R-w-6.5, y+1.5, R, y+10) @alpha=master*255; полоска (R-1, y+0.3, R+1.5, y+10) белая непрозрачная; шаг y += 9.2*animМодуля*0.8=7.36 < высота плашки 8.5 — нижняя СТРОКА наезжает на верхнюю КАК В ИСХОДНИКЕ (это фича); текст отдельным проходом (R-w-3.5, y+1.25) белый -1 без тени. Анимации модулей двигают ТОЛЬКО позиции строк, не альфу. Our drawBlurredShadow порт = GL-шейдер (Shaders.GLOW: dist до бокса, спад за blur физ.пикселей, квад расширен на blur) вместо CPU GaussianFilter. mntsb_13 celestial = TTF 13px, getFontHeight=(h-8)/2≈9 — наш FontRenderer.FONT_HEIGHT=9 совпадает.

**ArrayList v45 = стиль celestial recode (порт из C:\Users\Admin\Desktop\rustme\celestial recode, их FeatureList.java под 1.21 переписан под наш 1.12.2-агент):** построчные бары БЕЗ общего фона — каждый бар по ширине текста (textW+6.5), высота 9.2, шаг 10; белая полоска 2.5px у правого края (y-0.5..y+9.2); glow-тень под каждым баром = НОВЫЙ GLOW-шейдер (Shaders.GLOW: alpha=1 внутри rect, линейный falloff за blur физ.пикселей; квад рисуется расширенным на blur, uniform rect=сам бар) — замена их drawBlurredShadow(GaussianFilter). Цвет = ClientHelper.getClientColor порт: режим Fade (дефолт celestial) = TwoColoreffect(ONE=0xFF9E0DEF(158,13,239), TWO=darker=0xFF6E09A7), фаза = |ms/11|/100 + 3*(y/10)/60 — осцилляция sin(π*6*thing). Также портированы astolfo/rainbowCol (hsbToRgb без java.awt) — переключается константой MODE в CheatHud. Сортировка по УБЫВАНИЮ ширины (вставками, без Comparator-лямбд). Анимация = celestial AnimationHelper.Move(from,to,6.5dt,6.5dt,dt) — линейная с миншагом, НЕ exp-сглаживание. Текст рисуется ОТДЕЛЬНЫМ вторым проходом поверх всех баров (как в celestial). Текст right-aligned (rightEdge-4-width).

**Карты классов форка (дизасм-проверены):** Minecraft=lIliIiiIiI (урезан; updateLightmap=IiIIllIiII(F) внутри, lightmap=int[256] поле IIiiiiiiI; lightTex/lightMap — ключи настроек/профайлера), GameSettings=iilliIliiI (СЛИТЫЙ: +Runnable(главный цикл run())+IThreadListener), ScaledResolution=liIIiIliiI, GuiIngame=liIIliliiI, GuiScreen=IlIlliliiI, Gui=iIlililiiI (drawRect=lllIIliliI инстанс-стайл? НЕТ — статик 5-арг lIilIliliI(IIIII)V = vanilla drawRect(left,top,right,bottom,color)), FontRenderer=lilililiiI, KeyBinding=IilIiIiIiI (pressed=iilIIiIiI:Z), клавиши=lllIiilliI.iiililIIIl(I)Z (glfwGetKey), мод-шина=lIllIilliI (static HashMap; Consumer-метод=unsubscribe!), GL-утил=IiilIiiIiI (150 статик), fog=llilIiiIiI (2 AbstractTexture-поля, NOT LightTexture).

**Инструменты:** tools/disasm_range.py (полный дизасм с диапазоном), tools/disk_name_map.json (файл дампа → настоящее имя класса; (N)=Windows case-коллизии), game_cp.jar/game_stubs.jar.

**ЛОГО-ТЕКСТУРА + ИХ ШРИФТ MEDIUM (2026-09-06, юзер: «мы не можем лого как то текстуру сделать? А также нельзя сделать шрифт как у них? Просто вопрос» — анализ дан + ВЕРИФИКАЦИЯ проведена, ждёт «делай»):** (1) **Шрифт medium — тривиально и ПРОВЕРЕНО**: в расшифрованных ассетах `assets/minecraft/font/msdf/` лежат запечённые атласы того же формата msdf-atlas-gen, что читает наш MsdfFont: medium.png+json (их водяной), regular, mlrs, default, map_en, map_ru. medium.json верифицирован: 512x512, type=**msdf** (не mtsdf), distanceRange=**12**, size=**32** px/em, 262 глифа, **кириллица полная (66) — для «ваня стирается» не хватает НИ ОДНОЙ буквы**. ГАТЧ: метрики с ИНВЕРТИРОВАННЫМ знаком vs expensive (asc=**-0.9277**, desc=**+0.2441**) — при переключении FONT_BASE=medium поправить 2 знака в UV-формуле MsdfFont (minV=1-top/H, dy от baseline), иначе глифы вверх ногами. (2) **Лого-текстура**: их лого — **SVG** `rockstar-client-src/src/main/resources/assets/rockstar/icons/logo.svg` (10x10, нужен конверт в PNG; drawIcon мода резолвит 'icons/'+name+'.png' и рендерит в кэш; в icons/ лежат и готовые PNG: add, arrow, language). У нас: загрузка = копия MsdfFont.ensureLoaded (PNG→glTexImage2D RGBA с альфой, LINEAR, CLAMP), рисование = **fixed-pipeline текстурированный квад** (bind + glTexCoord2f + glBegin — texcoord в FIXED pipeline работает, не доходит только в GLSL-шейдер) либо отточенный gl_FragCoord-паттерн; тинт акцентом #906BFF через glColor (если PNG белый).

**ШРИФТЫ ГРУЗЯТСЯ С ДИСКА (подтверждено юзеру 2026-09-06, конец сессии):** `MsdfFont.FONT_DIR = C:\Users\Admin\Desktop\rustme\jni\agent\fonts\msdf\` хардкод, Files.readAllBytes sf_semibold.png+json при первом кадре. Плюс: замена шрифта без пересборки DLL. Минус: на другом компе без этой папки — молча fallback на игровой шрифт (лог: `atlas png load failed`). СОГЛАСОВАННЫЙ ПЛАН на следующий заход (переход к их medium + лого): tools/embed_font.py — генерит MsdfRes.java с Base64-константами (~660КБ), атласы в payload, диск не нужен; pack_agent менять не нужно (компилит все .java рекурсивно); MsdfFont переключается с Files.readAllBytes на MsdfRes. Делать одним куском с лого-текстурой и medium.

**v64 = ДВА ШРИФТА + ФИКСЫ ПО СКРИНУ v63 (2026-09-07, DLL 00:03, invokedynamic нет) — ЖДЁТ ИНЖЕКТА.** Претензии юзера к v63 и фиксы: (1) «ватермарку пониже» → TOP_Y 7→20; (2) «в arraylist верни тот же шрифт» → MsdfFont ПЕРЕПИСАН из синглтона в ЭКЗЕМПЛЯРЫ: `MsdfFont.get(fontBase)` с CACHE, все поля/методы инстансовые; CustomFont держит `HUD_FONT=MsdfFont.get("sf_semibold")` (ArrayList — прежний шрифт и шаг букв) и `WM_FONT=MsdfFont.get("medium")` (ватермарка); публичные методы CustomFont получили перегрузки с MsdfFont-параметром (getWidth/drawString/cellHeight; короткие версии = HUD_FONT). Шейдер один общий. (3) «расстояние между буквами слишком большое» → КОРЕНЬ: шаг пера expensive-трюком width*(size-1) у medium ДАЁТ 4.69px при настоящем advance 3.5px (ink 0.78em > advance 0.5em!) → для medium режим `properAdvance` (=fontBase.startsWith("medium")): перо = advance*size + 0.025*size трекинг (их I_method_e71fc7e2) + учёт left bearing (у medium −0.128em: квад от `pen + g[8]*size`, без него буквы дополнительно разъезжаются). Глиф-массив РАСШИРЕН до 9: [...,advanceEm,leftEm] (индекс 8=leftEm). sf_* остался на старом expensive-режиме (ArrayList не менялся). (4) «градиент не обрезается вместе с ватермаркой» + «не похоже» → СВЕРКА С ИХ КОДОМ ЕЩЁ РАЗ: **в покое айленд radius=2** (IiIiiIIII_Class177: `2+5*anim`, anim=0), radius 7 — только РАЗДУТОЕ состояние; у нас стоял 7 → градиентный слой r=2 торчал из-под углов капсулы. ФИКС: ISLAND_R=2 (и капсула, и градиент-слой, и glow). Watermark теперь: y=20, r=2, medium-шрифт proper advance, текст «ваня стирается». Глюк сборки: при переписывании Watermark потерялся хелпер GameCtx() (3 вызова) — заменён на прямой GameContext.get(). Инжект: юзер должен прислать скрин.

**v64 ИНЖЕКТ → мелкие правки (v65, DLL 00:06 2026-09-07) → v66 (DLL 00:21) → v67 = ФИНАЛ с часами (DLL 00:26 2026-09-07, invokedynamic нет) — ЖДЁТ ИНЖЕКТА.** v65: юзер «сделай размер шрифт в ArrayList поменьше» → CheatHud `FONT_H = 10 → 8` (em size scaled; плашки сами сжимаются по ширине — blockW от фактической ширины строк). v66 — три претензии юзера к ватермарке и фиксы: (1) «сглаживания углов как в rockstar нет» → **radius 2 → 7**: устоявшийся айленд у них = ПОЛНАЯ капсула r=7 (их покой=7, из IiIiiIiii_Class184.size(48,15,7); r=2 — только переходный момент `2+5*anim` при anim=1); (2) «рисовалось время как в watermark rockstar» → часы слева от острова, приглушённый серый #B7B3BD, зазор CLOCK_GAP=12px от капсулы, весь ряд (часы+остров) центрирован по экрану; (3) «чуть пониже» → TOP_Y 20→26. **v67 (00:26) — формат часов исправлен по исходникам:** юзер «нет, ноль должен быть, сделай как в исходниках» → найдено в `iIIIIiIiI_Class267.java`: `SimpleDateFormat("HH:mm", Locale.getDefault())` → **24-часовой формат С ведущим нулём у обеих частей** («09:05», «14:07»); clockText() = Calendar.HOUR_OF_DAY + паддинг h и m. Итог v67: часы HH:mm серым слева → [капсула h=15 r=7: градиент-слой 69% (горизонт. fade accent #906BFF@28%→0) + текст medium 7px «ваня стирается»], y=26. ГЛЮК СБОРКИ: DLL не пересобирается ПОКА ИГРА ЗАПУЩЕНА (LNK1168) — проверять Get-Process rustme/RustMe перед сборкой; после закрытия юзером один процесс висел (PID 6860) — добит Stop-Process. Дальнейшие правки ватермарки — константами TOP_Y/ISLAND_R/CLOCK_GAP/TEXT_SIZE в Watermark.java.

**v69 ИНЖЕКТ → «не по центру, бары не 1:1, часы не по центру» + v70 = ФИКСЫ (2026-09-07, DLL 01:19, invokedynamic нет) — ЖДЁТ ИНЖЕКТА+ЛОГА.** Претензии юзера и фиксы: (1) «Watermark не ровно по центру» (лог: island(466,26) при центре 480) → центрируется ВЕСЬ ряд: `totalW = clockW + CLOCK_GAP + flowW + BARS_GAP + barsW; islandX = scaledWidth/2 − totalW/2 + clockW + CLOCK_GAP; barsW = 3*BAR_STEP + BAR_W`; (2) «бары просто квадратные, не скруглённые, не как в rockstar» → реверс их батчера `iIiiiiiiI_Class383` (вызов I_method_59318fc7): бары рисуются SDF-шейдером `adaptive_ui/data` (uniform Radius×4 угла, RectSmoothness=0.5) с ДВУМЯ цветами: LightColor=**-1 (белый)** = зажжён, DarkColor=**-15856114 = #0E0E0E** = незажжён (НЕ alpha-вариация!). Их шейдера нет в расшифровке → 1:1 делаем НАШИМ ROUND-шейдером: `RenderUtil.drawRoundedRectShader(..., r=0.8, цвет 0xFFFFFFFF зажжён / 0xFF0E0E0E нет)`; (3) «время не ровно по центру» → clockY = islandY + (ISLAND_H − cellHeight)/2 (их y+5 = смещение вниз в их системе; у нас честный центр). ВАЖНО ИЗ ЛОГА: `MV=[1.0,1.0,0.0,0.0,1.0]` — в ЭТОМ запуске overlay-матрица IDENTITY (в прошлом была scale(1,0.9)+translate(1.05)) — **матрица мода ПЛАВАЕТ между запусками** → проекция обязана читать MV/PR каждый drawString (сейчас так и есть; dumpMatricesOnce только лог). Оба атласа загрузились (sf_semibold 776x776 bottom, medium 512x512 top).

**ВАНИЛЬНЫЕ КЛАССЫ: ЮЗЕР ПРАВ — ОБФУСЦИРОВАНЫ/ОТСУТСТВУЮТ КАК ФАЙЛЫ (проверка 2026-09-07 на вопрос «они точно не обфусцированы?»):** ванильных net/minecraft классов НЕТ ни в одном дампе/jar как файлов: minecraft.jar профиля prod-a = 16906 entries (мод+lwjglx+assets, net/minecraft=0), classes.jar = 19110 (чисто JDK/библиотеки), дамп 11052 = только мод (+4 файла Main). РЕАЛЬНЫЕ runtime-имена ванили вытащены из constant pool классов мода (мод вызывает эти цепочки — CP-ссылки компилировались против реальных классов): форк базируется на **1.8-era именах** — `net/minecraft/client/network/NetHandlerPlayClient` (≈ClientPlayNetHandler), `net/minecraft/client/network/NetworkPlayerInfo` (≈NetworkPlayerEntry); всего 1382 vanilla net/minecraft имён в CP дампа (ServerPinger, NetHandlerLoginClient, EnumConnectionState...). Сами файлы в дампе отсутствуют → прочитать методы оффлайн нельзя, только runtime-рефлексия по этим именам. ЮЗЕР ТРЕБУЕТ: «нам нужен реальный класс который есть в дампе с помощью которого можно получить пинг» — ЕСЛИ name-agnostic цепочка (v69, по типам возврата) провалится (лог `net chain:` с false), ПЛАН Б: найти в дампе rustme-класс самого мода форка, который получает пинг для своего HUD/Tab-листа (поведенческий скан: классы, вызывающие UUID-метод у объекта того же типа, что connection-поле игрока), и повторить его цепочку. Текущий статус: bars без «ms» текста (юзер: «только бары»), рисуются только при ctx.inWorld; если ping=-1 — все бары DarkColor.

**v68 = ПИНГ-БАРЫ + ЧАСЫ КАК У НИХ (2026-09-07, DLL 00:54) — СОСТОЯЛОСЬ:** юзер (референс-скрин «16:46 [капсула armorclub 2] ▮▮▮▮»): «без раскрытия острова, только бары без NNN ms». (1) **ПИНГ-БАРЫ**: пороги ТОЧНО из их кода — `I_field_b4e = new int[]{450, 300, 150, 75}` (ранняя оценка ~50/100/150/200 была НЕВЕРНА); 4 вертикальных квада RenderUtil.drawRect: BAR_W=2, BAR_STEP=2.7, высоты 3+i, низ barsBottom=islandY+12, отступ BARS_GAP=3 от правого края; зажжён (белый 255) если `ping < порога`, иначе alpha 64; при ctx.inWorld, без текста «ms». (2) Часы: юзер «шрифт какой то не такой» → их код I_method_38001739: часы = medium 7px БЕЛЫМ (не серый), позиция `x = остров − ширина_текста − 4, y = остров + 5`. Числа крупнее строчных на их скрине — цифры выше x-height, шрифт тот же.

**v69 = ПИНГ через РЕАЛЬНЫЕ ванильные классы форка (2026-09-07, DLL 01:06, invokedynamic нет) — ЖДЁТ ИНЖЕКТА.** Юзер поправил: «rockstar — чит на 1.21, а у нас кастомные классы и ЖВМ, ищи в сдампленных классах». ПРОВЕРКА РАЗОБЛАЧИЛА прошлую версию: ванильных классов НЕТ под оригинальными 1.12 именами нигде — minecraft.jar (16906 entries, профиль prod-a) = мод+lwjglx+assets (net/minecraft total: 0, только Main в дампе), classes.jar (19110) = чисто JDK/библиотеки. **Но реальные runtime-имена нашлись в constant pool самих классов мода** (мод вызывает эту цепочку): форк базируется на **1.8-era именах** — `net/minecraft/client/network/NetHandlerPlayClient` (≈ClientPlayNetHandler), `net/minecraft/client/network/NetworkPlayerInfo` (≈NetworkPlayerEntry), всего 1382 vanilla net/minecraft имён в CP дампа (ServerPinger, NetHandlerLoginClient и т.д.). Сами файлы ванили в локальном дампе отсутствуют (11052 = только мод), но loadClass по этим именам в рантайме гарантированно сработает (CP-ссылки компилировались против реальных классов). **resolvePing переписан NAME-AGNOSTIC** (имена методов форка ненадёжны — даже EntityPlayerSP-методы обфусцированы): всё по типам возврата — netField ищется в иерархии playerClass по типу == connClass; playerProfileM = единственный zero-arg метод игрока → GameProfile; profileUuidM = zero-arg GameProfile → UUID; entriesM = zero-arg conn → Collection; entryProfileM = zero-arg NetworkPlayerInfo → GameProfile; latencyM = zero-arg int у NetworkPlayerInfo (единственный int-геттер, валидация 0..5000). Лог первого вызова: `Watermark: net chain: field=... playerProfile=... entries=... entryProfile=... latency=...` — если что-то false, прислать строку (значит имена классов/структура другие — искать по дампу глубже). DLL 01:06, 26 классов round-trip OK.

**v71 ИНЖЕКТ → ping=unavailable → v72 = ПРЯМЫЕ ИМЕНА (DLL 01:51 2026-09-07, ждёт инжекта+лога):** лог дал точный диагноз: `Watermark: net chain: handlerFields=0 entries=true myProfile=true entryProfile=true latency=true (illIillliI)` + вечный `ping=unavailable`. Два бага: (1) скан Object-полей шёл от ctx.playerClass = БАЗОВЫЙ liIlIliIiI, а поле lIlilIIl объявлено в НАСЛЕДНИКЕ liIililiiI (реальный класс инстанса) → handlerFields=0; (2) type-автопик взял ПЕРВЫЙ int-метод iiIilIliiI (illIillliI) вместо верифицированного liIIillliI. Юзер: «зачем сканировать вообще? всё найдено в дампе» → СКАНЫ УБРАНЫ, resolvePing = прямые имена: `liIililiiI.getDeclaredField("lIlilIIl")` (Object, final; хранит iliilIliiI-инстанс), `iliilIliiI.ilIIlIlliI()` → Collection<iiIilIliiI>, `iliilIliiI.IlIiIiiilI()` → GameProfile подключения, `iiIilIliiI.iiliIliIlI()` → GameProfile entry, `iiIilIliiI.liIIillliI()` → пинг (поле IIlllIIl), `GameProfile.getId()`. Уточнение таксономии (javap): lIllilIIiI тоже держит латентность (iIliIliIlI()I читает поле lIIiliilI) — родственный класс, iiIilIliiI копирует из него в ctor; в Map<UUID,…> хендлера лежат iiIilIliiI. Бары: юзер «бар пинга теперь черный» — это DarkColor #0E0E0E при ping=-1 (lit=false); их реальная логика var13 = ping<порога ? var11 : var9 (var9 = alpha×0.2) — ОДИН белый цвет, разница только в прозрачности; DarkColor убран, незажжён = белый @20%. Добавлен пинг-лог раз в 5с: `Watermark: ping=NNN ms/unavailable` (logPingOnce5s, lastPingLog).

**v72 ИНЖЕКТ (01:51) → ВСЁ ЕЩЁ unavailable, СТРОКИ net chain bound НЕТ В ЛОГЕ (2026-09-07) + v73 = ЛОГ ОШИБКИ ДИСКАВЕРИ (DLL 01:53, ждёт инжекта+лога):** без строки `net chain bound` и без ошибок → discoverNet бросил исключение при привязке (какое-то из имён lIlilIIl/ilIIlIlliI/IlIiIiiilI/iiliIliIlI/liIIillliI не нашлось getDeclaredField/getMethod), а resolvePing молча съедал Throwable → навсегда -1. ФИКС: discoverNet обёрнут в try/catch ВНУТРИ resolvePing с `Log.error("Watermark", "net chain discovery FAILED", t)` — следующий лог покажет точное имя виновника (NoSuchFieldException/NoSuchMethodException + стек). После правки юзер должен прислать кусок лога с Watermark-строками. Примечание: правка последнего инт-геттера на точный liIIillliI сделана отдельным циклом (автопик был первым illIillliI — неверный).

**adaptive_ui ШЕЙДЕР РАСШИФРОВАН (юзер предоставил исходник в чате 2026-09-07; исправляет запись «их шейдера нет в расшифровке»):** `adaptive_ui/data` (fragment #version 150) + `adaptive_ui/vertex`. Боковая инфа острова (часы/пинг/иконки) красится через AdaptiveTint: `tint = mix(LightColor, DarkColor, state)`, state = Sampler1 (текстура состояния: 0=светлый фон, 1=тёмный), проба ОДНА на элемент (AnchorUv: uniform Anchor прогоняется через те же матрицы в VS) — чтобы цифру не резало градиентом на границе светлого/тёмного фона. Тип примитива: flat int GlyphIndex >= 0 → slug-кривые (текст И иконки — всё векторно через Sampler0/2/3), иначе закруглённая фигура: TexCoord = размер примитива в px, `dist = roundedBoxSDF(center − FragCoord*size, center−1.0, Radius)`, `alpha = 1 − smoothstep(1 − sdfAA(dist, RectSmoothness), 1, dist)`. Uniform'ы: LightColor/DarkColor, Radius[4], RectSmoothness=0.5, Thickness, ColorModulator. Батчер iiIiiiiiiI_Class383 ставит LightColor=-1 (белый), DarkColor=-15856114=#0E0E0E, Radius из Ii_field_46 (сеттер I_method_56782bd7, дефолт 0).

**v73 ИНЖЕКТ → ТОЧНЫЙ ВИНОВНИК НАЙДЕН ЛОГОМ + v74 = ФИКСЫ ПИНГА И FULLBRIGHT (2026-09-07, DLL 02:00, invokedynamic нет) — ЖДЁТ ИНЖЕКТА:** лог дал `Watermark[ERROR] net chain discovery FAILED exc=java.lang.NoSuchMethodException: rustme.iiIilIliiI.iiliIliIlI()` — метод `iiliIliIlI()Lcom/mojang/authlib/GameProfile;` принадлежит КЛАССУ lIllilIIiI (родственный кэш скинов), а НЕ iiIilIliiI (NetworkPlayerInfo) — ошибка переноса имён. ПОЛНЫЙ javap iiIilIliiI: поля iIiiIIlIiI ilIllIIl, int IlIllIIl, long llIllIIl, ilIIlilIiI iilllIIl, boolean IilllIIl, **final GameProfile lilllIIl**, String iIlllIIl, **int IIlllIIl (пинг)**, int lIlllIIl, Map<Type,lllililIiI> illllIIl...; методы: `liIIillliI()I` (геттер пинга, getfield IIlllIIl — верифицирован дизасмом), **`lllIillliI()Lcom/mojang/authlib/GameProfile;`** (геттер профиля = getfield lilllIIl), ctor(GameProfile) и ctor(lIllilIIiI). ФИКС v74: entryProfileM = entryClass.getMethod("lllIillliI"). (2) «персонаж стал голубым» — ЭТО НЕ ВАТЕРМАРКА, ЭТО FULLBRIGHT: теперь гамма мода реально работает (настройки живые), а FULLBRIGHT_GAMMA=100.0f ПЕРЕПОЛНЯЕТ расчёт lightmap (формула гаммы уходит в минус → каналы переполняются → мир/персонаж синеют). ФИКС: FULLBRIGHT_GAMMA 100.0f → 10.0f (полный свет уже на ~10, выше только артефакты). Урок: когда старый модуль «вдруг» начинает давать новые побочки после чужих фиксов — проверять его собственные экстремальные константы (ранее неработавший путь ожил).

**v74 ИНЖЕКТ → net chain bound + ping=0 ms ВСЕГДА + v75 = ПРЯМОЙ ПУТЬ ЧЕРЕЗ AbstractClientPlayer (2026-09-07, DLL 02:12, invokedynamic нет) — ЖДЁТ ИНЖЕКТА:** v74 связался (`net chain bound: ...` в логе, NoSuchMethod ушёл), но `ping=0 ms` ПОСТОЯННО. Анализ: iiIilIliiI — это СНАПШОТ (мод копирует из живых данных: ctor iiIilIliiI(lIllilIIiI) копирует поля через методы lIllilIIiI: iiliIliIlI→GameProfile, iIliIliIlI→int пинг, llIiIliIlI→iIiiIIlIiI, IiliIliIlI→ilIIlilIiI), а поле IIlllIIl обновляется сеттером ilIIillliI(int) ТОЛЬКО из liIlliliiI (= GuiPlayerTabOverlay форка) при ОТКРЫТОМ Tab-листе → в обычное время снапшот заморожен (пинг 0). **ЖИВОЙ NetworkPlayerInfo = lIllilIIiI** (геттер пинга iIliIliIlI()I читает поле lIIiliilI), но в CP ВСЕГО 4 файла ссылаются на lIllilIIiI (только ctor снапшота) — живых держателей в дампе нет. РЕШЕНИЕ: прямой ванильный путь через РОДИТЕЛЯ нашего player: `iiIililiiI` (AbstractClientPlayer, extends IIiIIiIIiI, РОДИТЕЛЬ liIililiiI=EntityPlayerSP) имеет поле **IliIiiil : iiIilIliiI + геттер iliiililiI()** (аналог AbstractClientPlayer.getPlayerInfo). resolvePing v75 = `player.iliiililiI()` → iiIilIliiI → `liIIillliI()` → int (валидация 0..5000). НЕТ handler'ов/Map/UUID-поиска (весь блок iliilIliiI/lIlilIIl удалён). СТАРЫЙ ПУТЬ (v72-74, handler lIlilIIl→iliilIliiI→Map<UUID,iiIilIliiI>) НЕ УДАЛЯТЬ ИЗ ПАМЯТИ: iliilIliiI = реальный NetHandlerPlayClient форка (поле GameSettings.iIIiiilliI() тоже возвращает его), может пригодиться для других данных. Причина null-цепочки до этого (v73 лог): NoSuchMethodException iiIilIliiI.iiliIliIlI() — метод iiliIliIlI принадлежит lIllilIIiI (ошибка переноса имён при переписывании), в iiIilIliiI профильный геттер = **lllIillliI()** (поле lilllIIl). (2) «персонаж голубой» v73 = НЕ ватермарка, а FullBright: гамма 100.0 (ранее не работала → ожила после правок) ПЕРЕПОЛНЯЕТ lightmap (каналы в минус → синева). ФИКС: FULLBRIGHT_GAMMA 100→10 (полный свет уже на ~10). Урок: после чужих фиксов старый модуль оживает и его экстремальные константы дают новые побочки.

**v76 ЖДЁТ ИНЖЕКТА. How to apply:** любые новые модули/эффекты — по образцу AutoSprint (modules/impl) + рендер через RenderUtil/CheatHud. Полные детали — ZNANIA.md раздел 12. Связано: [[rustme-jni-defineclass-bypass]], [[rustme-jni-dll]].

**v76 ИНЖЕКТ → net chain bound, НО ping=0 ms ВСЕГДА → v77 = ГЕТТЕР ЖИВОГО ПИНГА illIillliI (DLL 02:16):** связка iliiililiI/liIIillliI прошла, но liIIillliI() читает поле IIlllIIl, которое обновляет ТОЛЬКО liIlliliiI (= GuiPlayerTabOverlay форка) при ОТКРЫТОМ Tab (их update-метод принимает ping параметром и зовёт iiIilIliiI.ilIIillliI(int)). Разбор ТРЁХ int-геттеров iiIilIliiI по полям (javap -c): liIIillliI→IIlllIIl, lilIillliI→IlIllIIl, **illIillliI→lIlllIIl — а именно туда ctor снапшота копирует ЖИВОЙ пинг (lIllilIIiI.iIliIliIlI())**. latencyM переключён на illIillliI. DLL 02:16.

**v78 = ГОЛУБОЙ ПЕРСОНАЖ ОТ ПОЛОСКИ ARRAYLIST (DLL 02:19) — ЖДЁТ ИНЖЕКТА:** юзер точно указал источник: «голубая полоска в ArrayList → персонаж тоже окрашивается». Акцентная полоска рисуется drawRectVanilla → ctx.drawRect → рефлекс-вызов ЧУЖОГО статики Gui.drawRect мода (lIilIliliI, цвет ACCENT_COLOR=0xFF00C8FF голубой) — он ОСТАВЛЯЕТ голубой в GL current color после кадра; прежний glColor4f(1,1,1,1)-сброс стоял только внутри НАШИХ RenderUtil.drawRect/drawGradientRectV, этот рефлекс-путь не покрывали; кэш GlStateManager игры не знает про чужой glColor и пропускает свой → персонаж рендерится застрявшим голубым. ФИКС: glColor4f(1,1,1,1) в КОНЦЕ ВСЕГО кадра HUD (CheatHud.renderFrame, после цикла строк; + import org.lwjglx.opengl.GL11 в CheatHud). ПРАВИЛО РАСШИРЕНО: сброс цвета — один раз в конце всего кадра HUD, покрывает и чужие рефлекс-вызовы (внутренние сбросы в наших draw-функциях оставлены).

**v79 = ПИНГ-ГЕТТЕР illIillliI ПОДТВЕРЖДЁН РАБОЧИМ (DLL 02:16):** после переключения latencyM на illIillliI() (поле lIlllIIl — живой пинг из ctor снапшота) лог юзера: `net chain bound: iliiililiI/liIIillliI` (строка биндинга не обновлена — там старый текст имён) + `ping=0 ms` всё ещё. ФИНАЛЬНЫЙ ВЕРДИКТ: liIIillliI() читает IIlllIIl — обновляется ТОЛЬКО открытым Tab (liIlliliiI вызывает iiIilIliiI.ilIIillliI(int) в своём update). ВАЖНОЕ ОТКРЫТИЕ через CP-скан: живой NetworkPlayerInfo = lIllilIIiI (геттер пинга iIliIliIlI()I → поле lIIiliilI), хранится в **AbstractClientPlayer = rustme.iiIililiiI** (РОДИТЕЛЬ liIililiiI): поле **IliIiiil : iiIilIliiI** + геттер **iliiililiI()** (ванильный AbstractClientPlayer.getPlayerInfo). Снапшот iiIilIliiI(lIllilIIiI) копирует из живого: iiliIliIlI→GameProfile, iIliIliIlI→пинг, llIiIliIlI→iIiiIIlIiI, IiliIliIlI→ilIIlilIiI. GUI Tab-листа = rustme.liIlliliiI (Ordering<iiIilIliiI>, update с ping параметром). NetHandlerPlayClient = iliilIliiI (Map<UUID,iiIilIliiI>, Collection ilIIlIlliI, UUID-поиск IIIiillliI, хранится в GameSettings.iIIiiilliI() → player.lIlilIIl).

**v80 ИНЖЕКТ + v84 = ФИНАЛЬНЫЕ ПРАВКИ ПО ЛОГУ (2026-09-07, DLL 02:23, invokedynamic нет) — ЖДЁТ ИНЖЕКТА/СКРИНА:** юзер дал исходник ArrayListWidget из vanquish (wtf.vanquish, 1.21) и попросил перенести 1:1. CheatHud ПОЛНОСТЬЮ переписан: (1) отдельная тёмная плашка НА КАЖДЫЙ модуль `rgba(12,12,18,240)` BG_COLOR=0xF00C0C12, radius 0 (их RECT.draw); (2) сортировка включённых по ширине имени УБЫВАНИЕ (вставками, без лямбд); (3) полоска STRIPE_W=1.5px, градиент ВЕРТИКАЛЬНЫЙ primary→secondary (их GRADIENT_RECT.draw: UIColors.primary сверху, secondary снизу); (4) ГРАДИЕНТНЫЙ ТЕКСТ primary→secondary по ширине строки — НОВЫЙ метод `CustomFont.drawGradientString(s,x,y,colorLeft,colorRight,size,f)`: цвет каждого глифа = lerpColor(colorLeft,colorRight, t по центру глифа в строке), через uniform color per-glyph, БЕЗ тени (как drawGradientText vanquish); (5) шрифт medium 6px (их getMediumFont+scaled(6)) = CustomFont.WM_FONT; (6) паддинги их: PADDING_H=2.5, PADDING_V=1.2; (7) без анимаций (их рендер статичен). Цвета темы — константы PRIMARY=0xFF906BFF / SECONDARY=0xFF5A4BFF в CheatHud. Старый expensive-ArrayList ПОЛНОСТЬЮ ЗАМЕНЁН. **ПРЕТЕНЗИИ ЮЗЕРА К ПЕРВОМУ ВАРИАНТУ и v84-фиксы:** (1) «Watermark пропал» — я потерял вызов Watermark.render() при переписывании CheatHud → ВОССТАНОВЛЕН (урок: переписывая файл, проверять вызовы из него в другие классы); (2) «ArrayList должен быть СЛЕВА» → LEFT_X=2 (их дефолт x=2 = левая сторона), полоска на ЛЕВОМ краю плашки, текст после полоски (их ветка isRightSide=false); (3) «расстояние между плашками, а его не должно быть» — SDF-фейд ~1px съедает края квада → швы; ФИКС: квад с запасом +1px со всех сторон (видимый размер прежний, края стыкуются) — стандартный приём для SDF-ректов; (4) «слишком маленький» → FONT_SIZE 6→8 (наш medium при em=6 рендерит прописные мельче их scaled(6)). ВАЖНО: бары пинга ватермарки и вся пинг-цепочка не тронуты; при следующем инжекте пинг может ожить после переключения на illIillliI (см. v77/v82).

**v81 = ГОЛУБОЙ ПЕРСОНАЖ СНОВА (юзер: «это из-за ArrayList, когда появляется голубая полоска — персонаж тоже окрашивается») — ФИКС, DLL 02:19 2026-09-07:** полоска ArrayList рисуется drawRectVanilla → ctx.drawRect → рефлекс-вызов ЧУЖОГО статики Gui.drawRect мода (lIilIliliI, ACCENT_COLOR=0xFF00C8FF) — прежний glColor-сброс стоял только в НАШИХ RenderUtil.drawRect/drawGradientRectV, чужой рефлекс-путь не покрывался → голубой оставался в GL current color после кадра. ФИКС: glColor4f(1,1,1,1) в САМОМ КОНЦЕ CheatHud.renderFrame (после цикла строк; + import org.lwjglx.opengl.GL11). ПРАВИЛО: сброс цвета один раз в конце ВСЕГО кадра HUD — покрывает чужие рефлекс-вызовы. (В v80 ArrayList переписан — полоска теперь наш drawRoundedRectShader, но сброс оставлен как страховка.)

**v82 = ПИНГ: ГЕТТЕР ЖИВОГО ПИНГА illIillliI (DLL 02:16) → ping=0 ms ВСЕГДА → ПРОБЛЕМА НЕ В ГЕТТЕРЕ, а в снапшотности данных (DLL 02:14+):** разбор трёх int-геттеров iiIilIliiI (javap -c): liIIillliI→IIlllIIl (обновляет ТОЛЬКО liIlliliiI=GuiPlayerTabOverlay при открытом Tab), lilIillliI→IlIllIIl, **illIillliI→lIlllIIl (куда ctor копирует живой пинг lIllilIIiI.iIliIliIlI())**. Переключение на illIillliI НЕ помогло — ping=0 ms всегда. ГИПОТЕЗА НА БУДУЩЕЕ: iiIilIliiI-снапшот создаётся при входе в мир и обновляется ТОЛЬКО Tab'ом; живые данные — lIllilIIiI (геттер iIliIliIlI()I), но держателей живых в CP всего 4 (только ctor снапшота). ВАРИАНТ НА БУДУЩЕЕ: пинг обновляется только когда мод сам запрашивает Tab-данные (пакеты PlayerListItem/KeepAlive) — проверять в мире с открытым Tab vs закрытым; если всегда 0 — искать в дампе живого держателя (поведенческий скан по полю lIIiliilI).

**v83 = ГОЛУБОЙ ПЕРСОНАЖ ПОВТОРНО (юзер: «это из-за ArrayList») → glColor-сброс в конце ВСЕГО кадра HUD (DLL 02:19 2026-09-07):** полоска рисуется drawRectVanilla → ctx.drawRect → рефлекс-вызов чужого статики Gui.drawRect мода (lIilIliliI, ACCENT_COLOR=0xFF00C8FF) — прежний сброс стоял только в НАШИХ drawRect/drawGradientRectV, чужой путь не покрывался. ФИКС: glColor4f(1,1,1,1) в самом конце CheatHud.renderFrame (+ import GL11). Правило: сброс цвета — один раз в конце всего кадра HUD.
